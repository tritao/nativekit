// Generated by Haxeon from bindings/haxe/nativekit_import.h for portable-abi64. Do not edit.
interface NativeKit @target("portable-abi64") @library("nativekit") {
	/**
	 * The platform-provided host root; it cannot be removed or replaced.
	 */
	const NK_ACCESSIBILITY_ROOT = 0;
	/**
	 * Sentinel meaning that no text position or selection is supplied.
	 */
	const NK_ACCESSIBILITY_TEXT_POSITION_NONE = -1;
	/**
	 * Version of the NativeKit C ABI described by these headers.
	 */
	const NK_API_VERSION = 2;
	/**
	 * The hat is centered; no direction is active.
	 */
	const NK_JOYSTICK_HAT_CENTERED = 0;
	/**
	 * The hat is pressed downward.
	 */
	const NK_JOYSTICK_HAT_DOWN = 4;
	/**
	 * The hat is pressed to the left.
	 */
	const NK_JOYSTICK_HAT_LEFT = 8;
	/**
	 * The hat is pressed to the right.
	 */
	const NK_JOYSTICK_HAT_RIGHT = 2;
	/**
	 * The hat is pressed upward.
	 */
	const NK_JOYSTICK_HAT_UP = 1;
	/**
	 * The 0 key.
	 */
	const NK_KEY_0 = 48;
	/**
	 * The 1 key.
	 */
	const NK_KEY_1 = 49;
	/**
	 * The 2 key.
	 */
	const NK_KEY_2 = 50;
	/**
	 * The 3 key.
	 */
	const NK_KEY_3 = 51;
	/**
	 * The 4 key.
	 */
	const NK_KEY_4 = 52;
	/**
	 * The 5 key.
	 */
	const NK_KEY_5 = 53;
	/**
	 * The 6 key.
	 */
	const NK_KEY_6 = 54;
	/**
	 * The 7 key.
	 */
	const NK_KEY_7 = 55;
	/**
	 * The 8 key.
	 */
	const NK_KEY_8 = 56;
	/**
	 * The 9 key.
	 */
	const NK_KEY_9 = 57;
	/**
	 * The A key.
	 */
	const NK_KEY_A = 65;
	/**
	 * The apostrophe key.
	 */
	const NK_KEY_APOSTROPHE = 39;
	/**
	 * The B key.
	 */
	const NK_KEY_B = 66;
	/**
	 * The backslash key.
	 */
	const NK_KEY_BACKSLASH = 92;
	/**
	 * The Backspace key.
	 */
	const NK_KEY_BACKSPACE = 259;
	/**
	 * The C key.
	 */
	const NK_KEY_C = 67;
	/**
	 * The Caps Lock key.
	 */
	const NK_KEY_CAPS_LOCK = 280;
	/**
	 * The comma key.
	 */
	const NK_KEY_COMMA = 44;
	/**
	 * The D key.
	 */
	const NK_KEY_D = 68;
	/**
	 * The Delete key.
	 */
	const NK_KEY_DELETE = 261;
	/**
	 * The Down Arrow key.
	 */
	const NK_KEY_DOWN = 264;
	/**
	 * The E key.
	 */
	const NK_KEY_E = 69;
	/**
	 * The End key.
	 */
	const NK_KEY_END = 269;
	/**
	 * The Enter or Return key.
	 */
	const NK_KEY_ENTER = 257;
	/**
	 * The equals key.
	 */
	const NK_KEY_EQUAL = 61;
	/**
	 * The Escape key.
	 */
	const NK_KEY_ESCAPE = 256;
	/**
	 * The F key.
	 */
	const NK_KEY_F = 70;
	/**
	 * The F1 key.
	 */
	const NK_KEY_F1 = 290;
	/**
	 * The F10 key.
	 */
	const NK_KEY_F10 = 299;
	/**
	 * The F11 key.
	 */
	const NK_KEY_F11 = 300;
	/**
	 * The F12 key.
	 */
	const NK_KEY_F12 = 301;
	/**
	 * The F13 key.
	 */
	const NK_KEY_F13 = 302;
	/**
	 * The F14 key.
	 */
	const NK_KEY_F14 = 303;
	/**
	 * The F15 key.
	 */
	const NK_KEY_F15 = 304;
	/**
	 * The F16 key.
	 */
	const NK_KEY_F16 = 305;
	/**
	 * The F17 key.
	 */
	const NK_KEY_F17 = 306;
	/**
	 * The F18 key.
	 */
	const NK_KEY_F18 = 307;
	/**
	 * The F19 key.
	 */
	const NK_KEY_F19 = 308;
	/**
	 * The F2 key.
	 */
	const NK_KEY_F2 = 291;
	/**
	 * The F20 key.
	 */
	const NK_KEY_F20 = 309;
	/**
	 * The F21 key.
	 */
	const NK_KEY_F21 = 310;
	/**
	 * The F22 key.
	 */
	const NK_KEY_F22 = 311;
	/**
	 * The F23 key.
	 */
	const NK_KEY_F23 = 312;
	/**
	 * The F24 key.
	 */
	const NK_KEY_F24 = 313;
	/**
	 * The F25 key.
	 */
	const NK_KEY_F25 = 314;
	/**
	 * The F3 key.
	 */
	const NK_KEY_F3 = 292;
	/**
	 * The F4 key.
	 */
	const NK_KEY_F4 = 293;
	/**
	 * The F5 key.
	 */
	const NK_KEY_F5 = 294;
	/**
	 * The F6 key.
	 */
	const NK_KEY_F6 = 295;
	/**
	 * The F7 key.
	 */
	const NK_KEY_F7 = 296;
	/**
	 * The F8 key.
	 */
	const NK_KEY_F8 = 297;
	/**
	 * The F9 key.
	 */
	const NK_KEY_F9 = 298;
	/**
	 * The G key.
	 */
	const NK_KEY_G = 71;
	/**
	 * The grave-accent key.
	 */
	const NK_KEY_GRAVE_ACCENT = 96;
	/**
	 * The H key.
	 */
	const NK_KEY_H = 72;
	/**
	 * The Home key.
	 */
	const NK_KEY_HOME = 268;
	/**
	 * The I key.
	 */
	const NK_KEY_I = 73;
	/**
	 * The Insert key.
	 */
	const NK_KEY_INSERT = 260;
	/**
	 * The J key.
	 */
	const NK_KEY_J = 74;
	/**
	 * The K key.
	 */
	const NK_KEY_K = 75;
	/**
	 * The numeric keypad 0 key.
	 */
	const NK_KEY_KP_0 = 320;
	/**
	 * The numeric keypad 1 key.
	 */
	const NK_KEY_KP_1 = 321;
	/**
	 * The numeric keypad 2 key.
	 */
	const NK_KEY_KP_2 = 322;
	/**
	 * The numeric keypad 3 key.
	 */
	const NK_KEY_KP_3 = 323;
	/**
	 * The numeric keypad 4 key.
	 */
	const NK_KEY_KP_4 = 324;
	/**
	 * The numeric keypad 5 key.
	 */
	const NK_KEY_KP_5 = 325;
	/**
	 * The numeric keypad 6 key.
	 */
	const NK_KEY_KP_6 = 326;
	/**
	 * The numeric keypad 7 key.
	 */
	const NK_KEY_KP_7 = 327;
	/**
	 * The numeric keypad 8 key.
	 */
	const NK_KEY_KP_8 = 328;
	/**
	 * The numeric keypad 9 key.
	 */
	const NK_KEY_KP_9 = 329;
	/**
	 * The numeric keypad add key.
	 */
	const NK_KEY_KP_ADD = 334;
	/**
	 * The numeric keypad decimal key.
	 */
	const NK_KEY_KP_DECIMAL = 330;
	/**
	 * The numeric keypad divide key.
	 */
	const NK_KEY_KP_DIVIDE = 331;
	/**
	 * The numeric keypad Enter key.
	 */
	const NK_KEY_KP_ENTER = 335;
	/**
	 * The numeric keypad equals key.
	 */
	const NK_KEY_KP_EQUAL = 336;
	/**
	 * The numeric keypad multiply key.
	 */
	const NK_KEY_KP_MULTIPLY = 332;
	/**
	 * The numeric keypad subtract key.
	 */
	const NK_KEY_KP_SUBTRACT = 333;
	/**
	 * The L key.
	 */
	const NK_KEY_L = 76;
	/**
	 * Highest key value; useful for sizing key-state arrays.
	 */
	const NK_KEY_LAST = 348;
	/**
	 * The Left Arrow key.
	 */
	const NK_KEY_LEFT = 263;
	/**
	 * The left Alt key.
	 */
	const NK_KEY_LEFT_ALT = 342;
	/**
	 * The left bracket key.
	 */
	const NK_KEY_LEFT_BRACKET = 91;
	/**
	 * The left Control key.
	 */
	const NK_KEY_LEFT_CONTROL = 341;
	/**
	 * The left Shift key.
	 */
	const NK_KEY_LEFT_SHIFT = 340;
	/**
	 * The left Super, Command, or Windows key.
	 */
	const NK_KEY_LEFT_SUPER = 343;
	/**
	 * The M key.
	 */
	const NK_KEY_M = 77;
	/**
	 * The Menu or context-menu key.
	 */
	const NK_KEY_MENU = 348;
	/**
	 * The minus key.
	 */
	const NK_KEY_MINUS = 45;
	/**
	 * The N key.
	 */
	const NK_KEY_N = 78;
	/**
	 * The Num Lock key.
	 */
	const NK_KEY_NUM_LOCK = 282;
	/**
	 * The O key.
	 */
	const NK_KEY_O = 79;
	/**
	 * The P key.
	 */
	const NK_KEY_P = 80;
	/**
	 * The Page Down key.
	 */
	const NK_KEY_PAGE_DOWN = 267;
	/**
	 * The Page Up key.
	 */
	const NK_KEY_PAGE_UP = 266;
	/**
	 * The Pause key.
	 */
	const NK_KEY_PAUSE = 284;
	/**
	 * The period key.
	 */
	const NK_KEY_PERIOD = 46;
	/**
	 * The Print Screen key.
	 */
	const NK_KEY_PRINT_SCREEN = 283;
	/**
	 * The Q key.
	 */
	const NK_KEY_Q = 81;
	/**
	 * The R key.
	 */
	const NK_KEY_R = 82;
	/**
	 * The Right Arrow key.
	 */
	const NK_KEY_RIGHT = 262;
	/**
	 * The right Alt key.
	 */
	const NK_KEY_RIGHT_ALT = 346;
	/**
	 * The right bracket key.
	 */
	const NK_KEY_RIGHT_BRACKET = 93;
	/**
	 * The right Control key.
	 */
	const NK_KEY_RIGHT_CONTROL = 345;
	/**
	 * The right Shift key.
	 */
	const NK_KEY_RIGHT_SHIFT = 344;
	/**
	 * The right Super, Command, or Windows key.
	 */
	const NK_KEY_RIGHT_SUPER = 347;
	/**
	 * The S key.
	 */
	const NK_KEY_S = 83;
	/**
	 * The Scroll Lock key.
	 */
	const NK_KEY_SCROLL_LOCK = 281;
	/**
	 * The semicolon key.
	 */
	const NK_KEY_SEMICOLON = 59;
	/**
	 * The slash key.
	 */
	const NK_KEY_SLASH = 47;
	/**
	 * The Space key.
	 */
	const NK_KEY_SPACE = 32;
	/**
	 * The T key.
	 */
	const NK_KEY_T = 84;
	/**
	 * The Tab key.
	 */
	const NK_KEY_TAB = 258;
	/**
	 * The U key.
	 */
	const NK_KEY_U = 85;
	/**
	 * No supported key could be identified.
	 */
	const NK_KEY_UNKNOWN = 0;
	/**
	 * The Up Arrow key.
	 */
	const NK_KEY_UP = 265;
	/**
	 * The V key.
	 */
	const NK_KEY_V = 86;
	/**
	 * The W key.
	 */
	const NK_KEY_W = 87;
	/**
	 * The X key.
	 */
	const NK_KEY_X = 88;
	/**
	 * The Y key.
	 */
	const NK_KEY_Y = 89;
	/**
	 * The Z key.
	 */
	const NK_KEY_Z = 90;
	/**
	 * Authentication was required or rejected.
	 */
	const NK_NAVIGATION_ERROR_AUTH = 2;
	/**
	 * The navigation was cancelled.
	 */
	const NK_NAVIGATION_ERROR_CANCELLED = 6;
	/**
	 * The connection failed.
	 */
	const NK_NAVIGATION_ERROR_CONNECTION = 5;
	/**
	 * The requested resource was not found.
	 */
	const NK_NAVIGATION_ERROR_NOT_FOUND = 4;
	/**
	 * No more specific navigation failure category is available.
	 */
	const NK_NAVIGATION_ERROR_OTHER = 0;
	/**
	 * The request was malformed or rejected before transport.
	 */
	const NK_NAVIGATION_ERROR_REQUEST = 1;
	/**
	 * The request was blocked by a security policy.
	 */
	const NK_NAVIGATION_ERROR_SECURITY = 3;
	/**
	 * Sentinel meaning that a position, such as an absent composition, is not present.
	 */
	const NK_TEXT_POSITION_NONE = -1;
	/**
	 * Action requested by a platform accessibility service.
	 */
	enum nk_accessibility_action : u32 {
		/**
		 * Activate the node.
		 */
		NK_ACCESSIBILITY_ACTION_ACTIVATE = 1;
		/**
		 * Move accessibility focus to the node.
		 */
		NK_ACCESSIBILITY_ACTION_FOCUS = 2;
		/**
		 * Clear accessibility focus from the node.
		 */
		NK_ACCESSIBILITY_ACTION_CLEAR_FOCUS = 3;
		/**
		 * Set the node's value from the event's optional UTF-8 text.
		 */
		NK_ACCESSIBILITY_ACTION_SET_VALUE = 4;
		/**
		 * Set the node's selection from the event positions.
		 */
		NK_ACCESSIBILITY_ACTION_SET_SELECTION = 5;
		/**
		 * Increase the node's value.
		 */
		NK_ACCESSIBILITY_ACTION_INCREMENT = 6;
		/**
		 * Decrease the node's value.
		 */
		NK_ACCESSIBILITY_ACTION_DECREMENT = 7;
		/**
		 * Scroll the node forward.
		 */
		NK_ACCESSIBILITY_ACTION_SCROLL_FORWARD = 8;
		/**
		 * Scroll the node backward.
		 */
		NK_ACCESSIBILITY_ACTION_SCROLL_BACKWARD = 9;
		/**
		 * Move to the next semantic item.
		 */
		NK_ACCESSIBILITY_ACTION_MOVE_NEXT = 10;
		/**
		 * Move to the previous semantic item.
		 */
		NK_ACCESSIBILITY_ACTION_MOVE_PREVIOUS = 11;
	}
	/**
	 * Bit flags describing actions supported by an accessibility node.
	 */
	flags nk_accessibility_actions : u32 {
		/**
		 * The node accepts NK_ACCESSIBILITY_ACTION_ACTIVATE.
		 */
		NK_ACCESSIBILITY_CAN_ACTIVATE = 1;
		/**
		 * The node accepts focus and clear-focus actions.
		 */
		NK_ACCESSIBILITY_CAN_FOCUS = 2;
		/**
		 * The node accepts a new textual or numeric value.
		 */
		NK_ACCESSIBILITY_CAN_SET_VALUE = 4;
		/**
		 * The node accepts a new text selection.
		 */
		NK_ACCESSIBILITY_CAN_SET_SELECTION = 8;
		/**
		 * The node accepts an increment action.
		 */
		NK_ACCESSIBILITY_CAN_INCREMENT = 16;
		/**
		 * The node accepts a decrement action.
		 */
		NK_ACCESSIBILITY_CAN_DECREMENT = 32;
		/**
		 * The node accepts forward scrolling.
		 */
		NK_ACCESSIBILITY_CAN_SCROLL_FORWARD = 64;
		/**
		 * The node accepts backward scrolling.
		 */
		NK_ACCESSIBILITY_CAN_SCROLL_BACKWARD = 128;
		/**
		 * The node accepts moving to the next semantic item.
		 */
		NK_ACCESSIBILITY_CAN_MOVE_NEXT = 256;
		/**
		 * The node accepts moving to the previous semantic item.
		 */
		NK_ACCESSIBILITY_CAN_MOVE_PREVIOUS = 512;
	}
	/**
	 * Semantic role announced for an accessibility node.
	 */
	enum nk_accessibility_role : u32 {
		/**
		 * Generic grouping node.
		 */
		NK_ACCESSIBILITY_GROUP = 0;
		/**
		 * Activatable button.
		 */
		NK_ACCESSIBILITY_BUTTON = 1;
		/**
		 * Checkable toggle control.
		 */
		NK_ACCESSIBILITY_CHECKBOX = 2;
		/**
		 * Mutually exclusive selectable control.
		 */
		NK_ACCESSIBILITY_RADIO = 3;
		/**
		 * Static text content.
		 */
		NK_ACCESSIBILITY_TEXT = 4;
		/**
		 * Editable text control.
		 */
		NK_ACCESSIBILITY_TEXT_FIELD = 5;
		/**
		 * Navigable hyperlink.
		 */
		NK_ACCESSIBILITY_LINK = 6;
		/**
		 * Informational image.
		 */
		NK_ACCESSIBILITY_IMAGE = 7;
		/**
		 * Heading in the semantic document.
		 */
		NK_ACCESSIBILITY_HEADING = 8;
		/**
		 * Container representing a list.
		 */
		NK_ACCESSIBILITY_LIST = 9;
		/**
		 * Item inside a list.
		 */
		NK_ACCESSIBILITY_LIST_ITEM = 10;
		/**
		 * Numeric or ranged value control.
		 */
		NK_ACCESSIBILITY_SLIDER = 11;
		/**
		 * Scrollable content container.
		 */
		NK_ACCESSIBILITY_SCROLL_AREA = 12;
	}
	/**
	 * Bit flags describing the current state of an accessibility node.
	 */
	flags nk_accessibility_states : u32 {
		/**
		 * The node can receive accessibility focus.
		 */
		NK_ACCESSIBILITY_FOCUSABLE = 1;
		/**
		 * The node currently has accessibility focus.
		 */
		NK_ACCESSIBILITY_FOCUSED = 2;
		/**
		 * The node is selected.
		 */
		NK_ACCESSIBILITY_SELECTED = 4;
		/**
		 * A checkable node is checked.
		 */
		NK_ACCESSIBILITY_CHECKED = 8;
		/**
		 * The node is unavailable for interaction.
		 */
		NK_ACCESSIBILITY_DISABLED = 16;
		/**
		 * The node's value cannot be edited.
		 */
		NK_ACCESSIBILITY_READ_ONLY = 32;
		/**
		 * The node's value can contain multiple lines.
		 */
		NK_ACCESSIBILITY_MULTILINE = 64;
		/**
		 * The node's value should be announced as protected content.
		 */
		NK_ACCESSIBILITY_PASSWORD = 128;
		/**
		 * A node such as a group or tree item is expanded.
		 */
		NK_ACCESSIBILITY_EXPANDED = 256;
	}
	/**
	 * Text unit requested for a selection or navigation action.
	 */
	enum nk_accessibility_text_granularity : u32 {
		/**
		 * One Unicode code point.
		 */
		NK_ACCESSIBILITY_GRANULARITY_CHARACTER = 1;
		/**
		 * One word.
		 */
		NK_ACCESSIBILITY_GRANULARITY_WORD = 2;
		/**
		 * One line.
		 */
		NK_ACCESSIBILITY_GRANULARITY_LINE = 3;
		/**
		 * One paragraph.
		 */
		NK_ACCESSIBILITY_GRANULARITY_PARAGRAPH = 4;
		/**
		 * One page.
		 */
		NK_ACCESSIBILITY_GRANULARITY_PAGE = 5;
	}
	/**
	 * Flags selecting optional parts of an accessibility tree update.
	 */
	flags nk_accessibility_update_flags : u32 {
		/**
		 * Apply the `focus` field as part of the same atomic update.
		 */
		NK_ACCESSIBILITY_UPDATE_FOCUS = 1;
	}
	/**
	 * Bitmask describing the optional facilities exposed by the active backend.
	 */
	flags nk_capabilities : u64 {
		/**
		 * The backend can create and manage NativeKit-owned top-level windows.
		 */
		NK_CAP_WINDOW = 1;
		/**
		 * The backend can create embedded WebViews.
		 */
		NK_CAP_WEBVIEW = 2;
		/**
		 * The backend can show native file dialogs.
		 */
		NK_CAP_FILE_DIALOG = 4;
		/**
		 * The backend can read and write the system clipboard.
		 */
		NK_CAP_CLIPBOARD = 8;
		/**
		 * The backend can report and accept drag-and-drop data.
		 */
		NK_CAP_DRAG_DROP = 16;
		/**
		 * The backend can open files, URLs, or other resources with the system shell.
		 */
		NK_CAP_SHELL = 32;
		/**
		 * The backend can query or observe the system appearance.
		 */
		NK_CAP_SYSTEM_APPEARANCE = 64;
		/**
		 * The backend can export a descriptor for a NativeKit-owned native window.
		 */
		NK_CAP_EXPORT_NATIVE_WINDOW = 128;
		/**
		 * The backend can wrap a caller-owned native window descriptor.
		 */
		NK_CAP_WRAP_NATIVE_WINDOW = 256;
		/**
		 * The backend can post native system notifications.
		 */
		NK_CAP_NOTIFICATION = 512;
		/**
		 * The backend supports a mobile host integration.
		 */
		NK_CAP_MOBILE_HOST = 1024;
		/**
		 * The backend can deliver keyboard, text, pointer, or touch input events.
		 */
		NK_CAP_INPUT = 2048;
		/**
		 * The backend can create or expose an OpenGL surface.
		 */
		NK_CAP_OPENGL_SURFACE = 4096;
		/**
		 * The backend can create or expose an OpenGL ES surface.
		 */
		NK_CAP_OPENGL_ES_SURFACE = 8192;
		/**
		 * The backend supports cursor creation or window cursor control.
		 */
		NK_CAP_CURSOR = 16384;
		/**
		 * The backend supports capturing the pointer to a window.
		 */
		NK_CAP_POINTER_CAPTURE = 32768;
		/**
		 * The backend supports querying and changing window geometry.
		 */
		NK_CAP_WINDOW_GEOMETRY = 65536;
		/**
		 * The backend supports changing window style or behavior at runtime.
		 */
		NK_CAP_WINDOW_STYLING = 131072;
		/**
		 * The backend can enumerate and query monitors.
		 */
		NK_CAP_MONITOR = 262144;
		/**
		 * The backend supports monitor-specific fullscreen operations.
		 */
		NK_CAP_MONITOR_FULLSCREEN = 524288;
		/**
		 * The backend can enumerate and query raw joysticks.
		 */
		NK_CAP_JOYSTICK = 1048576;
		/**
		 * The backend supports sharing resources with another NativeKit subsystem.
		 */
		NK_CAP_RESOURCE_SHARING = 2097152;
		/**
		 * The backend supports NativeKit resource I/O.
		 */
		NK_CAP_RESOURCE_IO = 4194304;
		/**
		 * The backend can create or expose a Vulkan surface.
		 */
		NK_CAP_VULKAN_SURFACE = 8388608;
		/**
		 * The backend supports NativeKit accessibility integration.
		 */
		NK_CAP_ACCESSIBILITY = 16777216;
	}
	/**
	 * System color-scheme values returned in nk_system_appearance.
	 */
	enum nk_color_scheme : u32 {
		/**
		 * The platform did not report a color scheme.
		 */
		NK_COLOR_SCHEME_UNKNOWN = 0;
		/**
		 * The platform is using a light color scheme.
		 */
		NK_COLOR_SCHEME_LIGHT = 1;
		/**
		 * The platform is using a dark color scheme.
		 */
		NK_COLOR_SCHEME_DARK = 2;
	}
	/**
	 * Pointer presentation or capture mode for a window.
	 */
	enum nk_cursor_mode : u32 {
		/**
		 * Show the selected cursor normally.
		 */
		NK_CURSOR_MODE_NORMAL = 0;
		/**
		 * Hide the cursor while it is over the window.
		 */
		NK_CURSOR_MODE_HIDDEN = 1;
		/**
		 * Capture the pointer while it is interacting with the window.
		 */
		NK_CURSOR_MODE_CAPTURED = 2;
		/**
		 * Request disabled or relative-pointer behavior; may be unsupported.
		 */
		NK_CURSOR_MODE_DISABLED = 3;
	}
	/**
	 * Standard cursor shape understood by the active platform backend.
	 */
	enum nk_cursor_shape : u32 {
		/**
		 * The platform's default arrow cursor.
		 */
		NK_CURSOR_ARROW = 1;
		/**
		 * A text-entry or I-beam cursor.
		 */
		NK_CURSOR_IBEAM = 2;
		/**
		 * A crosshair cursor.
		 */
		NK_CURSOR_CROSSHAIR = 3;
		/**
		 * A link or hand cursor.
		 */
		NK_CURSOR_HAND = 4;
		/**
		 * A horizontal-resize cursor.
		 */
		NK_CURSOR_HORIZONTAL_RESIZE = 5;
		/**
		 * A vertical-resize cursor.
		 */
		NK_CURSOR_VERTICAL_RESIZE = 6;
		/**
		 * A diagonal northwest-southeast resize cursor.
		 */
		NK_CURSOR_NWSE_RESIZE = 7;
		/**
		 * A diagonal northeast-southwest resize cursor.
		 */
		NK_CURSOR_NESW_RESIZE = 8;
		/**
		 * A move cursor.
		 */
		NK_CURSOR_MOVE = 9;
		/**
		 * A cursor indicating that an operation is not allowed.
		 */
		NK_CURSOR_NOT_ALLOWED = 10;
	}
	/**
	 * Optional behavior flags for file and resource dialogs.
	 */
	flags nk_dialog_flags : u32 {
		/**
		 * Allow selecting more than one item in an open dialog.
		 */
		NK_DIALOG_ALLOW_MULTIPLE = 1;
		/**
		 * Ask for confirmation before replacing an existing destination.
		 */
		NK_DIALOG_CONFIRM_OVERWRITE = 2;
		/**
		 * Show hidden files and directories where supported.
		 */
		NK_DIALOG_SHOW_HIDDEN = 4;
	}
	/**
	 * Operation encoded in a dialog completion event's flags.
	 */
	enum nk_dialog_operation : u32 {
		/**
		 * Select one or more local files.
		 */
		NK_DIALOG_OPEN_FILE = 1;
		/**
		 * Select a destination for a local file.
		 */
		NK_DIALOG_SAVE_FILE = 2;
		/**
		 * Select a local directory.
		 */
		NK_DIALOG_SELECT_DIRECTORY = 3;
		/**
		 * Show a native message and collect the selected button.
		 */
		NK_DIALOG_MESSAGE = 4;
		/**
		 * Select one or more URI resources.
		 */
		NK_DIALOG_OPEN_RESOURCE = 5;
		/**
		 * Select a destination URI resource.
		 */
		NK_DIALOG_SAVE_RESOURCE = 6;
		/**
		 * Select a URI resource directory.
		 */
		NK_DIALOG_SELECT_RESOURCE_DIRECTORY = 7;
	}
	/**
	 * Numeric kind identifying the payload and source of an event.
	 */
	enum nk_event_kind : u32 {
		/**
		 * No event was available when the queue was polled.
		 */
		NK_EVENT_NONE = 0;
		/**
		 * The user requested that a window close.
		 */
		NK_EVENT_WINDOW_CLOSE = 1;
		/**
		 * The logical content size of a window changed.
		 */
		NK_EVENT_WINDOW_RESIZE = 2;
		/**
		 * The content-to-device pixel scale of a window changed.
		 */
		NK_EVENT_WINDOW_SCALE_CHANGED = 3;
		/**
		 * The native window state flags changed.
		 */
		NK_EVENT_WINDOW_STATE_CHANGED = 4;
		/**
		 * The logical position of a window changed.
		 */
		NK_EVENT_WINDOW_MOVE = 5;
		/**
		 * The device-pixel framebuffer size of a window changed.
		 */
		NK_EVENT_WINDOW_FRAMEBUFFER_RESIZE = 6;
		/**
		 * A keyboard key transitioned or repeated.
		 */
		NK_EVENT_KEY = 10;
		/**
		 * Committed Unicode text was entered.
		 */
		NK_EVENT_TEXT_INPUT = 11;
		/**
		 * The pointer moved within a window or graphics surface.
		 */
		NK_EVENT_POINTER_MOVE = 12;
		/**
		 * A pointer button transitioned.
		 */
		NK_EVENT_POINTER_BUTTON = 13;
		/**
		 * The pointer wheel or touchpad scroll position changed.
		 */
		NK_EVENT_POINTER_SCROLL = 14;
		/**
		 * The pointer entered or left a window or graphics surface.
		 */
		NK_EVENT_POINTER_ENTER = 15;
		/**
		 * A touch contact was created, moved, or released.
		 */
		NK_EVENT_TOUCH = 16;
		/**
		 * A structured text-edit transaction was produced by an IME.
		 */
		NK_EVENT_TEXT_EDIT = 17;
		/**
		 * A monitor became available.
		 */
		NK_EVENT_MONITOR_CONNECTED = 20;
		/**
		 * A monitor was disconnected and its handle invalidated.
		 */
		NK_EVENT_MONITOR_DISCONNECTED = 21;
		/**
		 * A raw joystick became available.
		 */
		NK_EVENT_JOYSTICK_CONNECTED = 30;
		/**
		 * A raw joystick was disconnected and its handle invalidated.
		 */
		NK_EVENT_JOYSTICK_DISCONNECTED = 31;
		/**
		 * A raw joystick axis changed.
		 */
		NK_EVENT_JOYSTICK_AXIS = 32;
		/**
		 * A raw joystick button changed.
		 */
		NK_EVENT_JOYSTICK_BUTTON = 33;
		/**
		 * A raw joystick hat changed.
		 */
		NK_EVENT_JOYSTICK_HAT = 34;
		/**
		 * A mapped gamepad axis changed.
		 */
		NK_EVENT_GAMEPAD_AXIS = 35;
		/**
		 * A mapped gamepad button changed.
		 */
		NK_EVENT_GAMEPAD_BUTTON = 36;
		/**
		 * An asynchronous file or directory dialog returned paths.
		 */
		NK_EVENT_DIALOG_PATHS_COMPLETE = 100;
		/**
		 * An asynchronous resource dialog returned resource URIs.
		 */
		NK_EVENT_DIALOG_RESOURCES_COMPLETE = 101;
		/**
		 * An asynchronous message dialog returned a button result.
		 */
		NK_EVENT_DIALOG_MESSAGE_COMPLETE = 102;
		/**
		 * A WebView completed a navigation.
		 */
		NK_EVENT_WEBVIEW_NAVIGATED = 200;
		/**
		 * A WebView delivered a page-to-native message.
		 */
		NK_EVENT_WEBVIEW_MESSAGE = 201;
		/**
		 * A WebView page title changed.
		 */
		NK_EVENT_WEBVIEW_TITLE_CHANGED = 202;
		/**
		 * A WebView JavaScript evaluation completed.
		 */
		NK_EVENT_WEBVIEW_EVAL_COMPLETE = 203;
		/**
		 * A WebView navigation failed.
		 */
		NK_EVENT_WEBVIEW_NAVIGATION_FAILED = 204;
		/**
		 * A WebView rendering process terminated and its handle was invalidated.
		 */
		NK_EVENT_WEBVIEW_PROCESS_TERMINATED = 205;
		/**
		 * A WebView finished asynchronous creation.
		 */
		NK_EVENT_WEBVIEW_READY = 206;
		/**
		 * A WebView navigation requires an application allow/cancel decision.
		 */
		NK_EVENT_WEBVIEW_NAVIGATION_REQUEST = 207;
		/**
		 * Local files were dropped on a window.
		 */
		NK_EVENT_DROP_FILES = 300;
		/**
		 * Text was dropped on a window.
		 */
		NK_EVENT_DROP_TEXT = 301;
		/**
		 * An asynchronous clipboard text read completed.
		 */
		NK_EVENT_CLIPBOARD_TEXT_COMPLETE = 400;
		/**
		 * An asynchronous clipboard file read completed.
		 */
		NK_EVENT_CLIPBOARD_FILES_COMPLETE = 401;
		/**
		 * An asynchronous clipboard resource read completed.
		 */
		NK_EVENT_CLIPBOARD_RESOURCES_COMPLETE = 402;
		/**
		 * A notification was delivered by the platform.
		 */
		NK_EVENT_NOTIFICATION_DELIVERED = 500;
		/**
		 * A user activated a notification.
		 */
		NK_EVENT_NOTIFICATION_ACTIVATED = 501;
		/**
		 * A notification was dismissed.
		 */
		NK_EVENT_NOTIFICATION_DISMISSED = 502;
		/**
		 * A notification could not be shown or completed.
		 */
		NK_EVENT_NOTIFICATION_FAILED = 503;
		/**
		 * The attached mobile host geometry or insets changed.
		 */
		NK_EVENT_MOBILE_HOST_GEOMETRY_CHANGED = 600;
		/**
		 * A graphics surface became ready for rendering.
		 */
		NK_EVENT_SURFACE_READY = 700;
		/**
		 * A graphics surface framebuffer changed size.
		 */
		NK_EVENT_SURFACE_RESIZE = 701;
		/**
		 * A graphics surface became unavailable.
		 */
		NK_EVENT_SURFACE_LOST = 702;
		/**
		 * A custom-surface accessibility action was requested.
		 */
		NK_EVENT_ACCESSIBILITY_ACTION = 703;
		/**
		 * A mobile host opened one or more resources.
		 */
		NK_EVENT_RESOURCE_OPENED = 800;
		/**
		 * A mobile host received a share operation.
		 */
		NK_EVENT_SHARE_RECEIVED = 801;
		/**
		 * A mobile host received dropped resources or text.
		 */
		NK_EVENT_RESOURCE_DROP = 802;
		/**
		 * An asynchronous URI resource load completed with raw byte data.
		 */
		NK_EVENT_RESOURCE_DATA_COMPLETE = 803;
	}
	/**
	 * Canonical axis index in nk_gamepad_state and gamepad events.
	 */
	enum nk_gamepad_axis : u32 {
		/**
		 * Left stick horizontal axis.
		 */
		NK_GAMEPAD_AXIS_LEFT_X = 0;
		/**
		 * Left stick vertical axis.
		 */
		NK_GAMEPAD_AXIS_LEFT_Y = 1;
		/**
		 * Right stick horizontal axis.
		 */
		NK_GAMEPAD_AXIS_RIGHT_X = 2;
		/**
		 * Right stick vertical axis.
		 */
		NK_GAMEPAD_AXIS_RIGHT_Y = 3;
		/**
		 * Left trigger axis.
		 */
		NK_GAMEPAD_AXIS_LEFT_TRIGGER = 4;
		/**
		 * Right trigger axis.
		 */
		NK_GAMEPAD_AXIS_RIGHT_TRIGGER = 5;
		/**
		 * Number of canonical axes.
		 */
		NK_GAMEPAD_AXIS_COUNT = 6;
	}
	/**
	 * Canonical button index in nk_gamepad_state and gamepad events.
	 */
	enum nk_gamepad_button : u32 {
		/**
		 * South face button.
		 */
		NK_GAMEPAD_BUTTON_A = 0;
		/**
		 * East face button.
		 */
		NK_GAMEPAD_BUTTON_B = 1;
		/**
		 * West face button.
		 */
		NK_GAMEPAD_BUTTON_X = 2;
		/**
		 * North face button.
		 */
		NK_GAMEPAD_BUTTON_Y = 3;
		/**
		 * Left shoulder button.
		 */
		NK_GAMEPAD_BUTTON_LEFT_BUMPER = 4;
		/**
		 * Right shoulder button.
		 */
		NK_GAMEPAD_BUTTON_RIGHT_BUMPER = 5;
		/**
		 * Back, select, or view button.
		 */
		NK_GAMEPAD_BUTTON_BACK = 6;
		/**
		 * Start, menu, or pause button.
		 */
		NK_GAMEPAD_BUTTON_START = 7;
		/**
		 * Guide, home, or platform button.
		 */
		NK_GAMEPAD_BUTTON_GUIDE = 8;
		/**
		 * Left stick click.
		 */
		NK_GAMEPAD_BUTTON_LEFT_THUMB = 9;
		/**
		 * Right stick click.
		 */
		NK_GAMEPAD_BUTTON_RIGHT_THUMB = 10;
		/**
		 * Up on the D-pad.
		 */
		NK_GAMEPAD_BUTTON_DPAD_UP = 11;
		/**
		 * Right on the D-pad.
		 */
		NK_GAMEPAD_BUTTON_DPAD_RIGHT = 12;
		/**
		 * Down on the D-pad.
		 */
		NK_GAMEPAD_BUTTON_DPAD_DOWN = 13;
		/**
		 * Left on the D-pad.
		 */
		NK_GAMEPAD_BUTTON_DPAD_LEFT = 14;
		/**
		 * Number of canonical buttons.
		 */
		NK_GAMEPAD_BUTTON_COUNT = 15;
	}
	/**
	 * Options controlling canonical gamepad normalization.
	 */
	flags nk_gamepad_flags : u32 {
		/**
		 * Report trigger axes in [0, 1] instead of the default [-1, 1].
		 */
		NK_GAMEPAD_TRIGGER_ZERO_TO_ONE = 1;
	}
	/**
	 * Origin of the mapping currently selected for a joystick.
	 */
	enum nk_gamepad_mapping_source : u32 {
		/**
		 * Mapping came from NativeKit's built-in database.
		 */
		NK_GAMEPAD_MAPPING_BUILT_IN = 1;
		/**
		 * Mapping was supplied by the application.
		 */
		NK_GAMEPAD_MAPPING_APPLICATION = 2;
	}
	/**
	 * Graphics API requested when creating a surface.
	 */
	enum nk_graphics_api : u32 {
		/**
		 * Desktop or compatible OpenGL.
		 */
		NK_GRAPHICS_OPENGL = 1;
		/**
		 * OpenGL ES, typically used on mobile or embedded systems.
		 */
		NK_GRAPHICS_OPENGL_ES = 2;
		/**
		 * Vulkan presentation surface.
		 */
		NK_GRAPHICS_VULKAN = 3;
	}
	/**
	 * Transition reported for a key or pointer button.
	 */
	enum nk_input_action : u32 {
		/**
		 * The key or button was released.
		 */
		NK_INPUT_RELEASE = 0;
		/**
		 * The key or button was pressed.
		 */
		NK_INPUT_PRESS = 1;
		/**
		 * The key is being held and generated a repeat event.
		 */
		NK_INPUT_REPEAT = 2;
	}
	/**
	 * Button flags accepted by a message dialog.
	 */
	flags nk_message_buttons : u32 {
		/**
		 * Show an OK button.
		 */
		NK_MESSAGE_BUTTON_OK = 1;
		/**
		 * Show a Cancel button.
		 */
		NK_MESSAGE_BUTTON_CANCEL = 2;
		/**
		 * Show a Yes button.
		 */
		NK_MESSAGE_BUTTON_YES = 4;
		/**
		 * Show a No button.
		 */
		NK_MESSAGE_BUTTON_NO = 8;
	}
	/**
	 * Visual and semantic kind of a message dialog.
	 */
	enum nk_message_kind : u32 {
		/**
		 * Informational message.
		 */
		NK_MESSAGE_INFO = 0;
		/**
		 * Warning message.
		 */
		NK_MESSAGE_WARNING = 1;
		/**
		 * Error message.
		 */
		NK_MESSAGE_ERROR = 2;
		/**
		 * Question that expects a choice.
		 */
		NK_MESSAGE_QUESTION = 3;
	}
	/**
	 * Button value returned in a message-dialog completion payload.
	 */
	enum nk_message_result : u32 {
		/**
		 * No button was selected, normally because the dialog was cancelled.
		 */
		NK_MESSAGE_RESULT_NONE = 0;
		/**
		 * The OK button was selected.
		 */
		NK_MESSAGE_RESULT_OK = 1;
		/**
		 * The Cancel button was selected.
		 */
		NK_MESSAGE_RESULT_CANCEL = 2;
		/**
		 * The Yes button was selected.
		 */
		NK_MESSAGE_RESULT_YES = 3;
		/**
		 * The No button was selected.
		 */
		NK_MESSAGE_RESULT_NO = 4;
	}
	/**
	 * Modifier keys active when an input event was generated.
	 */
	flags nk_modifiers : u32 {
		/**
		 * The Shift modifier is active.
		 */
		NK_MOD_SHIFT = 1;
		/**
		 * The Control modifier is active.
		 */
		NK_MOD_CONTROL = 2;
		/**
		 * The Alt modifier is active.
		 */
		NK_MOD_ALT = 4;
		/**
		 * The platform's Super, Command, or Windows modifier is active.
		 */
		NK_MOD_SUPER = 8;
		/**
		 * Caps Lock is active.
		 */
		NK_MOD_CAPS_LOCK = 16;
		/**
		 * Num Lock is active.
		 */
		NK_MOD_NUM_LOCK = 32;
	}
	/**
	 * Discriminator identifying the platform representation in nk_native_window.
	 */
	enum nk_native_window_kind : u32 {
		/**
		 * No platform representation is available.
		 */
		NK_NATIVE_WINDOW_UNKNOWN = 0;
		/**
		 * The native window value is a Win32 HWND.
		 */
		NK_NATIVE_WINDOW_WIN32 = 1;
		/**
		 * The native window and optional view values are Cocoa objects.
		 */
		NK_NATIVE_WINDOW_COCOA = 2;
		/**
		 * The display and window values identify an X11 display and window.
		 */
		NK_NATIVE_WINDOW_X11 = 3;
		/**
		 * The display and window values identify a Wayland display and surface.
		 */
		NK_NATIVE_WINDOW_WAYLAND = 4;
	}
	flags nk_notification_flags : u32 {
		/**
		 * Do not show notification sound or vibration when supported by the backend.
		 */
		NK_NOTIFICATION_SILENT = 1;
	}
	/**
	 * Mouse or pointer button identifier.
	 */
	enum nk_pointer_button : u32 {
		/**
		 * The primary or left pointer button.
		 */
		NK_POINTER_BUTTON_LEFT = 0;
		/**
		 * The secondary or right pointer button.
		 */
		NK_POINTER_BUTTON_RIGHT = 1;
		/**
		 * The middle pointer button.
		 */
		NK_POINTER_BUTTON_MIDDLE = 2;
		/**
		 * The fourth pointer button.
		 */
		NK_POINTER_BUTTON_4 = 3;
		/**
		 * The fifth pointer button.
		 */
		NK_POINTER_BUTTON_5 = 4;
		/**
		 * Highest pointer-button value reserved for state queries.
		 */
		NK_POINTER_BUTTON_LAST = 7;
	}
	/**
	 * Access and persistence flags attached to a URI resource.
	 */
	flags nk_resource_flags : u32 {
		/**
		 * The resource may be opened for reading.
		 */
		NK_RESOURCE_READABLE = 1;
		/**
		 * The resource may be opened for writing.
		 */
		NK_RESOURCE_WRITABLE = 2;
		/**
		 * The access grant is retained beyond the originating operation.
		 */
		NK_RESOURCE_PERSISTED = 4;
	}
	/**
	 * Access and creation flags accepted by nk_resource_open().
	 */
	flags nk_resource_open_flags : u32 {
		/**
		 * Open the stream for reading.
		 */
		NK_RESOURCE_OPEN_READ = 1;
		/**
		 * Open the stream for writing.
		 */
		NK_RESOURCE_OPEN_WRITE = 2;
		/**
		 * Create the resource when it does not already exist.
		 */
		NK_RESOURCE_OPEN_CREATE = 4;
		/**
		 * Truncate an existing resource before writing.
		 */
		NK_RESOURCE_OPEN_TRUNCATE = 8;
	}
	/**
	 * Capabilities reported for an opened resource stream.
	 */
	flags nk_resource_stream_flags : u32 {
		/**
		 * The stream accepts reads.
		 */
		NK_RESOURCE_STREAM_READABLE = 1;
		/**
		 * The stream accepts writes.
		 */
		NK_RESOURCE_STREAM_WRITABLE = 2;
		/**
		 * The stream supports position changes.
		 */
		NK_RESOURCE_STREAM_SEEKABLE = 4;
		/**
		 * The stream size field contains a known byte count.
		 */
		NK_RESOURCE_STREAM_SIZE_KNOWN = 8;
	}
	/**
	 * Signed result returned by NativeKit operations.
	 */
	enum nk_result : i32 {
		/**
		 * Operation completed successfully.
		 */
		NK_OK = 0;
		/**
		 * The backend encountered an unspecified failure.
		 */
		NK_ERROR_UNKNOWN = -1;
		/**
		 * A caller-provided pointer, value, size, or combination is invalid.
		 */
		NK_ERROR_INVALID_ARGUMENT = -2;
		/**
		 * A handle is zero, stale, or belongs to a different resource kind.
		 */
		NK_ERROR_INVALID_HANDLE = -3;
		/**
		 * The requested feature or operation is unavailable on this backend.
		 */
		NK_ERROR_UNSUPPORTED = -4;
		/**
		 * NativeKit has not been initialized on the calling thread.
		 */
		NK_ERROR_NOT_INITIALIZED = -5;
		/**
		 * NativeKit is already initialized by another active runtime.
		 */
		NK_ERROR_ALREADY_INITIALIZED = -6;
		/**
		 * An operation that requires the UI thread was called elsewhere.
		 */
		NK_ERROR_WRONG_THREAD = -7;
		/**
		 * The operation could not allocate the required memory.
		 */
		NK_ERROR_OUT_OF_MEMORY = -8;
		/**
		 * A non-terminal event could not be added because the queue is full.
		 */
		NK_ERROR_QUEUE_FULL = -9;
		/**
		 * A valid request can no longer complete in its current lifecycle state.
		 */
		NK_ERROR_INVALID_REQUEST = -10;
		/**
		 * A caller-provided output buffer is too small for the requested result.
		 */
		NK_ERROR_BUFFER_TOO_SMALL = -11;
	}
	/**
	 * Reference point used by nk_resource_seek().
	 */
	enum nk_seek_origin : u32 {
		/**
		 * Offset from the beginning of the stream.
		 */
		NK_SEEK_START = 0;
		/**
		 * Offset from the current stream position.
		 */
		NK_SEEK_CURRENT = 1;
		/**
		 * Offset from the end of the stream.
		 */
		NK_SEEK_END = 2;
	}
	/**
	 * Optional surface creation and presentation flags.
	 */
	flags nk_surface_flags : u32 {
		/**
		 * Create the surface hidden until explicitly shown.
		 */
		NK_SURFACE_HIDDEN = 1;
		/**
		 * Request an alpha-capable surface where the backend supports it.
		 */
		NK_SURFACE_ALPHA = 2;
		/**
		 * Request a depth buffer.
		 */
		NK_SURFACE_DEPTH = 4;
		/**
		 * Request a stencil buffer.
		 */
		NK_SURFACE_STENCIL = 8;
		/**
		 * Request additional graphics validation or debug output.
		 */
		NK_SURFACE_DEBUG_CONTEXT = 16;
		/**
		 * Request a forward-compatible OpenGL context.
		 */
		NK_SURFACE_FORWARD_COMPATIBLE = 32;
	}
	/**
	 * Well-known directories returned by nk_system_directory().
	 */
	enum nk_system_directory_kind : u32 {
		/**
		 * User home directory.
		 */
		NK_DIRECTORY_HOME = 1;
		/**
		 * User desktop directory.
		 */
		NK_DIRECTORY_DESKTOP = 2;
		/**
		 * User documents directory.
		 */
		NK_DIRECTORY_DOCUMENTS = 3;
		/**
		 * User downloads directory.
		 */
		NK_DIRECTORY_DOWNLOADS = 4;
		/**
		 * Application cache directory.
		 */
		NK_DIRECTORY_CACHE = 5;
		/**
		 * Application configuration directory.
		 */
		NK_DIRECTORY_CONFIG = 6;
		/**
		 * Application data directory.
		 */
		NK_DIRECTORY_DATA = 7;
		/**
		 * Temporary-file directory.
		 */
		NK_DIRECTORY_TEMP = 8;
	}
	/**
	 * Operation represented by an NK_EVENT_TEXT_EDIT transaction.
	 */
	enum nk_text_edit_action : u32 {
		/**
		 * Update the active composition without committing it.
		 */
		NK_TEXT_EDIT_COMPOSE = 1;
		/**
		 * Replace the reported range with committed UTF-8 text.
		 */
		NK_TEXT_EDIT_COMMIT = 2;
		/**
		 * Delete the reported range.
		 */
		NK_TEXT_EDIT_DELETE = 3;
		/**
		 * Move or extend the selection to the reported range.
		 */
		NK_TEXT_EDIT_SET_SELECTION = 4;
		/**
		 * Finish the active composition without further replacement text.
		 */
		NK_TEXT_EDIT_FINISH_COMPOSITION = 5;
		/**
		 * Set or update the active composition range.
		 */
		NK_TEXT_EDIT_SET_COMPOSITION = 6;
	}
	/**
	 * Action requested for the keyboard's enter or action key.
	 */
	enum nk_text_input_action : u32 {
		/**
		 * Use the platform or editor default action.
		 */
		NK_TEXT_INPUT_ACTION_DEFAULT = 0;
		/**
		 * Complete the current editing operation.
		 */
		NK_TEXT_INPUT_ACTION_DONE = 1;
		/**
		 * Submit a navigation or URL operation.
		 */
		NK_TEXT_INPUT_ACTION_GO = 2;
		/**
		 * Move focus to the next editor.
		 */
		NK_TEXT_INPUT_ACTION_NEXT = 3;
		/**
		 * Start a search operation.
		 */
		NK_TEXT_INPUT_ACTION_SEARCH = 4;
		/**
		 * Send or submit the current content.
		 */
		NK_TEXT_INPUT_ACTION_SEND = 5;
		/**
		 * Do not request a specific action key.
		 */
		NK_TEXT_INPUT_ACTION_NONE = 6;
	}
	/**
	 * Optional behavior requested from the platform text input method.
	 */
	flags nk_text_input_flags : u32 {
		/**
		 * The editor accepts multiple lines.
		 */
		NK_TEXT_INPUT_MULTILINE = 1;
		/**
		 * Ask the platform to provide autocorrection.
		 */
		NK_TEXT_INPUT_AUTOCORRECT = 2;
		/**
		 * Ask the platform to capitalize sentence starts.
		 */
		NK_TEXT_INPUT_CAPITALIZE_SENTENCES = 4;
	}
	/**
	 * Keyboard purpose hint for a custom text editor.
	 */
	enum nk_text_input_type : u32 {
		/**
		 * General text input.
		 */
		NK_TEXT_INPUT_TEXT = 0;
		/**
		 * Email address input.
		 */
		NK_TEXT_INPUT_EMAIL = 1;
		/**
		 * URL input.
		 */
		NK_TEXT_INPUT_URL = 2;
		/**
		 * Numeric input.
		 */
		NK_TEXT_INPUT_NUMBER = 3;
		/**
		 * Telephone number input.
		 */
		NK_TEXT_INPUT_PHONE = 4;
		/**
		 * Password input; the platform may obscure entered text.
		 */
		NK_TEXT_INPUT_PASSWORD = 5;
	}
	/**
	 * Touch contact transition.
	 */
	enum nk_touch_action : u32 {
		/**
		 * A new touch contact began.
		 */
		NK_TOUCH_BEGIN = 1;
		/**
		 * An existing touch contact moved.
		 */
		NK_TOUCH_MOVE = 2;
		/**
		 * An existing touch contact ended normally.
		 */
		NK_TOUCH_END = 3;
		/**
		 * An existing touch contact was cancelled.
		 */
		NK_TOUCH_CANCEL = 4;
	}
	/**
	 * Tool used for a touch contact.
	 */
	enum nk_touch_tool : u32 {
		/**
		 * A finger or ordinary touch contact.
		 */
		NK_TOUCH_TOOL_FINGER = 1;
		/**
		 * A stylus or pen contact.
		 */
		NK_TOUCH_TOOL_STYLUS = 2;
		/**
		 * An eraser end of a stylus.
		 */
		NK_TOUCH_TOOL_ERASER = 3;
	}
	flags nk_webview_flags : u32 {
		/**
		 * Enable the platform's WebView developer tools or inspector when supported.
		 */
		NK_WEBVIEW_DEVTOOLS = 1;
		/**
		 * Create the WebView hidden until nk_webview_show() enables it.
		 */
		NK_WEBVIEW_HIDDEN = 2;
		/**
		 * Pause navigations for decisions delivered through NK_EVENT_WEBVIEW_NAVIGATION_REQUEST.
		 */
		NK_WEBVIEW_NAVIGATION_POLICY = 4;
	}
	flags nk_window_flags : u32 {
		/**
		 * The window can be resized by the user.
		 */
		NK_WINDOW_RESIZABLE = 1;
		/**
		 * The window is initially hidden.
		 */
		NK_WINDOW_HIDDEN = 2;
		/**
		 * The window is created without standard decorations.
		 */
		NK_WINDOW_BORDERLESS = 4;
		/**
		 * The window is modal with respect to its owner.
		 */
		NK_WINDOW_MODAL = 8;
	}
	/**
	 * Category of a NativeKit-owned top-level window.
	 */
	enum nk_window_kind : u32 {
		/**
		 * A regular application window.
		 */
		NK_WINDOW_NORMAL = 0;
		/**
		 * A utility or tool window.
		 */
		NK_WINDOW_UTILITY = 1;
	}
	flags nk_window_state_flags : u32 {
		/**
		 * The window is visible.
		 */
		NK_WINDOW_STATE_VISIBLE = 1;
		/**
		 * The window is active or focused.
		 */
		NK_WINDOW_STATE_ACTIVE = 2;
		/**
		 * The window is minimized.
		 */
		NK_WINDOW_STATE_MINIMIZED = 4;
		/**
		 * The window is maximized.
		 */
		NK_WINDOW_STATE_MAXIMIZED = 8;
		/**
		 * The window is fullscreen.
		 */
		NK_WINDOW_STATE_FULLSCREEN = 16;
		/**
		 * Attention was requested and focus has not yet returned to the window.
		 */
		NK_WINDOW_STATE_ATTENTION_REQUESTED = 32;
	}
	/**
	 * Reads the optional UTF-8 value from an accessibility action event.
	 *
	 * The event must be NK_EVENT_ACCESSIBILITY_ACTION. On NK_OK, `out_value`
	 * points into the event data and remains valid until nk_event_release(); do
	 * not free or retain it. `out_length` receives the UTF-8 byte length excluding
	 * the NUL terminator. For actions without a value, the pointer is NULL and
	 * the length is zero.
	 */
	extern fn nk_accessibility_action_event_value(event: ptr<const<nk_event>>, out_value: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Returns the ABI version implemented by the loaded NativeKit library.
	 */
	extern fn nk_api_version() -> u32;
	/**
	 * Returned UTF-8 views remain owned by the event until nk_event_release().
	 */
	extern fn nk_clipboard_event_file(event: ptr<const<nk_event>>, index: u32, out_path: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Starts an asynchronous read of local clipboard files.
	 */
	extern fn nk_clipboard_read_files(out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous read of URI resources from the system clipboard.
	 */
	extern fn nk_clipboard_read_resources(out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous clipboard read. Completion is delivered through the
	 * corresponding NK_EVENT_CLIPBOARD_* event with the returned request ID.
	 * Missing clipboard content completes successfully with empty event data.
	 */
	extern fn nk_clipboard_read_text(out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Copies local paths into the clipboard as a URI list. UI thread only.
	 */
	extern fn nk_clipboard_set_files(paths: ptr<utf8> @in_array("path_count"), path_count: u32) -> nk_result;
	/**
	 * Copies URI resources into the system clipboard.
	 */
	extern fn nk_clipboard_set_resources(resources: ptr<const<nk_resource>> @in_array("resource_count"), resource_count: u32) -> nk_result;
	/**
	 * Copies UTF-8 text into the system clipboard. UI thread only.
	 */
	extern fn nk_clipboard_set_text(text: utf8) -> nk_result;
	/**
	 * Creates a cursor from copied RGBA8 pixels, with the first row at the top.
	 */
	extern fn nk_cursor_create_custom(image: ptr<const<nk_cursor_image>>, out_cursor: ptr<nk_handle>) -> nk_result;
	/**
	 * Creates a standard platform cursor and returns its NativeKit handle.
	 */
	extern fn nk_cursor_create_standard(shape: nk_cursor_shape, out_cursor: ptr<nk_handle>) -> nk_result;
	/**
	 * Destroys a cursor handle; a cursor already selected by a window remains usable.
	 */
	extern fn nk_cursor_destroy(cursor: nk_handle) -> nk_result;
	/**
	 * Cancels a pending dialog. Its completion event is still emitted.
	 */
	extern fn nk_dialog_cancel(request: nk_request_id) -> nk_result;
	/**
	 * Validates and returns a path from a dialog event. The returned UTF-8 view is
	 * owned by the event and remains valid until nk_event_release().
	 */
	extern fn nk_dialog_event_path(event: ptr<const<nk_event>>, index: u32, out_path: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Starts an asynchronous native message dialog.
	 */
	extern fn nk_dialog_message(parent: nk_handle, options: ptr<const<nk_message_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts a non-blocking native dialog on the UI thread. `parent` may be zero;
	 * otherwise it must be a live window. All strings and filters are copied before
	 * the function returns. Completion uses NK_EVENT_DIALOG_PATHS_COMPLETE or
	 * NK_EVENT_DIALOG_MESSAGE_COMPLETE, with the operation in event.flags and the
	 * returned request ID in event.request_id.
	 */
	extern fn nk_dialog_open_file(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Resource dialogs return NK_EVENT_DIALOG_RESOURCES_COMPLETE with nk_resource_list data.
	 */
	extern fn nk_dialog_open_resource(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native save-file dialog.
	 */
	extern fn nk_dialog_save_file(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native URI-resource save dialog.
	 */
	extern fn nk_dialog_save_resource(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native directory-selection dialog.
	 */
	extern fn nk_dialog_select_directory(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native URI-resource directory dialog.
	 */
	extern fn nk_dialog_select_resource_directory(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Returns one borrowed UTF-8 item from a drop event by zero-based index.
	 */
	extern fn nk_drop_event_item(event: ptr<const<nk_event>>, index: u32, out_item: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Releases payload storage returned by nk_poll_event and clears the event.
	 * Safe to call with NULL or an empty event; struct_size is preserved for reuse.
	 */
	extern fn nk_event_release(event: ptr<nk_event>) -> void;
	/**
	 * Adds or replaces one SDL/GLFW controller mapping for its 32-character GUID.
	 */
	extern fn nk_gamepad_add_mapping(mapping: utf8) -> nk_result;
	/**
	 * Adds newline-separated mappings, ignoring blank lines, comments, and entries
	 * for other platforms. The update is atomic if any applicable line is invalid.
	 */
	extern fn nk_gamepad_add_mappings(database: utf8, out_added: ptr<u32>) -> nk_result;
	/**
	 * Returns the pinned SDL_GameControllerDB Git revision used for built-ins.
	 */
	extern fn nk_gamepad_get_builtin_database_revision(buffer: ptr<c_char>, inout_size: ptr<u32>) -> nk_result;
	/**
	 * Reports whether the active mapping is built-in or application-supplied.
	 */
	extern fn nk_gamepad_get_mapping_source(joystick: nk_handle, out_source: ptr<nk_gamepad_mapping_source>) -> nk_result;
	/**
	 * Copies the mapped gamepad name into a caller-owned UTF-8 buffer.
	 */
	extern fn nk_gamepad_get_name(joystick: nk_handle, buffer: ptr<c_char>, inout_size: ptr<u32>) -> nk_result;
	/**
	 * Returns the process-wide gamepad normalization options.
	 */
	extern fn nk_gamepad_get_options(out_options: ptr<nk_gamepad_options>) -> nk_result;
	/**
	 * Returns the current normalized state of a mapped joystick.
	 */
	extern fn nk_gamepad_get_state(joystick: nk_handle, out_state: ptr<nk_gamepad_state>) -> nk_result;
	/**
	 * Reports whether a joystick has an active canonical gamepad mapping.
	 */
	extern fn nk_gamepad_is_mapped(joystick: nk_handle, out_mapped: ptr<nk_bool>) -> nk_result;
	/**
	 * Sets process-wide normalization used by state queries and gamepad events.
	 */
	extern fn nk_gamepad_set_options(options: ptr<const<nk_gamepad_options>>) -> nk_result;
	/**
	 * Returns process-wide capabilities of the compiled platform backend.
	 */
	extern fn nk_get_capabilities() -> nk_capabilities;
	/**
	 * Initializes NativeKit on the calling thread. That thread becomes the UI
	 * thread until nk_shutdown(). Exactly one initialization may be active.
	 *
	 * @param options Non-null, zero-initialized options with struct_size and
	 * api_version set.
	 * @return NK_OK, or an error describing invalid options, an unsupported
	 * version, an existing runtime, or allocation failure.
	 */
	extern fn nk_init(options: ptr<const<nk_init_options>>) -> nk_result;
	/**
	 * Each state function accepts NULL to query its element count. Axis values are
	 * normalized to [-1, 1], buttons are 0 or 1, and hats use NK_JOYSTICK_HAT_*.
	 */
	extern fn nk_joystick_get_axes(joystick: nk_handle, axes: ptr<f32>, inout_count: ptr<u32>) -> nk_result;
	/**
	 * Copies the current button states as zero or one bytes.
	 */
	extern fn nk_joystick_get_buttons(joystick: nk_handle, buttons: ptr<u8>, inout_count: ptr<u32>) -> nk_result;
	/**
	 * Returns a transport warning, such as a missing /dev/input directory,
	 * insufficient device permissions, or unavailable inotify monitoring. An
	 * empty string means that no transport problem has been observed.
	 */
	extern fn nk_joystick_get_diagnostics(buffer: ptr<c_char>, inout_size: ptr<u32>) -> nk_result;
	/**
	 * Returns a 32-character SDL-compatible device GUID plus a trailing NUL.
	 */
	extern fn nk_joystick_get_guid(joystick: nk_handle, buffer: ptr<c_char>, inout_size: ptr<u32>) -> nk_result;
	/**
	 * Copies the current hat direction flags.
	 */
	extern fn nk_joystick_get_hats(joystick: nk_handle, hats: ptr<u8>, inout_count: ptr<u32>) -> nk_result;
	/**
	 * Copies the device name into a caller-owned UTF-8 buffer.
	 */
	extern fn nk_joystick_get_name(joystick: nk_handle, buffer: ptr<c_char>, inout_size: ptr<u32>) -> nk_result;
	/**
	 * Writes connected joystick handles. Pass NULL to query the required count.
	 * Hotplug is reported with NK_EVENT_JOYSTICK_CONNECTED and
	 * NK_EVENT_JOYSTICK_DISCONNECTED.
	 */
	extern fn nk_joystick_list(joysticks: ptr<nk_handle>, inout_count: ptr<u32>) -> nk_result;
	/**
	 * On NK_OK, returns the current action state for a key. Call on the UI thread.
	 */
	extern fn nk_key_get_state(window: nk_handle, key: nk_key, out_action: ptr<nk_input_action>) -> nk_result;
	/**
	 * Returns a thread-local UTF-8 diagnostic for the most recent failing call.
	 * The pointer remains valid until the next NativeKit call on this thread.
	 * The returned string may be empty after a successful call.
	 */
	extern fn nk_last_error() -> utf8 @borrowed;
	/**
	 * Returns the mode currently selected by the monitor compositor.
	 */
	extern fn nk_monitor_get_current_mode(monitor: nk_handle, out_mode: ptr<nk_video_mode> @out) -> nk_result;
	/**
	 * Returns the monitor geometry in logical desktop coordinates.
	 */
	extern fn nk_monitor_get_geometry(monitor: nk_handle, out_geometry: ptr<nk_monitor_geometry> @out) -> nk_result;
	/**
	 * GTK 3 exposes only the current compositor mode, so this currently returns a
	 * one-element list.
	 */
	extern fn nk_monitor_get_modes(monitor: nk_handle, modes: ptr<nk_video_mode>, inout_count: ptr<u32>) -> nk_result;
	/**
	 * Copies the monitor name into a caller-owned UTF-8 buffer.
	 */
	extern fn nk_monitor_get_name(monitor: nk_handle, buffer: nullable<ptr<c_char>> @out_buffer("inout_size"), inout_size: ptr<u32> @inout) -> nk_result;
	/**
	 * Returns the primary monitor handle.
	 */
	extern fn nk_monitor_get_primary(out_monitor: ptr<nk_handle> @out) -> nk_result;
	/**
	 * Writes connected monitor handles in GDK display order. Pass NULL to query
	 * the required count. A missing or undersized array returns
	 * NK_ERROR_BUFFER_TOO_SMALL and updates inout_count.
	 */
	extern fn nk_monitor_list(monitors: ptr<nk_handle>, inout_count: ptr<u32>) -> nk_result;
	/**
	 * Removes a delivered notification. A successful close emits DISMISSED.
	 */
	extern fn nk_notification_close(request: nk_request_id) -> nk_result;
	/**
	 * Requests a desktop notification. Strings are copied before return. Success
	 * means the request was accepted for asynchronous processing; delivery or
	 * failure is reported with the same request ID through the event queue.
	 * `icon` is an optional platform-resolved icon name or absolute file path.
	 */
	extern fn nk_notification_show(options: ptr<const<nk_notification_options>>, out_request: ptr<nk_request_id>) -> nk_result;
	/**
	 * On NK_OK, returns the current action state of one pointer button.
	 */
	extern fn nk_pointer_button_get_state(window: nk_handle, button: nk_pointer_button, out_action: ptr<nk_input_action>) -> nk_result;
	/**
	 * On NK_OK, returns the current pointer position in surface-local logical pixels.
	 */
	extern fn nk_pointer_get_position(window: nk_handle, out_x: ptr<f64>, out_y: ptr<f64>) -> nk_result;
	/**
	 * Polls one event on the UI thread. Returns NK_OK with NK_EVENT_NONE when the
	 * queue is empty. `event` must be zero-initialized with struct_size set.
	 * A successfully polled event, including NK_EVENT_NONE, must be released with
	 * nk_event_release() before the structure is reused.
	 *
	 * @param event Output event structure. Its data pointer must be NULL before
	 * the call.
	 * @return NK_OK for an event or an empty queue, or an error if event is invalid
	 * or the call is made from the wrong lifecycle/thread state.
	 */
	extern fn nk_poll_event(event: ptr<nk_event> @inout) -> nk_result;
	/**
	 * Returns non-zero when the active backend provides raw relative pointer motion.
	 */
	extern fn nk_raw_pointer_motion_supported() -> nk_bool;
	/**
	 * Closes a resource stream and invalidates its handle.
	 */
	extern fn nk_resource_close(stream: nk_handle) -> nk_result;
	/**
	 * Returns optional UTF-8 text from a resource-drop event.
	 */
	extern fn nk_resource_drop_event_text(event: ptr<const<nk_event>>, out_text: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Returned views remain owned by the event until nk_event_release().
	 */
	extern fn nk_resource_event_item(event: ptr<const<nk_event>>, index: u32, out_resource: ptr<nk_resource_view>) -> nk_result;
	/**
	 * Queries the readable/writable access retained for a URI resource.
	 */
	extern fn nk_resource_get_persisted_access(resource: ptr<const<nk_resource>>, out_flags: ptr<nk_resource_flags> @out) -> nk_result;
	/**
	 * Starts an asynchronous read of the complete URI resource. Completion is
	 * delivered through NK_EVENT_RESOURCE_DATA_COMPLETE; its event data contains
	 * the loaded bytes and its result reports the fetch outcome.
	 */
	extern fn nk_resource_load_async(resource: ptr<const<nk_resource>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Opens a URI-backed stream on the UI thread. Stream operations copy bytes
	 * synchronously and may be called from worker threads. A successful read may
	 * return fewer bytes than requested; zero bytes means end of stream.
	 */
	extern fn nk_resource_open(resource: ptr<const<nk_resource>>, flags: nk_resource_open_flags, out_stream: ptr<nk_handle> @out) -> nk_result;
	/**
	 * Reads up to `size` bytes; a successful read may return fewer bytes.
	 */
	extern fn nk_resource_read(stream: nk_handle, buffer: ptr<void>, size: u64, out_read: ptr<u64>) -> nk_result;
	/**
	 * Moves the stream position and returns the resulting absolute byte offset.
	 */
	extern fn nk_resource_seek(stream: nk_handle, offset: i64, origin: nk_seek_origin, out_position: ptr<u64>) -> nk_result;
	/**
	 * Controls durable URI access on platforms that support it. access_flags is a
	 * combination of NK_RESOURCE_READABLE and NK_RESOURCE_WRITABLE; zero releases
	 * all persisted access. out_flags receives the actual resource flags retained.
	 * These functions are UI-thread-only.
	 */
	extern fn nk_resource_set_persisted_access(resource: ptr<const<nk_resource>>, access_flags: nk_resource_flags, out_flags: ptr<nk_resource_flags> @out) -> nk_result;
	/**
	 * Returns capabilities and size information for a resource stream.
	 */
	extern fn nk_resource_stream_info_get(stream: nk_handle, out_info: ptr<nk_resource_stream_info>) -> nk_result;
	/**
	 * Writes up to `size` bytes; a successful write may transfer fewer bytes.
	 */
	extern fn nk_resource_write(stream: nk_handle, buffer: ptr<const<void>>, size: u64, out_written: ptr<u64>) -> nk_result;
	/**
	 * Launches the platform share UI with optional text and URI resources.
	 */
	extern fn nk_share(options: ptr<const<nk_share_options>>) -> nk_result;
	/**
	 * Returns optional UTF-8 subject text from a received-share event.
	 */
	extern fn nk_share_event_subject(event: ptr<const<nk_event>>, out_subject: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Returns optional UTF-8 text from a received-share event.
	 */
	extern fn nk_share_event_text(event: ptr<const<nk_event>>, out_text: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Opens a local file with the platform's default application.
	 */
	extern fn nk_shell_open_file(path: utf8) -> nk_result;
	/**
	 * URI inputs are copied before return. These functions are UI-thread-only.
	 */
	extern fn nk_shell_open_resource(resource: ptr<const<nk_resource>>) -> nk_result;
	/**
	 * Requests that the desktop open a URI, open a local file with its default
	 * application, or reveal a local file in its containing folder. Input is
	 * copied before return. These functions must be called on the UI thread.
	 */
	extern fn nk_shell_open_url(url: utf8) -> nk_result;
	/**
	 * Reveals a local file in its containing folder.
	 */
	extern fn nk_shell_reveal_file(path: utf8) -> nk_result;
	/**
	 * Destroys all remaining resources and ends the active runtime generation.
	 * Call on the UI thread after releasing any event currently held by the caller.
	 */
	extern fn nk_shutdown() -> void;
	/**
	 * Removes all virtual nodes from a surface's accessibility tree.
	 */
	extern fn nk_surface_accessibility_clear(surface: nk_handle) -> nk_result;
	/**
	 * Removes a virtual node and all of its descendants.
	 *
	 * `node` must identify an existing non-root node. The operation runs on the UI
	 * thread and requires NK_CAP_ACCESSIBILITY.
	 */
	extern fn nk_surface_accessibility_remove_node(surface: nk_handle, node: nk_accessibility_node_id) -> nk_result;
	/**
	 * Moves accessibility focus to a virtual node.
	 *
	 * Pass NK_ACCESSIBILITY_ROOT to clear virtual focus. Any non-root node must
	 * already exist in the surface's tree.
	 */
	extern fn nk_surface_accessibility_set_focus(surface: nk_handle, node: nk_accessibility_node_id) -> nk_result;
	/**
	 * Inserts or replaces one virtual semantic node.
	 *
	 * `node` is copied during the call. The node ID must be positive, its parent
	 * must already exist (or be NK_ACCESSIBILITY_ROOT), and its text positions
	 * must describe the supplied value window. Requires the UI thread and
	 * NK_CAP_ACCESSIBILITY.
	 */
	extern fn nk_surface_accessibility_set_node(surface: nk_handle, node: ptr<const<nk_accessibility_node>>) -> nk_result;
	/**
	 * Supplies screen-reader geometry for text ranges in a node's value.
	 *
	 * Ranges are borrowed during the call and copied on success. They must be
	 * ordered, non-overlapping, non-empty, and use document code-point offsets.
	 * Pass a zero count to clear the node's current range geometry.
	 */
	extern fn nk_surface_accessibility_set_text_ranges(surface: nk_handle, node: nk_accessibility_node_id, ranges: ptr<const<nk_accessibility_text_range>>, range_count: u32) -> nk_result;
	/**
	 * Applies node replacements, recursive removals, and optional focus atomically.
	 *
	 * The update and its arrays are borrowed during the call. Removals are applied
	 * first, then replacement nodes in array order; a replacement parent must
	 * therefore already exist or precede its children in `nodes`. Set
	 * NK_ACCESSIBILITY_UPDATE_FOCUS to apply `focus`, where ROOT clears focus.
	 * Invalid input leaves the previous tree unchanged.
	 */
	extern fn nk_surface_accessibility_update(surface: nk_handle, update: ptr<const<nk_accessibility_update>>) -> nk_result;
	/**
	 * Creates a graphics surface inside a NativeKit window or mobile host.
	 *
	 * Context configuration is fixed at creation. Desktop backends support
	 * OpenGL and OpenGL ES; Android supports OpenGL ES and Vulkan presentation
	 * views. A share_surface must remain alive until every surface sharing it has
	 * been destroyed. Vulkan views do not support GL context flags or sharing.
	 * On NK_OK, writes the new surface handle to `out_surface`.
	 */
	extern fn nk_surface_create(window: nk_handle, options: ptr<const<nk_surface_options>>, out_surface: ptr<nk_handle> @out) -> nk_result;
	/**
	 * Destroys a graphics surface; its handle becomes invalid.
	 */
	extern fn nk_surface_destroy(surface: nk_handle) -> nk_result;
	/**
	 * Returns the current backend-native render target for a surface frame.
	 */
	extern fn nk_surface_get_frame_target(surface: nk_handle, out_target: ptr<nk_surface_frame_target> @out) -> nk_result;
	/**
	 * Returns the current framebuffer size in device pixels.
	 */
	extern fn nk_surface_get_framebuffer_size(surface: nk_handle, out_width: ptr<i32> @out, out_height: ptr<i32> @out) -> nk_result;
	/**
	 * Resolves a graphics function for the current surface context.
	 *
	 * `name` is a non-null UTF-8 function name. On NK_OK, writes a callable
	 * function pointer to `out_proc`; the pointer remains valid only while the
	 * associated graphics context and loader remain valid.
	 */
	extern fn nk_surface_get_proc_address(surface: nk_handle, name: utf8, out_proc: ptr<nk_graphics_proc>) -> nk_result;
	/**
	 * Makes the surface's graphics context and framebuffer current on the UI thread.
	 */
	extern fn nk_surface_make_current(surface: nk_handle) -> nk_result;
	/**
	 * Presents drawing performed since the last make-current call.
	 *
	 * On GTK this schedules composition of the GtkGLArea framebuffer instead of
	 * swapping a caller-owned native surface.
	 */
	extern fn nk_surface_present(surface: nk_handle) -> nk_result;
	/**
	 * Changes a surface's logical position and size within its parent.
	 *
	 * Width and height must be positive. The backend reports resulting framebuffer
	 * dimensions through NK_EVENT_SURFACE_RESIZE or the frame callback.
	 */
	extern fn nk_surface_set_bounds(surface: nk_handle, x: i32, y: i32, width: i32, height: i32) -> nk_result;
	/**
	 * Installs or removes the surface's frame callback.
	 *
	 * The callback runs on the UI thread while the framebuffer is current. Do not
	 * call nk_surface_present() recursively from it. Pass NULL to detach it; the
	 * `user_data` value is not retained after the callback is removed.
	 */
	extern fn nk_surface_set_frame_callback(surface: nk_handle, callback: nk_surface_frame_callback, user_data: ptr<void>) -> nk_result;
	/**
	 * Shows or hides the software keyboard for a custom graphics surface.
	 */
	extern fn nk_surface_set_text_input_active(surface: nk_handle, active: nk_bool) -> nk_result;
	/**
	 * Synchronizes a custom editor with the platform IME using absolute code-point positions.
	 */
	extern fn nk_surface_set_text_input_state(surface: nk_handle, state: ptr<const<nk_text_input_state>>) -> nk_result;
	/**
	 * Shows or hides a surface without destroying it.
	 */
	extern fn nk_surface_show(surface: nk_handle, visible: nk_bool) -> nk_result;
	/**
	 * Writes a NUL-terminated UTF-8 value. `inout_size` is the buffer capacity on
	 * entry and receives the required size including NUL on every valid call. Pass
	 * NULL as `buffer` to query the size. An absent or small buffer returns
	 * NK_ERROR_BUFFER_TOO_SMALL without truncating the value.
	 */
	extern fn nk_system_directory(kind: nk_system_directory_kind, buffer: ptr<c_char>, inout_size: ptr<u32>) -> nk_result;
	/**
	 * Returns the current desktop appearance. UI thread only.
	 */
	extern fn nk_system_get_appearance(appearance: ptr<nk_system_appearance>) -> nk_result;
	/**
	 * Copies the current system locale identifier into a caller-owned UTF-8 buffer.
	 */
	extern fn nk_system_locale(buffer: ptr<c_char>, inout_size: ptr<u32>) -> nk_result;
	/**
	 * Returns a borrowed UTF-8 view valid until nk_event_release().
	 */
	extern fn nk_text_edit_event_text(event: ptr<const<nk_event>>, out_text: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Reports whether the WebView currently has a back-history entry.
	 */
	extern fn nk_webview_can_go_back(webview: nk_handle, out_can_go_back: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Reports whether the WebView currently has a forward-history entry.
	 */
	extern fn nk_webview_can_go_forward(webview: nk_handle, out_can_go_forward: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Creates a native child WebView inside `parent`. Bounds are logical pixels;
	 * width and height must be positive. `initial_url` is nullable UTF-8. Creation
	 * may finish asynchronously; NK_EVENT_WEBVIEW_READY reports when the native
	 * controller can display content. Operations issued before readiness are
	 * retained in call order by asynchronous backends.
	 */
	extern fn nk_webview_create(parent: nk_handle, options: ptr<const<nk_webview_options>>, out_webview: ptr<nk_handle> @out) -> nk_result;
	/**
	 * Destroys a WebView and cancels its pending evaluations and navigation decisions.
	 */
	extern fn nk_webview_destroy(webview: nk_handle) -> nk_result;
	/**
	 * Starts JavaScript evaluation. Completion is reported as
	 * NK_EVENT_WEBVIEW_EVAL_COMPLETE with the returned request ID. Event data is
	 * UTF-8 JSON for the result, or usually an error message when the event result
	 * is not NK_OK. Cancellation events have empty data. Top-level `undefined`,
	 * functions, and symbols, plus BigInt values and cyclic objects, are not
	 * JSON-serializable and complete with an error. Normal JSON.stringify rules
	 * apply to unsupported values nested in arrays or objects.
	 * Destroying the WebView before completion emits exactly one final event with
	 * NK_ERROR_INVALID_REQUEST, allowing consumers to settle pending futures.
	 */
	extern fn nk_webview_eval(webview: nk_handle, script: utf8, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Submits a request to navigate to the previous history entry.
	 */
	extern fn nk_webview_go_back(webview: nk_handle) -> nk_result;
	/**
	 * Submits a request to navigate to the next history entry.
	 */
	extern fn nk_webview_go_forward(webview: nk_handle) -> nk_result;
	/**
	 * Starts loading a UTF-8 URL in the WebView.
	 */
	extern fn nk_webview_navigate(webview: nk_handle, url: utf8) -> nk_result;
	/**
	 * Resolves an NK_EVENT_WEBVIEW_NAVIGATION_REQUEST. Navigation-policy events
	 * are emitted only for WebViews created with NK_WEBVIEW_NAVIGATION_POLICY.
	 * Event data is the proposed URL. Pending requests are cancelled when their
	 * WebView is destroyed; each request may be resolved exactly once.
	 */
	extern fn nk_webview_navigation_decide(request: nk_request_id, allow: nk_bool) -> nk_result;
	/**
	 * Reloads the current WebView document.
	 */
	extern fn nk_webview_reload(webview: nk_handle) -> nk_result;
	/**
	 * Changes a WebView's logical position and size within its parent.
	 */
	extern fn nk_webview_set_bounds(webview: nk_handle, x: i32, y: i32, width: i32, height: i32) -> nk_result;
	/**
	 * Replaces the document with UTF-8 HTML, using an optional base URL.
	 */
	extern fn nk_webview_set_html(webview: nk_handle, html: utf8, base_url: nullable<utf8>) -> nk_result;
	/**
	 * Shows or hides a WebView without destroying it.
	 */
	extern fn nk_webview_show(webview: nk_handle, visible: nk_bool) -> nk_result;
	/**
	 * Stops the current WebView load.
	 */
	extern fn nk_webview_stop(webview: nk_handle) -> nk_result;
	/**
	 * Requests activation and focus for the window.
	 */
	extern fn nk_window_activate(window: nk_handle) -> nk_result;
	/**
	 * Creates a NativeKit-owned top-level window on the UI thread. Dimensions are
	 * logical pixels and must be positive. `title` is nullable UTF-8. On success,
	 * `out_window` receives a non-zero generation-checked handle. `owner` may name
	 * an existing top-level window; owned windows are destroyed recursively with
	 * their owner. Modal windows require an owner. Window ownership is deliberately
	 * shallow and does not introduce a general-purpose widget hierarchy.
	 */
	extern fn nk_window_create(options: ptr<const<nk_window_options>>, out_window: ptr<nk_handle> @out) -> nk_result;
	/**
	 * Destroys a NativeKit-owned window and recursively destroys its owned windows.
	 * The handle and any child WebView handles become invalid after this call.
	 *
	 * @param window Window handle to destroy.
	 * @return NK_OK, or an error such as NK_ERROR_INVALID_HANDLE.
	 */
	extern fn nk_window_destroy(window: nk_handle) -> nk_result;
	/**
	 * On NK_OK, fills in the current horizontal and vertical content scale factors.
	 */
	extern fn nk_window_get_content_scale(window: nk_handle, out_scale: ptr<nk_window_content_scale> @out) -> nk_result;
	/**
	 * On NK_OK, returns the window's current pointer presentation mode.
	 */
	extern fn nk_window_get_cursor_mode(window: nk_handle, out_mode: ptr<nk_cursor_mode>) -> nk_result;
	/**
	 * On NK_OK, fills in the non-client frame extents in logical screen pixels.
	 */
	extern fn nk_window_get_frame_extents(window: nk_handle, out_extents: ptr<nk_window_frame_extents> @out) -> nk_result;
	/**
	 * On NK_OK, fills in the framebuffer width and height in device pixels.
	 */
	extern fn nk_window_get_framebuffer_size(window: nk_handle, out_width: ptr<i32> @out, out_height: ptr<i32> @out) -> nk_result;
	/**
	 * Sets `*out_hovered` to 1 when the pointer is over the window, and 0 otherwise.
	 */
	extern fn nk_window_get_hovered(window: nk_handle, out_hovered: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Returns a borrowed platform descriptor. Its pointer-sized values are valid
	 * only while the NativeKit window is alive and must never be freed by callers.
	 * This is an explicit interoperability escape hatch, not a portable resource.
	 */
	extern fn nk_window_get_native(window: nk_handle, out_native: ptr<nk_native_window> @out) -> nk_result;
	/**
	 * On NK_OK, fills in the window's logical screen position.
	 */
	extern fn nk_window_get_position(window: nk_handle, out_x: ptr<i32> @out, out_y: ptr<i32> @out) -> nk_result;
	/**
	 * Writes the current logical-to-device-pixel scale. UI thread only.
	 */
	extern fn nk_window_get_scale(window: nk_handle, out_scale: ptr<f32> @out) -> nk_result;
	/**
	 * On NK_OK, fills in the window's logical client width and height.
	 */
	extern fn nk_window_get_size(window: nk_handle, out_width: ptr<i32> @out, out_height: ptr<i32> @out) -> nk_result;
	/**
	 * On NK_OK, fills in the current NK_WINDOW_STATE_* flags.
	 */
	extern fn nk_window_get_state(window: nk_handle, out_state: ptr<nk_window_state> @out) -> nk_result;
	/**
	 * Sets `*out_focused` to 1 if the window has focus, and 0 otherwise.
	 */
	extern fn nk_window_is_focused(window: nk_handle, out_focused: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Sets `*out_visible` to 1 if the window is visible, and 0 otherwise.
	 */
	extern fn nk_window_is_visible(window: nk_handle, out_visible: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Requests that the window manager maximize the window.
	 */
	extern fn nk_window_maximize(window: nk_handle) -> nk_result;
	/**
	 * Requests that the window manager minimize the window.
	 */
	extern fn nk_window_minimize(window: nk_handle) -> nk_result;
	/**
	 * Requests that the window manager draw the user's attention to the window.
	 */
	extern fn nk_window_request_attention(window: nk_handle) -> nk_result;
	/**
	 * Requests that the window manager restore a minimized or maximized window.
	 */
	extern fn nk_window_restore(window: nk_handle) -> nk_result;
	/**
	 * Passing zero for both values disables the aspect-ratio constraint.
	 */
	extern fn nk_window_set_aspect_ratio(window: nk_handle, numerator: i32, denominator: i32) -> nk_result;
	/**
	 * Moves and resizes a top-level window in logical pixels. UI thread only.
	 */
	extern fn nk_window_set_bounds(window: nk_handle, x: i32, y: i32, width: i32, height: i32) -> nk_result;
	/**
	 * Selects a cursor for a window; NK_INVALID_HANDLE restores the platform default.
	 */
	extern fn nk_window_set_cursor(window: nk_handle, cursor: nk_handle) -> nk_result;
	/**
	 * Changes how the window displays or captures the pointer.
	 */
	extern fn nk_window_set_cursor_mode(window: nk_handle, mode: nk_cursor_mode) -> nk_result;
	/**
	 * Enables or disables standard native window decorations.
	 */
	extern fn nk_window_set_decorated(window: nk_handle, enabled: nk_bool) -> nk_result;
	/**
	 * Enables or disables the text and/or local-file drop formats supported by the backend.
	 */
	extern fn nk_window_set_drop_enabled(window: nk_handle, enabled: nk_bool) -> nk_result;
	/**
	 * Enables or disables keeping the window above its peers.
	 */
	extern fn nk_window_set_floating(window: nk_handle, enabled: nk_bool) -> nk_result;
	/**
	 * Requests entering fullscreen when enabled is non-zero, or leaving it otherwise.
	 */
	extern fn nk_window_set_fullscreen(window: nk_handle, enabled: nk_bool) -> nk_result;
	/**
	 * An invalid monitor handle leaves fullscreen and restores windowed placement.
	 */
	extern fn nk_window_set_fullscreen_monitor(window: nk_handle, monitor: nk_handle) -> nk_result;
	/**
	 * Enables or disables passing pointer input through the window.
	 */
	extern fn nk_window_set_mouse_passthrough(window: nk_handle, enabled: nk_bool) -> nk_result;
	/**
	 * Sets the window opacity; supported backends accept values from zero to one.
	 */
	extern fn nk_window_set_opacity(window: nk_handle, opacity: f32) -> nk_result;
	/**
	 * Enables or disables user resizing of the window.
	 */
	extern fn nk_window_set_resizable(window: nk_handle, enabled: nk_bool) -> nk_result;
	/**
	 * Zero disables the corresponding constraint; maxima must not be below minima.
	 */
	extern fn nk_window_set_size_limits(window: nk_handle, limits: ptr<const<nk_window_size_limits>>) -> nk_result;
	/**
	 * Copies the nullable UTF-8 title before returning. UI thread only.
	 */
	extern fn nk_window_set_title(window: nk_handle, title: utf8) -> nk_result;
	/**
	 * Shows when `visible` is non-zero and hides otherwise. UI thread only.
	 */
	extern fn nk_window_show(window: nk_handle, visible: nk_bool) -> nk_result;
	/**
	 * Attaches NativeKit to a caller-owned native window. Destroying the returned
	 * handle only detaches NativeKit. Backends return NK_ERROR_UNSUPPORTED until
	 * they can guarantee correct event and ownership behavior for the given kind.
	 */
	extern fn nk_window_wrap_native(native: ptr<const<nk_native_window>>, out_window: ptr<nk_handle> @out) -> nk_result;
	/**
	 * Payload of NK_EVENT_ACCESSIBILITY_ACTION.
	 *
	 * Selection positions are absolute Unicode code-point offsets in the
	 * document. Use nk_accessibility_action_event_value() to safely obtain the
	 * optional UTF-8 value associated with the action.
	 */
	struct nk_accessibility_action_event @layout(32, 4) {
		/**
		 * ID of the node that received the platform action.
		 */
		node_id: nk_accessibility_node_id @offset(0);
		/**
		 * Requested accessibility action.
		 */
		action: nk_accessibility_action @offset(4);
		/**
		 * Byte offset of the optional NUL-terminated UTF-8 value in event data.
		 */
		value_offset: u32 @offset(8);
		/**
		 * UTF-8 value length in bytes, excluding its NUL terminator.
		 */
		value_length: u32 @offset(12);
		/**
		 * Selection start supplied by the platform, or NONE.
		 */
		selection_start: nk_accessibility_text_position @offset(16);
		/**
		 * Selection end supplied by the platform, or NONE.
		 */
		selection_end: nk_accessibility_text_position @offset(20);
		/**
		 * Requested unit for navigation or selection.
		 */
		granularity: nk_accessibility_text_granularity @offset(24);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(28);
	}
	/**
	 * One virtual node in a surface's accessibility tree.
	 *
	 * Set `struct_size` before passing the node. IDs must be positive and stable
	 * for the lifetime of the semantic item. A node's parent must already exist;
	 * use `child_index` to define its order among siblings. The label and value
	 * strings are nullable UTF-8 and are copied during the update.
	 */
	struct nk_accessibility_node @layout(104, 8) {
		/**
		 * Set to sizeof(nk_accessibility_node) or a larger compatible size.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Stable positive ID chosen by the application.
		 */
		id: nk_accessibility_node_id @offset(4);
		/**
		 * Parent ID, or NK_ACCESSIBILITY_ROOT for a top-level node.
		 */
		parent_id: nk_accessibility_node_id @offset(8);
		/**
		 * Zero-based order among the parent's children.
		 */
		child_index: u32 @offset(12);
		/**
		 * Role announced to the platform accessibility service.
		 */
		role: nk_accessibility_role @offset(16);
		/**
		 * Current NK_ACCESSIBILITY_* state flags.
		 */
		states: nk_accessibility_states @offset(20);
		/**
		 * Supported NK_ACCESSIBILITY_CAN_* action flags.
		 */
		actions: nk_accessibility_actions @offset(24);
		/**
		 * Reserved; set to zero.
		 */
		reserved0: u32 @offset(28);
		/**
		 * Left edge in surface-local logical pixels.
		 */
		x: f32 @offset(32);
		/**
		 * Top edge in surface-local logical pixels.
		 */
		y: f32 @offset(36);
		/**
		 * Node width in logical pixels; must be non-negative.
		 */
		width: f32 @offset(40);
		/**
		 * Node height in logical pixels; must be non-negative.
		 */
		height: f32 @offset(44);
		/**
		 * Human-facing accessible name; nullable UTF-8 copied during the call.
		 */
		label: nullable<utf8> @offset(48);
		/**
		 * Optional textual value; nullable UTF-8 copied during the call.
		 */
		value: nullable<utf8> @offset(56);
		/**
		 * Current numeric value, used especially by slider roles.
		 */
		numeric_value: f64 @offset(64);
		/**
		 * Minimum numeric value for a ranged role.
		 */
		numeric_minimum: f64 @offset(72);
		/**
		 * Maximum numeric value for a ranged role.
		 */
		numeric_maximum: f64 @offset(80);
		/**
		 * Code-point offset where `value` begins in the complete document.
		 */
		text_start: nk_accessibility_text_position @offset(88);
		/**
		 * Total code-point length of the complete document.
		 */
		document_length: nk_accessibility_text_position @offset(92);
		/**
		 * Selection start in document coordinates, or NONE for no selection.
		 */
		selection_start: nk_accessibility_text_position @offset(96);
		/**
		 * Selection end in document coordinates, or NONE for no selection.
		 */
		selection_end: nk_accessibility_text_position @offset(100);
	}
	/**
	 * Screen-reader geometry for one non-empty text range in a node's value.
	 */
	struct nk_accessibility_text_range @layout(24, 4) {
		/**
		 * Inclusive start code-point offset in the complete document.
		 */
		start: nk_accessibility_text_position @offset(0);
		/**
		 * Exclusive end code-point offset in the complete document.
		 */
		end: nk_accessibility_text_position @offset(4);
		/**
		 * Left edge in surface-local logical pixels.
		 */
		x: f32 @offset(8);
		/**
		 * Top edge in surface-local logical pixels.
		 */
		y: f32 @offset(12);
		/**
		 * Range width in logical pixels; must be non-negative.
		 */
		width: f32 @offset(16);
		/**
		 * Range height in logical pixels; must be non-negative.
		 */
		height: f32 @offset(20);
	}
	/**
	 * Atomic batch of accessibility node replacements, removals, and focus.
	 *
	 * Arrays are borrowed for the duration of the call and copied on success.
	 * Removed nodes are removed recursively. Replacement nodes are processed in
	 * array order, so parents must precede their children. Invalid batches leave
	 * the existing tree unchanged.
	 */
	struct nk_accessibility_update @layout(48, 8) {
		/**
		 * Set to sizeof(nk_accessibility_update) or a larger compatible size.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Optional NK_ACCESSIBILITY_UPDATE_* flags.
		 */
		flags: nk_accessibility_update_flags @offset(4);
		/**
		 * Nodes to insert or replace; borrowed during the call.
		 */
		nodes: ptr<const<nk_accessibility_node>> @offset(8) @borrowed @length_field("node_count");
		/**
		 * Number of entries in `nodes`.
		 */
		node_count: u32 @offset(16);
		/**
		 * Node IDs to remove recursively; borrowed during the call.
		 */
		removed_nodes: ptr<const<nk_accessibility_node_id>> @offset(24);
		/**
		 * Number of entries in `removed_nodes`.
		 */
		removed_node_count: u32 @offset(32);
		/**
		 * Node to focus when NK_ACCESSIBILITY_UPDATE_FOCUS is set; root clears focus.
		 */
		focus: nk_accessibility_node_id @offset(36);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(40);
	}
	/**
	 * Header at the start of NK_EVENT_CLIPBOARD_FILES_COMPLETE data.
	 */
	struct nk_clipboard_files @layout(8, 4) {
		/**
		 * Number of paths in the event payload.
		 */
		path_count: u32 @offset(0);
		/**
		 * Byte offset from this header to the packed UTF-8 path strings.
		 */
		strings_offset: u32 @offset(4);
	}
	/**
	 * RGBA8 pixels used to create a custom cursor.
	 */
	struct nk_cursor_image @layout(48, 8) {
		/**
		 * Set to sizeof(nk_cursor_image) before calling nk_cursor_create_custom.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Image width in pixels.
		 */
		width: i32 @offset(4);
		/**
		 * Image height in pixels.
		 */
		height: i32 @offset(8);
		/**
		 * Number of bytes between the starts of adjacent rows.
		 */
		stride: i32 @offset(12);
		/**
		 * Horizontal hotspot position in pixels.
		 */
		hotspot_x: i32 @offset(16);
		/**
		 * Vertical hotspot position in pixels.
		 */
		hotspot_y: i32 @offset(20);
		/**
		 * Pointer to width * height RGBA8 pixels, with the first row at the top.
		 */
		rgba: ptr<const<void>> @offset(24);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: array<u64, 2> @offset(32);
	}
	/**
	 * One user-visible name and glob pattern set for a file dialog.
	 */
	struct nk_dialog_filter @layout(16, 8) {
		/**
		 * Optional label shown for the filter.
		 */
		name: nullable<utf8> @offset(0);
		/**
		 * Semicolon-separated UTF-8 glob patterns, for example "*.png;*.jpg".
		 */
		patterns: utf8 @offset(8);
	}
	/**
	 * Payload of NK_EVENT_DIALOG_MESSAGE_COMPLETE.
	 */
	struct nk_dialog_message_result @layout(4, 4) {
		/**
		 * Button selected by the user, or NK_MESSAGE_RESULT_NONE on cancellation.
		 */
		button: nk_message_result @offset(0);
	}
	/**
	 * Header at the start of NK_EVENT_DIALOG_PATHS_COMPLETE data.
	 */
	struct nk_dialog_paths @layout(16, 4) {
		/**
		 * 1 when the user accepted the dialog, and 0 when it was cancelled.
		 */
		accepted: nk_bool @offset(0);
		/**
		 * Number of returned paths.
		 */
		path_count: u32 @offset(4);
		/**
		 * Byte offset from the payload start to the uint32_t path-offset table.
		 */
		offsets_offset: u32 @offset(8);
		/**
		 * Byte offset from the payload start to the packed NUL-terminated paths.
		 */
		strings_offset: u32 @offset(12);
	}
	/**
	 * Header at the start of NK_EVENT_DROP_FILES and NK_EVENT_DROP_TEXT data.
	 */
	struct nk_drop_data @layout(16, 4) {
		/**
		 * Drop position in the receiving window's logical coordinates.
		 */
		x: i32 @offset(0);
		/**
		 * Drop position in the receiving window's logical coordinates.
		 */
		y: i32 @offset(4);
		/**
		 * Number of dropped items in the event payload.
		 */
		item_count: u32 @offset(8);
		/**
		 * Byte offset from this header to the packed UTF-8 item strings.
		 */
		strings_offset: u32 @offset(12);
	}
	/**
	 * Variable-length event data is owned by NativeKit. It remains valid until
	 * nk_event_release() is called. A successfully polled event must be released,
	 * even when data is NULL. Strings are UTF-8 and data_size excludes any trailing
	 * NUL byte. Callers must zero-initialize this structure and set struct_size.
	 */
	struct nk_event @layout(64, 8) {
		/**
		 * Size of this structure in bytes; must be set before polling.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Event kind; use this to select the payload interpretation.
		 */
		kind: nk_event_kind @offset(4);
		/**
		 * Originating resource handle, or NK_INVALID_HANDLE for global events.
		 */
		source: nk_handle @offset(8);
		/**
		 * Event-specific flags; zero when the event kind defines no flags.
		 */
		flags: u32 @offset(12);
		/**
		 * Related asynchronous request, or NK_INVALID_REQUEST_ID when absent.
		 */
		request_id: nk_request_id @offset(16);
		/**
		 * Result associated with a completion event; usually NK_OK on success.
		 */
		result: nk_result @offset(24);
		/**
		 * Number of logical records in data for payloads that contain an array.
		 */
		data_count: u32 @offset(28);
		/**
		 * Borrowed pointer to event-owned payload bytes; valid until release.
		 */
		data: ptr<const<void>> @offset(32) @borrowed @length_field("data_size");
		/**
		 * Payload size in bytes, excluding any trailing NUL byte.
		 */
		data_size: u64 @offset(40);
		/**
		 * Reserved for compatible extensions; initialize to zero.
		 */
		reserved: array<u64, 2> @offset(48);
	}
	/**
	 * Options shared by local-file and URI-resource dialogs.
	 */
	struct nk_file_dialog_options @layout(48, 8) {
		/**
		 * Set to sizeof(nk_file_dialog_options) before starting the dialog.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Bitwise OR of NK_DIALOG_* flags.
		 */
		flags: nk_dialog_flags @offset(4);
		/**
		 * Optional dialog title.
		 */
		title: nullable<utf8> @offset(8);
		/**
		 * Optional initial local path or platform resource location.
		 */
		initial_path: nullable<utf8> @offset(16);
		/**
		 * Optional suggested filename for save operations.
		 */
		suggested_name: nullable<utf8> @offset(24);
		/**
		 * Caller-owned filters read during the call and copied by NativeKit.
		 */
		filters: ptr<const<nk_dialog_filter>> @offset(32) @borrowed @length_field("filter_count");
		/**
		 * Number of entries in `filters`.
		 */
		filter_count: u32 @offset(40);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(44);
	}
	/**
	 * Payload of NK_EVENT_GAMEPAD_AXIS.
	 */
	struct nk_gamepad_axis_event @layout(8, 4) {
		/**
		 * Canonical axis index.
		 */
		axis: nk_gamepad_axis @offset(0);
		/**
		 * Normalized axis value.
		 */
		value: f32 @offset(4);
	}
	/**
	 * Payload of NK_EVENT_GAMEPAD_BUTTON.
	 */
	struct nk_gamepad_button_event @layout(8, 4) {
		/**
		 * Canonical button index.
		 */
		button: nk_gamepad_button @offset(0);
		/**
		 * 1 while the button is pressed, and 0 when it is released.
		 */
		pressed: nk_bool @offset(4);
	}
	/**
	 * Process-wide normalization options for mapped gamepads.
	 */
	struct nk_gamepad_options @layout(40, 8) {
		/**
		 * Set to sizeof(nk_gamepad_options) before setting or getting options.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Radial dead-zone fraction for both sticks, in [0, 1).
		 */
		stick_dead_zone: f32 @offset(4);
		/**
		 * Dead-zone fraction for both triggers, in [0, 1).
		 */
		trigger_dead_zone: f32 @offset(8);
		/**
		 * Bitwise OR of NK_GAMEPAD_* flags.
		 */
		flags: nk_gamepad_flags @offset(12);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(16);
		/**
		 * Reserved for future normalization options; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(24);
	}
	/**
	 * Normalized canonical gamepad state for one mapped joystick.
	 */
	struct nk_gamepad_state @layout(80, 8) {
		/**
		 * Set to sizeof(nk_gamepad_state) before requesting state.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Canonical button states, each normalized to zero or one.
		 */
		buttons: array<u8, 15> @offset(4);
		/**
		 * Canonical axis values, normally normalized to [-1, 1].
		 */
		axes: array<f32, 6> @offset(20);
		/**
		 * Reserved for future state data; set all elements to zero.
		 */
		reserved: array<u64, 4> @offset(48);
	}
	/**
	 * Options used to start one NativeKit runtime generation.
	 */
	struct nk_init_options @layout(16, 4) {
		/**
		 * Size of this structure in bytes; must be at least sizeof(nk_init_options).
		 */
		struct_size: u32 @offset(0);
		/**
		 * ABI version requested by the caller; normally NK_API_VERSION.
		 */
		api_version: u32 @offset(4);
		/**
		 * Event queue capacity; zero selects the backend default.
		 */
		event_queue_capacity: u32 @offset(8);
		/**
		 * Reserved for compatible extensions; initialize to zero.
		 */
		reserved: u32 @offset(12);
	}
	/**
	 * Payload of NK_EVENT_JOYSTICK_AXIS.
	 */
	struct nk_joystick_axis_event @layout(8, 4) {
		/**
		 * Zero-based raw joystick axis index.
		 */
		axis: u32 @offset(0);
		/**
		 * Axis value normalized to [-1, 1].
		 */
		value: f32 @offset(4);
	}
	/**
	 * Payload of NK_EVENT_JOYSTICK_BUTTON.
	 */
	struct nk_joystick_button_event @layout(8, 4) {
		/**
		 * Zero-based raw joystick button index.
		 */
		button: u32 @offset(0);
		/**
		 * 1 while the button is pressed, and 0 when it is released.
		 */
		pressed: nk_bool @offset(4);
	}
	/**
	 * Payload of NK_EVENT_JOYSTICK_HAT.
	 */
	struct nk_joystick_hat_event @layout(8, 4) {
		/**
		 * Zero-based raw joystick hat index.
		 */
		hat: u32 @offset(0);
		/**
		 * Bitwise OR of NK_JOYSTICK_HAT_* direction values.
		 */
		value: u32 @offset(4);
	}
	/**
	 * Payload of NK_EVENT_KEY. Scancodes remain platform-specific.
	 */
	struct nk_key_event @layout(16, 4) {
		/**
		 * Normalized key identifier, or NK_KEY_UNKNOWN when it is unavailable.
		 */
		key: nk_key @offset(0);
		/**
		 * Platform hardware scancode, for applications that need physical keys.
		 */
		scancode: u32 @offset(4);
		/**
		 * Press, release, or repeat transition.
		 */
		action: nk_input_action @offset(8);
		/**
		 * Modifier keys active for this event.
		 */
		modifiers: nk_modifiers @offset(12);
	}
	/**
	 * Options for a native message dialog.
	 */
	struct nk_message_dialog_options @layout(32, 8) {
		/**
		 * Set to sizeof(nk_message_dialog_options) before starting the dialog.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Message presentation kind.
		 */
		kind: nk_message_kind @offset(4);
		/**
		 * Bitwise OR of NK_MESSAGE_BUTTON_* values; zero defaults to OK.
		 */
		buttons: nk_message_buttons @offset(8);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(12);
		/**
		 * Optional dialog title.
		 */
		title: nullable<utf8> @offset(16);
		/**
		 * Required UTF-8 message text.
		 */
		message: utf8 @offset(24);
	}
	/**
	 * Geometry, work area, physical size, and scale of a monitor.
	 */
	struct nk_monitor_geometry @layout(72, 8) {
		/**
		 * Set to sizeof(nk_monitor_geometry) before passing the structure.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Monitor origin in logical desktop coordinates.
		 */
		x: i32 @offset(4);
		/**
		 * Monitor origin in logical desktop coordinates.
		 */
		y: i32 @offset(8);
		/**
		 * Monitor width in logical pixels.
		 */
		width: i32 @offset(12);
		/**
		 * Monitor height in logical pixels.
		 */
		height: i32 @offset(16);
		/**
		 * Work-area origin in logical desktop coordinates.
		 */
		work_x: i32 @offset(20);
		/**
		 * Work-area origin in logical desktop coordinates.
		 */
		work_y: i32 @offset(24);
		/**
		 * Work-area width in logical pixels, excluding reserved system areas.
		 */
		work_width: i32 @offset(28);
		/**
		 * Work-area height in logical pixels, excluding reserved system areas.
		 */
		work_height: i32 @offset(32);
		/**
		 * Physical monitor width in millimeters, or zero when unavailable.
		 */
		width_mm: i32 @offset(36);
		/**
		 * Physical monitor height in millimeters, or zero when unavailable.
		 */
		height_mm: i32 @offset(40);
		/**
		 * Horizontal scale from logical pixels to framebuffer pixels.
		 */
		scale_x: f32 @offset(44);
		/**
		 * Vertical scale from logical pixels to framebuffer pixels.
		 */
		scale_y: f32 @offset(48);
		/**
		 * Reserved; set to zero.
		 */
		reserved: array<u64, 2> @offset(56);
	}
	/**
	 * Borrowed platform handles for interoperating with a NativeKit window.
	 */
	struct nk_native_window @layout(64, 8) {
		/**
		 * Set to sizeof(nk_native_window) before passing this structure to NativeKit.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Identifies which platform-specific fields are populated.
		 */
		kind: nk_native_window_kind @offset(4);
		/**
		 * Reserved for future platform-neutral interoperation flags; initialize to zero.
		 */
		flags: u32 @offset(8);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: u32 @offset(12);
		/**
		 * Platform display connection, when the selected kind uses one.
		 */
		display: usize @offset(16);
		/**
		 * Platform window or surface handle.
		 */
		window: usize @offset(24);
		/**
		 * Optional platform view handle, currently used by Cocoa.
		 */
		view: usize @offset(32);
		/**
		 * Reserved pointer-sized values; initialize to zero.
		 */
		reserved2: array<usize, 3> @offset(40);
	}
	/**
	 * Options for an asynchronous desktop notification request.
	 */
	struct nk_notification_options @layout(56, 8) {
		/**
		 * Set to sizeof(nk_notification_options) before passing the structure.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Bitwise OR of NK_NOTIFICATION_* flags.
		 */
		flags: nk_notification_flags @offset(4);
		/**
		 * Required UTF-8 notification title.
		 */
		title: utf8 @offset(8);
		/**
		 * Optional UTF-8 notification body.
		 */
		body: nullable<utf8> @offset(16);
		/**
		 * Optional platform-resolved icon name or absolute file path.
		 */
		icon: nullable<utf8> @offset(24);
		/**
		 * Requested display duration in milliseconds; zero uses the platform default.
		 */
		timeout_ms: u32 @offset(32);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(36);
		/**
		 * Reserved for future notification options; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(40);
	}
	/**
	 * Payload of NK_EVENT_POINTER_BUTTON in surface-local logical pixels.
	 */
	struct nk_pointer_button_event @layout(32, 8) {
		/**
		 * Pointer button that changed.
		 */
		button: nk_pointer_button @offset(0);
		/**
		 * Press or release transition.
		 */
		action: nk_input_action @offset(4);
		/**
		 * Modifier keys active for this event.
		 */
		modifiers: nk_modifiers @offset(8);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: u32 @offset(12);
		/**
		 * Pointer x coordinate in logical pixels.
		 */
		x: f64 @offset(16);
		/**
		 * Pointer y coordinate in logical pixels.
		 */
		y: f64 @offset(24);
	}
	/**
	 * Payload of NK_EVENT_POINTER_MOVE in surface-local logical pixels.
	 */
	struct nk_pointer_move_event @layout(16, 8) {
		/**
		 * Pointer x coordinate in logical pixels.
		 */
		x: f64 @offset(0);
		/**
		 * Pointer y coordinate in logical pixels.
		 */
		y: f64 @offset(8);
	}
	/**
	 * Payload of NK_EVENT_POINTER_SCROLL containing scroll deltas.
	 */
	struct nk_pointer_scroll_event @layout(16, 8) {
		/**
		 * Horizontal scroll delta; smooth devices may report fractional values.
		 */
		x: f64 @offset(0);
		/**
		 * Vertical scroll delta; smooth devices may report fractional values.
		 */
		y: f64 @offset(8);
	}
	/**
	 * Header at the start of NK_EVENT_SHARE_RECEIVED data.
	 */
	struct nk_received_share @layout(16, 4) {
		/**
		 * Byte offset to the embedded nk_resource_list.
		 */
		resources_offset: u32 @offset(0);
		/**
		 * Byte offset to optional shared UTF-8 text, or zero when absent.
		 */
		text_offset: u32 @offset(4);
		/**
		 * Byte offset to optional UTF-8 subject text, or zero when absent.
		 */
		subject_offset: u32 @offset(8);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(12);
	}
	/**
	 * URI identity and metadata for a resource.
	 */
	struct nk_resource @layout(56, 8) {
		/**
		 * Set to sizeof(nk_resource) before passing the structure.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Access and persistence flags known by the producer.
		 */
		flags: nk_resource_flags @offset(4);
		/**
		 * Required absolute UTF-8 URI; this is not necessarily a filesystem path.
		 */
		uri: utf8 @offset(8);
		/**
		 * Optional UTF-8 MIME type.
		 */
		mime_type: nullable<utf8> @offset(16);
		/**
		 * Optional UTF-8 name suitable for display.
		 */
		display_name: nullable<utf8> @offset(24);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(32);
		/**
		 * Reserved for future resource metadata; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(40);
	}
	/**
	 * Header at the start of NK_EVENT_RESOURCE_DROP data. Coordinates are logical pixels.
	 */
	struct nk_resource_drop @layout(32, 8) {
		/**
		 * Byte offset to the embedded nk_resource_list.
		 */
		resources_offset: u32 @offset(0);
		/**
		 * Byte offset to optional dropped UTF-8 text, or zero when absent.
		 */
		text_offset: u32 @offset(4);
		/**
		 * Drop x coordinate in the host's logical pixels.
		 */
		x: f32 @offset(8);
		/**
		 * Drop y coordinate in the host's logical pixels.
		 */
		y: f32 @offset(12);
		/**
		 * Reserved for future drop information; set all elements to zero.
		 */
		reserved: array<u64, 2> @offset(16);
	}
	/**
	 * One resource entry in a packed resource-bearing event payload.
	 */
	struct nk_resource_item @layout(16, 4) {
		/**
		 * Access and persistence flags for this resource.
		 */
		flags: nk_resource_flags @offset(0);
		/**
		 * Byte offset to the required URI string, relative to the payload start.
		 */
		uri_offset: u32 @offset(4);
		/**
		 * Byte offset to the optional MIME string, or zero when absent.
		 */
		mime_type_offset: u32 @offset(8);
		/**
		 * Byte offset to the optional display-name string, or zero when absent.
		 */
		display_name_offset: u32 @offset(12);
	}
	/**
	 * Header and item table stored in resource-bearing event data.
	 */
	struct nk_resource_list @layout(16, 4) {
		/**
		 * 1 when the user accepted the operation, and 0 when it was cancelled.
		 */
		accepted: nk_bool @offset(0);
		/**
		 * Number of resource items in the following item table.
		 */
		item_count: u32 @offset(4);
		/**
		 * Byte offset from the payload start to the nk_resource_item table.
		 */
		items_offset: u32 @offset(8);
		/**
		 * Byte offset from the payload start to packed NUL-terminated strings.
		 */
		strings_offset: u32 @offset(12);
	}
	/**
	 * Capabilities and size information for an opened resource stream.
	 */
	struct nk_resource_stream_info @layout(32, 8) {
		/**
		 * Set to sizeof(nk_resource_stream_info) before querying.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Bitwise OR of NK_RESOURCE_STREAM_* capabilities.
		 */
		flags: nk_resource_stream_flags @offset(4);
		/**
		 * Size in bytes, or UINT64_MAX when the provider cannot report it.
		 */
		size: u64 @offset(8);
		/**
		 * Reserved for future stream information; set all elements to zero.
		 */
		reserved: array<u64, 2> @offset(16);
	}
	/**
	 * Borrowed resource metadata decoded from an event payload.
	 */
	struct nk_resource_view @layout(72, 8) {
		/**
		 * Set to sizeof(nk_resource_view) before decoding an item.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Access and persistence flags for this resource.
		 */
		flags: nk_resource_flags @offset(4);
		/**
		 * Borrowed UTF-8 URI; valid until nk_event_release().
		 */
		uri: ptr<const<c_char>> @offset(8);
		/**
		 * URI length in bytes, excluding its trailing NUL.
		 */
		uri_length: u32 @offset(16);
		/**
		 * Borrowed optional MIME string, or NULL when absent.
		 */
		mime_type: ptr<const<c_char>> @offset(24);
		/**
		 * MIME length in bytes, excluding its trailing NUL.
		 */
		mime_type_length: u32 @offset(32);
		/**
		 * Borrowed optional display name, or NULL when absent.
		 */
		display_name: ptr<const<c_char>> @offset(40);
		/**
		 * Display-name length in bytes, excluding its trailing NUL.
		 */
		display_name_length: u32 @offset(48);
		/**
		 * Reserved for future decoded metadata; set all elements to zero.
		 */
		reserved: array<u64, 2> @offset(56);
	}
	/**
	 * Text and URI resources supplied to the platform share UI.
	 */
	struct nk_share_options @layout(56, 8) {
		/**
		 * Set to sizeof(nk_share_options) before sharing.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Reserved; set to zero.
		 */
		flags: u32 @offset(4);
		/**
		 * Optional title shown by the share UI.
		 */
		title: nullable<utf8> @offset(8);
		/**
		 * Optional UTF-8 text to share.
		 */
		text: nullable<utf8> @offset(16);
		/**
		 * Caller-owned resources read during the call and copied by NativeKit.
		 */
		resources: ptr<const<nk_resource>> @offset(24) @borrowed @length_field("resource_count");
		/**
		 * Number of entries in `resources`.
		 */
		resource_count: u32 @offset(32);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(36);
		/**
		 * Reserved for future share options; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(40);
	}
	/**
	 * Backend-native render target for the current surface frame.
	 *
	 * Callers must make the surface current before requesting this descriptor.
	 * For OpenGL and OpenGL ES, `native_target` is the current draw framebuffer
	 * name, with zero representing the default framebuffer. Other graphics APIs
	 * may use the field for a backend-native target handle in a future backend.
	 */
	struct nk_surface_frame_target @layout(40, 8) {
		/**
		 * Set to sizeof(nk_surface_frame_target) or a larger compatible size.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Graphics API that owns the target.
		 */
		api: nk_graphics_api @offset(4);
		/**
		 * Current framebuffer width in device pixels.
		 */
		width: i32 @offset(8);
		/**
		 * Current framebuffer height in device pixels.
		 */
		height: i32 @offset(12);
		/**
		 * Backend-native target token; opaque to NativeKit callers.
		 */
		native_target: u64 @offset(16);
		/**
		 * Reserved for future target metadata; set to zero.
		 */
		reserved: array<u64, 2> @offset(24);
	}
	/**
	 * Options used to create a NativeKit graphics surface.
	 */
	struct nk_surface_options @layout(64, 8) {
		/**
		 * Set to sizeof(nk_surface_options) or a larger compatible size.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Bitwise OR of NK_SURFACE_* flags.
		 */
		flags: nk_surface_flags @offset(4);
		/**
		 * Graphics API and context family to request.
		 */
		api: nk_graphics_api @offset(8);
		/**
		 * Major context/API version, when applicable.
		 */
		major_version: u32 @offset(12);
		/**
		 * Minor context/API version, when applicable.
		 */
		minor_version: u32 @offset(16);
		/**
		 * Child-surface x position in the parent window's logical coordinates.
		 */
		x: i32 @offset(20);
		/**
		 * Child-surface y position in the parent window's logical coordinates.
		 */
		y: i32 @offset(24);
		/**
		 * Initial logical width; must be positive.
		 */
		width: i32 @offset(28);
		/**
		 * Initial logical height; must be positive.
		 */
		height: i32 @offset(32);
		/**
		 * Optional compatible surface whose graphics context should be shared.
		 */
		share_surface: nk_handle @offset(36);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(40);
		/**
		 * Reserved for future options; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(48);
	}
	/**
	 * Payload of NK_EVENT_SURFACE_RESIZE.
	 */
	struct nk_surface_resize_event @layout(16, 4) {
		/**
		 * New logical surface width.
		 */
		width: i32 @offset(0);
		/**
		 * New logical surface height.
		 */
		height: i32 @offset(4);
		/**
		 * New framebuffer width in device pixels.
		 */
		framebuffer_width: i32 @offset(8);
		/**
		 * New framebuffer height in device pixels.
		 */
		framebuffer_height: i32 @offset(12);
	}
	/**
	 * Current system appearance reported by the attached platform.
	 */
	struct nk_system_appearance @layout(32, 8) {
		/**
		 * Set to sizeof(nk_system_appearance) before the query.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Current platform color scheme.
		 */
		color_scheme: nk_color_scheme @offset(4);
		/**
		 * Non-zero when the platform's high-contrast mode is enabled.
		 */
		high_contrast: u32 @offset(8);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(12);
		/**
		 * Reserved for future appearance information; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(16);
	}
	/**
	 * Payload of NK_EVENT_TEXT_EDIT; positions are absolute code-point indices.
	 */
	struct nk_text_edit_event @layout(48, 4) {
		/**
		 * Operation to apply to the editor's text model.
		 */
		action: nk_text_edit_action @offset(0);
		/**
		 * Byte offset of replacement UTF-8 text in the event payload, or zero.
		 */
		text_offset: u32 @offset(4);
		/**
		 * Byte length of replacement UTF-8 text, excluding its trailing NUL.
		 */
		text_length: u32 @offset(8);
		/**
		 * Start of the document range to replace.
		 */
		replace_start: nk_text_position @offset(12);
		/**
		 * Exclusive end of the document range to replace.
		 */
		replace_end: nk_text_position @offset(16);
		/**
		 * Selection start after applying the transaction.
		 */
		selection_start: nk_text_position @offset(20);
		/**
		 * Exclusive selection end after applying the transaction.
		 */
		selection_end: nk_text_position @offset(24);
		/**
		 * Active composition start after applying the transaction, or NONE.
		 */
		composition_start: nk_text_position @offset(28);
		/**
		 * Exclusive active composition end after applying the transaction, or NONE.
		 */
		composition_end: nk_text_position @offset(32);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: array<u32, 3> @offset(36);
	}
	/**
	 * Payload of NK_EVENT_TEXT_INPUT containing committed Unicode text.
	 */
	struct nk_text_input_event @layout(8, 4) {
		/**
		 * Unicode code point committed by the keyboard or input method.
		 */
		codepoint: u32 @offset(0);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: u32 @offset(4);
	}
	/**
	 * State published by a custom editor to synchronize the platform IME.
	 */
	struct nk_text_input_state @layout(80, 8) {
		/**
		 * Set to sizeof(nk_text_input_state) before calling NativeKit.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Text-input behavior requested from the platform IME.
		 */
		flags: nk_text_input_flags @offset(4);
		/**
		 * NUL-terminated UTF-8 window into the document, used during the call.
		 */
		text: utf8 @offset(8);
		/**
		 * Absolute code-point index of the first code point in text.
		 */
		text_start: nk_text_position @offset(16);
		/**
		 * Total document length in Unicode code points.
		 */
		document_length: nk_text_position @offset(20);
		/**
		 * Absolute selection start; must be within the supplied text window.
		 */
		selection_start: nk_text_position @offset(24);
		/**
		 * Exclusive absolute selection end; must be within the supplied text window.
		 */
		selection_end: nk_text_position @offset(28);
		/**
		 * Absolute composition start, or NK_TEXT_POSITION_NONE.
		 */
		composition_start: nk_text_position @offset(32);
		/**
		 * Exclusive absolute composition end, or NK_TEXT_POSITION_NONE.
		 */
		composition_end: nk_text_position @offset(36);
		/**
		 * Keyboard purpose requested for the editor.
		 */
		input_type: nk_text_input_type @offset(40);
		/**
		 * Enter-key action requested for the editor.
		 */
		action: nk_text_input_action @offset(44);
		/**
		 * Cursor x coordinate in surface-local logical pixels.
		 */
		cursor_x: f32 @offset(48);
		/**
		 * Cursor y coordinate in surface-local logical pixels.
		 */
		cursor_y: f32 @offset(52);
		/**
		 * Cursor width in surface-local logical pixels.
		 */
		cursor_width: f32 @offset(56);
		/**
		 * Cursor height in surface-local logical pixels.
		 */
		cursor_height: f32 @offset(60);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: array<u64, 2> @offset(64);
	}
	/**
	 * Payload of NK_EVENT_TOUCH in surface-local logical pixels.
	 */
	struct nk_touch_event @layout(48, 8) {
		/**
		 * Identifier that remains stable for the lifetime of this contact.
		 */
		pointer_id: u32 @offset(0);
		/**
		 * Begin, move, end, or cancellation transition.
		 */
		action: nk_touch_action @offset(4);
		/**
		 * Finger, stylus, or eraser tool.
		 */
		tool: nk_touch_tool @offset(8);
		/**
		 * Modifier keys active for this event.
		 */
		modifiers: nk_modifiers @offset(12);
		/**
		 * Touch x coordinate in logical pixels.
		 */
		x: f64 @offset(16);
		/**
		 * Touch y coordinate in logical pixels.
		 */
		y: f64 @offset(24);
		/**
		 * Normalized pressure, when available; otherwise a backend default.
		 */
		pressure: f32 @offset(32);
		/**
		 * Stylus tilt around the x axis, in backend-defined normalized units.
		 */
		tilt_x: f32 @offset(36);
		/**
		 * Stylus tilt around the y axis, in backend-defined normalized units.
		 */
		tilt_y: f32 @offset(40);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: u32 @offset(44);
	}
	/**
	 * A display mode reported by a monitor backend.
	 */
	struct nk_video_mode @layout(56, 8) {
		/**
		 * Set to sizeof(nk_video_mode) before passing the structure.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Mode width in physical pixels.
		 */
		width: i32 @offset(4);
		/**
		 * Mode height in physical pixels.
		 */
		height: i32 @offset(8);
		/**
		 * Nominal refresh rate in hertz.
		 */
		refresh_rate: f64 @offset(16);
		/**
		 * Red channel precision in bits.
		 */
		red_bits: u32 @offset(24);
		/**
		 * Green channel precision in bits.
		 */
		green_bits: u32 @offset(28);
		/**
		 * Blue channel precision in bits.
		 */
		blue_bits: u32 @offset(32);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(36);
		/**
		 * Reserved for future mode information; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(40);
	}
	/**
	 * Options used to create a native child WebView.
	 */
	struct nk_webview_options @layout(48, 8) {
		/**
		 * Set to sizeof(nk_webview_options) before creating the WebView.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Bitwise OR of NK_WEBVIEW_* flags.
		 */
		flags: nk_webview_flags @offset(4);
		/**
		 * Child position in the parent's logical coordinates.
		 */
		x: i32 @offset(8);
		/**
		 * Child position in the parent's logical coordinates.
		 */
		y: i32 @offset(12);
		/**
		 * Initial logical width; must be positive.
		 */
		width: i32 @offset(16);
		/**
		 * Initial logical height; must be positive.
		 */
		height: i32 @offset(20);
		/**
		 * Optional UTF-8 URL loaded after creation.
		 */
		initial_url: nullable<utf8> @offset(24);
		/**
		 * Reserved for future WebView options; set all elements to zero.
		 */
		reserved: array<u64, 2> @offset(32);
	}
	/**
	 * Per-axis content scale returned for a window.
	 */
	struct nk_window_content_scale @layout(32, 8) {
		/**
		 * Set to sizeof(nk_window_content_scale) before calling the query API.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Horizontal logical-to-device-pixel scale factor.
		 */
		x: f32 @offset(4);
		/**
		 * Vertical logical-to-device-pixel scale factor.
		 */
		y: f32 @offset(8);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: u32 @offset(12);
		/**
		 * Reserved pointer-sized values; initialize to zero.
		 */
		reserved2: array<u64, 2> @offset(16);
	}
	/**
	 * Non-client frame extents returned in logical screen pixels.
	 */
	struct nk_window_frame_extents @layout(40, 8) {
		/**
		 * Set to sizeof(nk_window_frame_extents) before calling the query API.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Distance from the client area's left edge to the window frame.
		 */
		left: i32 @offset(4);
		/**
		 * Distance from the client area's top edge to the window frame.
		 */
		top: i32 @offset(8);
		/**
		 * Distance from the client area's right edge to the window frame.
		 */
		right: i32 @offset(12);
		/**
		 * Distance from the client area's bottom edge to the window frame.
		 */
		bottom: i32 @offset(16);
		/**
		 * Reserved for future use; initialize to zero.
		 */
		reserved: u32 @offset(20);
		/**
		 * Reserved pointer-sized values; initialize to zero.
		 */
		reserved2: array<u64, 2> @offset(24);
	}
	/**
	 * Payload of NK_EVENT_WINDOW_FRAMEBUFFER_RESIZE; dimensions are device pixels.
	 */
	struct nk_window_framebuffer_resize_event @layout(8, 4) {
		/**
		 * New framebuffer width in device pixels.
		 */
		width: i32 @offset(0);
		/**
		 * New framebuffer height in device pixels.
		 */
		height: i32 @offset(4);
	}
	/**
	 * Payload of NK_EVENT_WINDOW_MOVE; coordinates are logical screen pixels.
	 */
	struct nk_window_move_event @layout(8, 4) {
		/**
		 * New logical x coordinate of the top-level window.
		 */
		x: i32 @offset(0);
		/**
		 * New logical y coordinate of the top-level window.
		 */
		y: i32 @offset(4);
	}
	/**
	 * Options for creating a NativeKit-owned top-level window.
	 */
	struct nk_window_options @layout(40, 8) {
		/**
		 * Set to sizeof(nk_window_options) before calling nk_window_create.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Combination of NK_WINDOW_* creation flags.
		 */
		flags: nk_window_flags @offset(4);
		/**
		 * Initial client width in logical pixels; must be positive.
		 */
		width: i32 @offset(8);
		/**
		 * Initial client height in logical pixels; must be positive.
		 */
		height: i32 @offset(12);
		/**
		 * Optional NUL-terminated UTF-8 title, copied by the create call.
		 */
		title: nullable<utf8> @offset(16);
		/**
		 * Optional owner window; owned windows are destroyed with their owner.
		 */
		owner: nk_handle @offset(24);
		/**
		 * Window category, such as NK_WINDOW_NORMAL or NK_WINDOW_UTILITY.
		 */
		kind: nk_window_kind @offset(28);
		/**
		 * Reserved for future options; initialize to zero.
		 */
		reserved: u64 @offset(32);
	}
	/**
	 * Payload of NK_EVENT_WINDOW_RESIZE; dimensions are logical pixels.
	 */
	struct nk_window_resize_event @layout(8, 4) {
		/**
		 * New client width in logical pixels.
		 */
		width: i32 @offset(0);
		/**
		 * New client height in logical pixels.
		 */
		height: i32 @offset(4);
	}
	/**
	 * Payload of NK_EVENT_WINDOW_SCALE_CHANGED.
	 */
	struct nk_window_scale_event @layout(4, 4) {
		/**
		 * New logical-to-device-pixel scale factor.
		 */
		scale: f32 @offset(0);
	}
	/**
	 * Logical-pixel size constraints for a window.
	 */
	struct nk_window_size_limits @layout(40, 8) {
		/**
		 * Set to sizeof(nk_window_size_limits) before passing it to NativeKit.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Minimum client width; zero disables the minimum.
		 */
		min_width: i32 @offset(4);
		/**
		 * Minimum client height; zero disables the minimum.
		 */
		min_height: i32 @offset(8);
		/**
		 * Maximum client width; zero disables the maximum.
		 */
		max_width: i32 @offset(12);
		/**
		 * Maximum client height; zero disables the maximum.
		 */
		max_height: i32 @offset(16);
		/**
		 * Reserved pointer-sized values; initialize to zero.
		 */
		reserved: array<u64, 2> @offset(24);
	}
	/**
	 * Current window state flags returned by nk_window_get_state.
	 */
	struct nk_window_state @layout(24, 8) {
		/**
		 * Set to sizeof(nk_window_state) before calling nk_window_get_state.
		 */
		struct_size: u32 @offset(0);
		/**
		 * Combination of NK_WINDOW_STATE_* flags.
		 */
		flags: nk_window_state_flags @offset(4);
		/**
		 * Reserved pointer-sized values; initialize to zero.
		 */
		reserved: array<u64, 2> @offset(8);
	}
	/**
	 * Positive identifier for a caller-owned virtual accessibility node.
	 */
	type nk_accessibility_node_id = u32;
	/**
	 * Unicode code-point offset used by accessibility text APIs.
	 */
	type nk_accessibility_text_position = u32;
	/**
	 * Boolean value used by the NativeKit ABI. Zero is false; non-zero input is
	 * true; NativeKit normalizes boolean outputs to zero or one.
	 */
	type nk_bool = u32;
	/**
	 * Function-pointer type returned by nk_surface_get_proc_address().
	 */
	callback nk_graphics_proc = fn() -> void;
	/**
	 * Opaque generation-checked identifier for a live NativeKit resource.
	 */
	handle nk_handle : u32;
	/**
	 * Normalized physical or logical keyboard key identifier.
	 */
	type nk_key = u32;
	/**
	 * Identifier for one asynchronous operation; it is not a resource handle.
	 */
	type nk_request_id = u64;
	/**
	 * Callback invoked while a surface framebuffer is current and ready to draw.
	 *
	 * The callback runs on the UI thread. `user_data` is the value supplied to
	 * nk_surface_set_frame_callback().
	 */
	callback nk_surface_frame_callback = fn(arg0: nk_handle, arg1: i32, arg2: i32, arg3: ptr<void>) -> void;
	/**
	 * Absolute Unicode code-point position used by the text-input API.
	 */
	type nk_text_position = u32;
}
