// Generated by Haxeon from modules/audio/bindings/nativekit_audio_import.h for portable-abi64. Do not edit.
interface NativeKitAudio @target("portable-abi64") @library("nativekit") @depends("NativeKit") {
	/**
	 * Distance attenuation model applied by a spatialized voice.
	 */
	enum nk_audio_attenuation_model : u32 {
		/**
		 * No distance attenuation.
		 */
		NK_AUDIO_ATTENUATION_NONE = 0;
		/**
		 * Inverse-distance attenuation, clamped to the configured distances.
		 */
		NK_AUDIO_ATTENUATION_INVERSE = 1;
		/**
		 * Linear attenuation between the configured distances.
		 */
		NK_AUDIO_ATTENUATION_LINEAR = 2;
		/**
		 * Exponential attenuation between the configured distances.
		 */
		NK_AUDIO_ATTENUATION_EXPONENTIAL = 3;
	}
	/**
	 * Loading lifecycle for an audio clip. Synchronous clips start ready.
	 */
	enum nk_audio_clip_load_state : u32 {
		/**
		 * The encoded resource bytes are still loading.
		 */
		NK_AUDIO_CLIP_LOADING = 0;
		/**
		 * The encoded resource bytes have been validated and are ready for voices.
		 */
		NK_AUDIO_CLIP_READY = 1;
		/**
		 * The resource or its encoded audio data failed to load.
		 */
		NK_AUDIO_CLIP_LOAD_FAILED = 2;
	}
	/**
	 * Built-in processing effect types available on mixer buses.
	 */
	enum nk_audio_effect_type : u32 {
		/**
		 * A Butterworth low-pass filter.
		 */
		NK_AUDIO_EFFECT_LOW_PASS = 0;
		/**
		 * A Butterworth high-pass filter.
		 */
		NK_AUDIO_EFFECT_HIGH_PASS = 1;
		/**
		 * A feedback delay with configurable wet/dry mix.
		 */
		NK_AUDIO_EFFECT_DELAY = 2;
	}
	/**
	 * Coordinate space used by a voice's spatial position.
	 */
	enum nk_audio_positioning : u32 {
		/**
		 * The voice position is expressed in world space.
		 */
		NK_AUDIO_POSITIONING_ABSOLUTE = 0;
		/**
		 * The voice position is expressed relative to the listener.
		 */
		NK_AUDIO_POSITIONING_RELATIVE = 1;
	}
	/**
	 * Flags controlling how a voice is loaded and played.
	 */
	flags nk_audio_voice_flags : u32 {
		/**
		 * Repeats the voice after it reaches its end.
		 */
		NK_AUDIO_VOICE_LOOPING = 1;
		/**
		 * Streams the source instead of decoding the complete source up front.
		 */
		NK_AUDIO_VOICE_STREAM = 2;
		/**
		 * Loads the source asynchronously when the backend supports it.
		 */
		NK_AUDIO_VOICE_ASYNC = 4;
	}
	/**
	 * Loading lifecycle for an audio voice. Synchronous voices start ready.
	 */
	enum nk_audio_voice_load_state : u32 {
		/**
		 * The asynchronous source is still loading.
		 */
		NK_AUDIO_VOICE_LOADING = 0;
		/**
		 * The source has reached its playable readiness point.
		 */
		NK_AUDIO_VOICE_READY = 1;
		/**
		 * The source failed to load; the failure event carries the result code.
		 */
		NK_AUDIO_VOICE_LOAD_FAILED = 2;
	}
	/**
	 * Optional behavior flags for file and resource dialogs.
	 */
	flags nk_dialog_flags : u32 {
		/**
		 * Allow selecting more than one item in an open dialog.
		 */
		NK_DIALOG_ALLOW_MULTIPLE = 1;
		/**
		 * Ask for confirmation before replacing an existing destination.
		 */
		NK_DIALOG_CONFIRM_OVERWRITE = 2;
		/**
		 * Show hidden files and directories where supported.
		 */
		NK_DIALOG_SHOW_HIDDEN = 4;
	}
	/**
	 * Operation encoded in a dialog completion event's flags.
	 */
	enum nk_dialog_operation : u32 {
		/**
		 * Select one or more local files.
		 */
		NK_DIALOG_OPEN_FILE = 1;
		/**
		 * Select a destination for a local file.
		 */
		NK_DIALOG_SAVE_FILE = 2;
		/**
		 * Select a local directory.
		 */
		NK_DIALOG_SELECT_DIRECTORY = 3;
		/**
		 * Show a native message and collect the selected button.
		 */
		NK_DIALOG_MESSAGE = 4;
		/**
		 * Select one or more URI resources.
		 */
		NK_DIALOG_OPEN_RESOURCE = 5;
		/**
		 * Select a destination URI resource.
		 */
		NK_DIALOG_SAVE_RESOURCE = 6;
		/**
		 * Select a URI resource directory.
		 */
		NK_DIALOG_SELECT_RESOURCE_DIRECTORY = 7;
	}
	/**
	 * Button flags accepted by a message dialog.
	 */
	flags nk_message_buttons : u32 {
		/**
		 * Show an OK button.
		 */
		NK_MESSAGE_BUTTON_OK = 1;
		/**
		 * Show a Cancel button.
		 */
		NK_MESSAGE_BUTTON_CANCEL = 2;
		/**
		 * Show a Yes button.
		 */
		NK_MESSAGE_BUTTON_YES = 4;
		/**
		 * Show a No button.
		 */
		NK_MESSAGE_BUTTON_NO = 8;
	}
	/**
	 * Visual and semantic kind of a message dialog.
	 */
	enum nk_message_kind : u32 {
		/**
		 * Informational message.
		 */
		NK_MESSAGE_INFO = 0;
		/**
		 * Warning message.
		 */
		NK_MESSAGE_WARNING = 1;
		/**
		 * Error message.
		 */
		NK_MESSAGE_ERROR = 2;
		/**
		 * Question that expects a choice.
		 */
		NK_MESSAGE_QUESTION = 3;
	}
	/**
	 * Button value returned in a message-dialog completion payload.
	 */
	enum nk_message_result : u32 {
		/**
		 * No button was selected, normally because the dialog was cancelled.
		 */
		NK_MESSAGE_RESULT_NONE = 0;
		/**
		 * The OK button was selected.
		 */
		NK_MESSAGE_RESULT_OK = 1;
		/**
		 * The Cancel button was selected.
		 */
		NK_MESSAGE_RESULT_CANCEL = 2;
		/**
		 * The Yes button was selected.
		 */
		NK_MESSAGE_RESULT_YES = 3;
		/**
		 * The No button was selected.
		 */
		NK_MESSAGE_RESULT_NO = 4;
	}
	/**
	 * Access and persistence flags attached to a URI resource.
	 */
	flags nk_resource_flags : u32 {
		/**
		 * The resource may be opened for reading.
		 */
		NK_RESOURCE_READABLE = 1;
		/**
		 * The resource may be opened for writing.
		 */
		NK_RESOURCE_WRITABLE = 2;
		/**
		 * The access grant is retained beyond the originating operation.
		 */
		NK_RESOURCE_PERSISTED = 4;
	}
	/**
	 * Access and creation flags accepted by nk_resource_open().
	 */
	flags nk_resource_open_flags : u32 {
		/**
		 * Open the stream for reading.
		 */
		NK_RESOURCE_OPEN_READ = 1;
		/**
		 * Open the stream for writing.
		 */
		NK_RESOURCE_OPEN_WRITE = 2;
		/**
		 * Create the resource when it does not already exist.
		 */
		NK_RESOURCE_OPEN_CREATE = 4;
		/**
		 * Truncate an existing resource before writing.
		 */
		NK_RESOURCE_OPEN_TRUNCATE = 8;
	}
	/**
	 * Capabilities reported for an opened resource stream.
	 */
	flags nk_resource_stream_flags : u32 {
		/**
		 * The stream accepts reads.
		 */
		NK_RESOURCE_STREAM_READABLE = 1;
		/**
		 * The stream accepts writes.
		 */
		NK_RESOURCE_STREAM_WRITABLE = 2;
		/**
		 * The stream supports position changes.
		 */
		NK_RESOURCE_STREAM_SEEKABLE = 4;
		/**
		 * The stream size field contains a known byte count.
		 */
		NK_RESOURCE_STREAM_SIZE_KNOWN = 8;
	}
	/**
	 * Reference point used by nk_resource_seek().
	 */
	enum nk_seek_origin : u32 {
		/**
		 * Offset from the beginning of the stream.
		 */
		NK_SEEK_START = 0;
		/**
		 * Offset from the current stream position.
		 */
		NK_SEEK_CURRENT = 1;
		/**
		 * Offset from the end of the stream.
		 */
		NK_SEEK_END = 2;
	}
	/**
	 * Clears the bus's scheduled start, stop, and fade transitions.
	 */
	extern fn nk_audio_bus_clear_schedule(bus: nk_audio_bus) -> nk_result;
	/**
	 * Creates an active mixer bus. Voices retain their bus until destroyed.
	 */
	extern fn nk_audio_bus_create(out_bus: ptr<nk_audio_bus> @out @owned) -> nk_result;
	/**
	 * Stops and destroys a mixer bus and its effects, invalidating their handles.
	 */
	extern fn nk_audio_bus_destroy(bus: nk_audio_bus) -> nk_result;
	/**
	 * Appends a delay to the bus effect chain. Delay frames must be positive and
	 * decay is in the range [0, 1].
	 */
	extern fn nk_audio_bus_effect_create_delay(bus: nk_audio_bus, delay_pcm_frames: u32, decay: f32, out_effect: ptr<nk_audio_bus_effect> @out @owned) -> nk_result;
	/**
	 * Appends a high-pass filter to the bus effect chain.
	 */
	extern fn nk_audio_bus_effect_create_high_pass(bus: nk_audio_bus, cutoff_frequency_hz: f32, order: u32, out_effect: ptr<nk_audio_bus_effect> @out @owned) -> nk_result;
	/**
	 * Appends a low-pass filter to the bus effect chain. Cutoff is in Hz and
	 * order must be in the range [1, 8].
	 */
	extern fn nk_audio_bus_effect_create_low_pass(bus: nk_audio_bus, cutoff_frequency_hz: f32, order: u32, out_effect: ptr<nk_audio_bus_effect> @out @owned) -> nk_result;
	/**
	 * Removes an effect from its bus and invalidates its handle.
	 */
	extern fn nk_audio_bus_effect_destroy(effect: nk_audio_bus_effect) -> nk_result;
	/**
	 * Returns a delay's feedback decay.
	 */
	extern fn nk_audio_bus_effect_get_delay_decay(effect: nk_audio_bus_effect, out_decay: ptr<f32> @out) -> nk_result;
	/**
	 * Returns a delay's dry gain.
	 */
	extern fn nk_audio_bus_effect_get_delay_dry(effect: nk_audio_bus_effect, out_dry: ptr<f32> @out) -> nk_result;
	/**
	 * Returns a delay's wet gain.
	 */
	extern fn nk_audio_bus_effect_get_delay_wet(effect: nk_audio_bus_effect, out_wet: ptr<f32> @out) -> nk_result;
	/**
	 * Returns a high-pass filter's cutoff and order.
	 */
	extern fn nk_audio_bus_effect_get_high_pass(effect: nk_audio_bus_effect, out_cutoff_frequency_hz: ptr<f32> @out, out_order: ptr<u32> @out) -> nk_result;
	/**
	 * Returns a low-pass filter's cutoff and order.
	 */
	extern fn nk_audio_bus_effect_get_low_pass(effect: nk_audio_bus_effect, out_cutoff_frequency_hz: ptr<f32> @out, out_order: ptr<u32> @out) -> nk_result;
	/**
	 * Returns an effect's zero-based position in its bus chain.
	 */
	extern fn nk_audio_bus_effect_get_position(effect: nk_audio_bus_effect, out_position: ptr<u32> @out) -> nk_result;
	/**
	 * Returns the effect type.
	 */
	extern fn nk_audio_bus_effect_get_type(effect: nk_audio_bus_effect, out_type: ptr<nk_audio_effect_type> @out) -> nk_result;
	/**
	 * Returns whether an effect is active in its bus chain.
	 */
	extern fn nk_audio_bus_effect_is_enabled(effect: nk_audio_bus_effect, out_enabled: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Sets a delay's feedback decay.
	 */
	extern fn nk_audio_bus_effect_set_delay_decay(effect: nk_audio_bus_effect, decay: f32) -> nk_result;
	/**
	 * Sets a delay's dry gain.
	 */
	extern fn nk_audio_bus_effect_set_delay_dry(effect: nk_audio_bus_effect, dry: f32) -> nk_result;
	/**
	 * Sets a delay's wet gain.
	 */
	extern fn nk_audio_bus_effect_set_delay_wet(effect: nk_audio_bus_effect, wet: f32) -> nk_result;
	/**
	 * Enables or bypasses an effect without removing it from the chain.
	 */
	extern fn nk_audio_bus_effect_set_enabled(effect: nk_audio_bus_effect, enabled: nk_bool) -> nk_result;
	/**
	 * Updates a high-pass filter's cutoff and order.
	 */
	extern fn nk_audio_bus_effect_set_high_pass(effect: nk_audio_bus_effect, cutoff_frequency_hz: f32, order: u32) -> nk_result;
	/**
	 * Updates a low-pass filter's cutoff and order.
	 */
	extern fn nk_audio_bus_effect_set_low_pass(effect: nk_audio_bus_effect, cutoff_frequency_hz: f32, order: u32) -> nk_result;
	/**
	 * Moves an effect to a zero-based position in its bus chain.
	 */
	extern fn nk_audio_bus_effect_set_position(effect: nk_audio_bus_effect, position: u32) -> nk_result;
	/**
	 * Fades the bus between linear gains over a duration in PCM frames. The
	 * starting volume may be NK_AUDIO_VOLUME_CURRENT.
	 */
	extern fn nk_audio_bus_fade(bus: nk_audio_bus, volume_begin: f32, volume_end: f32, duration_pcm_frames: u64) -> nk_result;
	/**
	 * Fades the bus at an absolute process-wide audio time in PCM frames.
	 */
	extern fn nk_audio_bus_fade_at(bus: nk_audio_bus, volume_begin: f32, volume_end: f32, duration_pcm_frames: u64, absolute_start_time_pcm_frames: u64) -> nk_result;
	/**
	 * Returns the bus's configured linear gain.
	 */
	extern fn nk_audio_bus_get_volume(bus: nk_audio_bus, out_volume: ptr<f32> @out) -> nk_result;
	/**
	 * Returns whether the bus is muted.
	 */
	extern fn nk_audio_bus_is_muted(bus: nk_audio_bus, out_muted: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Returns whether at least one voice routed through the bus is playing.
	 */
	extern fn nk_audio_bus_is_playing(bus: nk_audio_bus, out_playing: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Schedules all voices routed through the bus to start at an absolute
	 * process-wide audio time in PCM frames. Call nk_audio_bus_start() after
	 * setting the schedule.
	 */
	extern fn nk_audio_bus_schedule_start(bus: nk_audio_bus, absolute_time_pcm_frames: u64) -> nk_result;
	/**
	 * Schedules all voices routed through the bus to stop at an absolute
	 * process-wide audio time in PCM frames.
	 */
	extern fn nk_audio_bus_schedule_stop(bus: nk_audio_bus, absolute_time_pcm_frames: u64) -> nk_result;
	/**
	 * Enables or disables the bus mute state without changing its configured gain.
	 */
	extern fn nk_audio_bus_set_muted(bus: nk_audio_bus, muted: nk_bool) -> nk_result;
	/**
	 * Sets the bus's linear gain; zero mutes the bus.
	 */
	extern fn nk_audio_bus_set_volume(bus: nk_audio_bus, volume: f32) -> nk_result;
	/**
	 * Starts all voices routed through the bus.
	 */
	extern fn nk_audio_bus_start(bus: nk_audio_bus) -> nk_result;
	/**
	 * Stops all voices routed through the bus without destroying them.
	 */
	extern fn nk_audio_bus_stop(bus: nk_audio_bus) -> nk_result;
	/**
	 * Creates a reusable clip from a UTF-8 native filesystem path. NativeKit
	 * copies the path and validates the source before returning. The clip is
	 * stopped and has no playback state; create a voice to play it.
	 */
	extern fn nk_audio_clip_create_from_file(path: utf8, out_clip: ptr<nk_audio_clip> @out @owned) -> nk_result;
	/**
	 * Creates a reusable clip from encoded audio bytes. NativeKit copies the
	 * bytes before returning, so the caller may release its buffer after the
	 * call. The source currently supports WAV, FLAC, and MP3.
	 */
	extern fn nk_audio_clip_create_from_memory(data: ptr<const<void>>, data_size: u64, out_clip: ptr<nk_audio_clip> @out @owned) -> nk_result;
	/**
	 * Creates a reusable clip from a readable URI resource. NativeKit opens the
	 * resource through its platform provider and keeps each voice's decoder
	 * backed by an independent resource stream.
	 */
	extern fn nk_audio_clip_create_from_resource(resource: ptr<const<nk_resource>>, out_clip: ptr<nk_audio_clip> @out @owned) -> nk_result;
	/**
	 * Starts loading a reusable clip from a URI resource. The clip handle is
	 * returned immediately in NK_AUDIO_CLIP_LOADING; completion is reported by
	 * NK_EVENT_AUDIO_CLIP_READY or NK_EVENT_AUDIO_CLIP_LOAD_FAILED with the
	 * returned request ID.
	 */
	extern fn nk_audio_clip_create_from_resource_async(resource: ptr<const<nk_resource>>, out_clip: ptr<nk_audio_clip> @out @owned, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Stops using and destroys a clip. Existing voices retain their source.
	 */
	extern fn nk_audio_clip_destroy(clip: nk_audio_clip) -> nk_result;
	/**
	 * Returns the loading lifecycle state of an audio clip.
	 */
	extern fn nk_audio_clip_get_load_state(clip: nk_audio_clip, out_state: ptr<nk_audio_clip_load_state> @out) -> nk_result;
	/**
	 * Returns linear gain for the process-wide NativeKit audio mixer.
	 */
	extern fn nk_audio_get_master_volume(out_volume: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the process-wide audio engine sample rate in frames per second.
	 */
	extern fn nk_audio_get_sample_rate(out_sample_rate: ptr<u32> @out) -> nk_result;
	/**
	 * Returns the process-wide audio engine clock in PCM frames.
	 */
	extern fn nk_audio_get_time_pcm_frames(out_time_pcm_frames: ptr<u64> @out) -> nk_result;
	/**
	 * Returns the listener's directional attenuation cone.
	 */
	extern fn nk_audio_listener_get_cone(out_inner_angle_radians: ptr<f32> @out, out_outer_angle_radians: ptr<f32> @out, out_outer_gain: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the listener's forward direction.
	 */
	extern fn nk_audio_listener_get_direction(out_direction: ptr<nk_audio_vec3> @out) -> nk_result;
	/**
	 * Returns the single process-wide listener's world position.
	 */
	extern fn nk_audio_listener_get_position(out_position: ptr<nk_audio_vec3> @out) -> nk_result;
	/**
	 * Returns the speed of sound used for Doppler calculations.
	 */
	extern fn nk_audio_listener_get_speed_of_sound(out_speed: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the listener's velocity in world units per second.
	 */
	extern fn nk_audio_listener_get_velocity(out_velocity: ptr<nk_audio_vec3> @out) -> nk_result;
	/**
	 * Returns the listener's world-up direction.
	 */
	extern fn nk_audio_listener_get_world_up(out_world_up: ptr<nk_audio_vec3> @out) -> nk_result;
	/**
	 * Sets the listener's directional attenuation cone in radians and linear gain.
	 */
	extern fn nk_audio_listener_set_cone(inner_angle_radians: f32, outer_angle_radians: f32, outer_gain: f32) -> nk_result;
	/**
	 * Sets the listener's forward direction; it must be finite and non-zero.
	 */
	extern fn nk_audio_listener_set_direction(direction: nk_audio_vec3) -> nk_result;
	/**
	 * Sets the single process-wide listener's world position.
	 */
	extern fn nk_audio_listener_set_position(position: nk_audio_vec3) -> nk_result;
	/**
	 * Sets the speed of sound used for Doppler calculations in world units per second.
	 */
	extern fn nk_audio_listener_set_speed_of_sound(speed: f32) -> nk_result;
	/**
	 * Sets the listener's velocity in world units per second.
	 */
	extern fn nk_audio_listener_set_velocity(velocity: nk_audio_vec3) -> nk_result;
	/**
	 * Sets the listener's world-up direction; it must be finite and non-zero.
	 */
	extern fn nk_audio_listener_set_world_up(world_up: nk_audio_vec3) -> nk_result;
	/**
	 * Sets linear gain for the process-wide NativeKit audio mixer.
	 */
	extern fn nk_audio_set_master_volume(volume: f32) -> nk_result;
	/**
	 * Returns whether a voice has reached its end.
	 */
	extern fn nk_audio_voice_at_end(voice: nk_audio_voice, out_at_end: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Clears a voice's scheduled start, stop, and fade transitions.
	 */
	extern fn nk_audio_voice_clear_schedule(voice: nk_audio_voice) -> nk_result;
	/**
	 * Creates a stopped independent playback voice from a reusable clip.
	 */
	extern fn nk_audio_voice_create(clip: nk_audio_clip, options: ptr<const<nk_audio_voice_options>>, out_voice: ptr<nk_audio_voice> @out @owned) -> nk_result;
	/**
	 * Stops and destroys a playback voice, invalidating its handle.
	 */
	extern fn nk_audio_voice_destroy(voice: nk_audio_voice) -> nk_result;
	/**
	 * Fades a voice over a duration in PCM frames. volume_begin may be
	 * NK_AUDIO_VOLUME_CURRENT; volume_end must be finite and non-negative.
	 */
	extern fn nk_audio_voice_fade(voice: nk_audio_voice, volume_begin: f32, volume_end: f32, duration_pcm_frames: u64) -> nk_result;
	/**
	 * Fades a voice starting at an absolute process-wide audio time in PCM frames.
	 */
	extern fn nk_audio_voice_fade_at(voice: nk_audio_voice, volume_begin: f32, volume_end: f32, duration_pcm_frames: u64, absolute_start_time_pcm_frames: u64) -> nk_result;
	/**
	 * Returns the voice's distance attenuation model.
	 */
	extern fn nk_audio_voice_get_attenuation_model(voice: nk_audio_voice, out_model: ptr<nk_audio_attenuation_model> @out) -> nk_result;
	/**
	 * Returns the voice's directional attenuation cone.
	 */
	extern fn nk_audio_voice_get_cone(voice: nk_audio_voice, out_inner_angle_radians: ptr<f32> @out, out_outer_angle_radians: ptr<f32> @out, out_outer_gain: ptr<f32> @out) -> nk_result;
	/**
	 * Returns a voice's forward direction.
	 */
	extern fn nk_audio_voice_get_direction(voice: nk_audio_voice, out_direction: ptr<nk_audio_vec3> @out) -> nk_result;
	/**
	 * Returns how strongly source direction affects per-channel spatial gain.
	 */
	extern fn nk_audio_voice_get_directional_attenuation_factor(voice: nk_audio_voice, out_factor: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the minimum and maximum distances used by attenuation.
	 */
	extern fn nk_audio_voice_get_distance_limits(voice: nk_audio_voice, out_min_distance: ptr<f32> @out, out_max_distance: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the voice's Doppler multiplier.
	 */
	extern fn nk_audio_voice_get_doppler_factor(voice: nk_audio_voice, out_factor: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the minimum and maximum gain applied by spatialization.
	 */
	extern fn nk_audio_voice_get_gain_limits(voice: nk_audio_voice, out_min_gain: ptr<f32> @out, out_max_gain: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the decoded voice length in seconds.
	 */
	extern fn nk_audio_voice_get_length_seconds(voice: nk_audio_voice, out_seconds: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the asynchronous loading lifecycle state of a voice.
	 */
	extern fn nk_audio_voice_get_load_state(voice: nk_audio_voice, out_state: ptr<nk_audio_voice_load_state> @out) -> nk_result;
	/**
	 * Returns stereo voice pan in the range [-1, 1].
	 */
	extern fn nk_audio_voice_get_pan(voice: nk_audio_voice, out_pan: ptr<f32> @out) -> nk_result;
	/**
	 * Returns voice playback pitch.
	 */
	extern fn nk_audio_voice_get_pitch(voice: nk_audio_voice, out_pitch: ptr<f32> @out) -> nk_result;
	/**
	 * Returns a voice's position.
	 */
	extern fn nk_audio_voice_get_position(voice: nk_audio_voice, out_position: ptr<nk_audio_vec3> @out) -> nk_result;
	/**
	 * Returns the voice's position interpretation.
	 */
	extern fn nk_audio_voice_get_positioning(voice: nk_audio_voice, out_positioning: ptr<nk_audio_positioning> @out) -> nk_result;
	/**
	 * Returns the distance attenuation rolloff.
	 */
	extern fn nk_audio_voice_get_rolloff(voice: nk_audio_voice, out_rolloff: ptr<f32> @out) -> nk_result;
	/**
	 * Returns the current voice playback position in seconds.
	 */
	extern fn nk_audio_voice_get_time_seconds(voice: nk_audio_voice, out_seconds: ptr<f32> @out) -> nk_result;
	/**
	 * Returns a voice's velocity.
	 */
	extern fn nk_audio_voice_get_velocity(voice: nk_audio_voice, out_velocity: ptr<nk_audio_vec3> @out) -> nk_result;
	/**
	 * Returns linear voice gain.
	 */
	extern fn nk_audio_voice_get_volume(voice: nk_audio_voice, out_volume: ptr<f32> @out) -> nk_result;
	/**
	 * Returns whether voice looping is enabled.
	 */
	extern fn nk_audio_voice_is_looping(voice: nk_audio_voice, out_looping: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Returns whether a voice is currently playing.
	 */
	extern fn nk_audio_voice_is_playing(voice: nk_audio_voice, out_playing: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Returns whether 3D spatialization is enabled for a voice.
	 */
	extern fn nk_audio_voice_is_spatialization_enabled(voice: nk_audio_voice, out_enabled: ptr<nk_bool> @out) -> nk_result;
	/**
	 * Seeks a voice back to its beginning without changing its playing state.
	 */
	extern fn nk_audio_voice_rewind(voice: nk_audio_voice) -> nk_result;
	/**
	 * Schedules a voice to start at an absolute process-wide audio time in PCM
	 * frames. Call nk_audio_voice_start() after setting the schedule.
	 */
	extern fn nk_audio_voice_schedule_start(voice: nk_audio_voice, absolute_time_pcm_frames: u64) -> nk_result;
	/**
	 * Schedules a voice to stop at an absolute process-wide audio time in PCM
	 * frames. A scheduled stop does not emit a natural-end completion event.
	 */
	extern fn nk_audio_voice_schedule_stop(voice: nk_audio_voice, absolute_time_pcm_frames: u64) -> nk_result;
	/**
	 * Sets the voice's distance attenuation model.
	 */
	extern fn nk_audio_voice_set_attenuation_model(voice: nk_audio_voice, model: nk_audio_attenuation_model) -> nk_result;
	/**
	 * Sets the voice's directional attenuation cone in radians and linear gain.
	 */
	extern fn nk_audio_voice_set_cone(voice: nk_audio_voice, inner_angle_radians: f32, outer_angle_radians: f32, outer_gain: f32) -> nk_result;
	/**
	 * Sets a voice's forward direction; it must be finite and non-zero.
	 */
	extern fn nk_audio_voice_set_direction(voice: nk_audio_voice, direction: nk_audio_vec3) -> nk_result;
	/**
	 * Sets how strongly source direction affects per-channel spatial gain.
	 */
	extern fn nk_audio_voice_set_directional_attenuation_factor(voice: nk_audio_voice, factor: f32) -> nk_result;
	/**
	 * Sets the minimum and maximum distances used by attenuation.
	 */
	extern fn nk_audio_voice_set_distance_limits(voice: nk_audio_voice, min_distance: f32, max_distance: f32) -> nk_result;
	/**
	 * Sets the voice's Doppler multiplier; zero disables Doppler pitch shifting.
	 */
	extern fn nk_audio_voice_set_doppler_factor(voice: nk_audio_voice, factor: f32) -> nk_result;
	/**
	 * Sets the minimum and maximum gain applied by spatialization.
	 */
	extern fn nk_audio_voice_set_gain_limits(voice: nk_audio_voice, min_gain: f32, max_gain: f32) -> nk_result;
	/**
	 * Enables or disables voice looping.
	 */
	extern fn nk_audio_voice_set_looping(voice: nk_audio_voice, looping: nk_bool) -> nk_result;
	/**
	 * Sets stereo voice pan in the range [-1, 1].
	 */
	extern fn nk_audio_voice_set_pan(voice: nk_audio_voice, pan: f32) -> nk_result;
	/**
	 * Sets voice playback pitch where 1 is the source pitch.
	 */
	extern fn nk_audio_voice_set_pitch(voice: nk_audio_voice, pitch: f32) -> nk_result;
	/**
	 * Sets a voice's position in world or listener-relative coordinates.
	 */
	extern fn nk_audio_voice_set_position(voice: nk_audio_voice, position: nk_audio_vec3) -> nk_result;
	/**
	 * Sets whether the voice position is absolute or listener-relative.
	 */
	extern fn nk_audio_voice_set_positioning(voice: nk_audio_voice, positioning: nk_audio_positioning) -> nk_result;
	/**
	 * Sets the distance attenuation rolloff; zero disables falloff progression.
	 */
	extern fn nk_audio_voice_set_rolloff(voice: nk_audio_voice, rolloff: f32) -> nk_result;
	/**
	 * Enables or disables 3D spatialization for a voice.
	 */
	extern fn nk_audio_voice_set_spatialization_enabled(voice: nk_audio_voice, enabled: nk_bool) -> nk_result;
	/**
	 * Sets a voice's velocity in world units per second.
	 */
	extern fn nk_audio_voice_set_velocity(voice: nk_audio_voice, velocity: nk_audio_vec3) -> nk_result;
	/**
	 * Sets linear voice gain; zero mutes the voice.
	 */
	extern fn nk_audio_voice_set_volume(voice: nk_audio_voice, volume: f32) -> nk_result;
	/**
	 * Starts a voice from its current position.
	 */
	extern fn nk_audio_voice_start(voice: nk_audio_voice) -> nk_result;
	/**
	 * Stops a voice without rewinding it.
	 */
	extern fn nk_audio_voice_stop(voice: nk_audio_voice) -> nk_result;
	/**
	 * Starts an asynchronous read of URI resources from the system clipboard.
	 */
	extern fn nk_clipboard_read_resources(out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Copies URI resources into the system clipboard.
	 */
	extern fn nk_clipboard_set_resources(resources: ptr<const<nk_resource>> @in_array("resource_count"), resource_count: u32) -> nk_result;
	/**
	 * Cancels a pending dialog. Its completion event is still emitted.
	 */
	extern fn nk_dialog_cancel(request: nk_request_id) -> nk_result;
	/**
	 * Validates and returns a path from a dialog event. The returned UTF-8 view is
	 * owned by the event and remains valid until nk_event_release().
	 */
	extern fn nk_dialog_event_path(event: ptr<const<nk_event>>, index: u32, out_path: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Starts an asynchronous native message dialog.
	 */
	extern fn nk_dialog_message(parent: nk_handle, options: ptr<const<nk_message_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts a non-blocking native dialog on the UI thread. `parent` may be zero;
	 * where a parent is supported, desktop backends accept an nk_window and Android
	 * resource dialogs accept an nk_mobile_host. All strings and filters are copied
	 * before the function returns. Completion uses NK_EVENT_DIALOG_PATHS_COMPLETE
	 * or NK_EVENT_DIALOG_MESSAGE_COMPLETE, with the operation in event.flags and the
	 * returned request ID in event.request_id.
	 */
	extern fn nk_dialog_open_file(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Resource dialogs return NK_EVENT_DIALOG_RESOURCES_COMPLETE with
	 * nk_resource_list data. `parent` is a desktop nk_window or an Android
	 * nk_mobile_host; zero requests an unparented dialog where the backend supports
	 * it.
	 */
	extern fn nk_dialog_open_resource(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native save-file dialog.
	 */
	extern fn nk_dialog_save_file(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native URI-resource save dialog.
	 */
	extern fn nk_dialog_save_resource(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native directory-selection dialog.
	 */
	extern fn nk_dialog_select_directory(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Starts an asynchronous native URI-resource directory dialog.
	 */
	extern fn nk_dialog_select_resource_directory(parent: nk_handle, options: ptr<const<nk_file_dialog_options>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Closes a resource stream and invalidates its handle.
	 */
	extern fn nk_resource_close(stream: nk_resource_stream) -> nk_result;
	/**
	 * Returns optional UTF-8 text from a resource-drop event.
	 */
	extern fn nk_resource_drop_event_text(event: ptr<const<nk_event>>, out_text: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Returned views remain owned by the event until nk_event_release().
	 */
	extern fn nk_resource_event_item(event: ptr<const<nk_event>>, index: u32, out_resource: ptr<nk_resource_view>) -> nk_result;
	/**
	 * Queries the readable/writable access retained for a URI resource.
	 */
	extern fn nk_resource_get_persisted_access(resource: ptr<const<nk_resource>>, out_flags: ptr<nk_resource_flags> @out) -> nk_result;
	/**
	 * Starts an asynchronous read of the complete URI resource. Completion is
	 * delivered through NK_EVENT_RESOURCE_DATA_COMPLETE; its event data contains
	 * the loaded bytes and its result reports the fetch outcome. Use
	 * nk_resource_load_cancel() to abort a pending load.
	 */
	extern fn nk_resource_load_async(resource: ptr<const<nk_resource>>, out_request: ptr<nk_request_id> @out) -> nk_result;
	/**
	 * Cancels a pending asynchronous resource load. No completion event is
	 * delivered for a successfully cancelled request; a backend completion that
	 * races with cancellation is consumed by NativeKit. This function is
	 * UI-thread-only.
	 */
	extern fn nk_resource_load_cancel(request: nk_request_id) -> nk_result;
	/**
	 * Opens a URI-backed stream on the UI thread. Stream operations copy bytes
	 * synchronously and may be called from worker threads. A successful read may
	 * return fewer bytes than requested; zero bytes means end of stream.
	 */
	extern fn nk_resource_open(resource: ptr<const<nk_resource>>, flags: nk_resource_open_flags, out_stream: ptr<nk_resource_stream> @out @owned) -> nk_result;
	/**
	 * Reads up to `size` bytes; a successful read may return fewer bytes.
	 */
	extern fn nk_resource_read(stream: nk_resource_stream, buffer: ptr<void>, size: u64, out_read: ptr<u64>) -> nk_result;
	/**
	 * Moves the stream position and returns the resulting absolute byte offset.
	 */
	extern fn nk_resource_seek(stream: nk_resource_stream, offset: i64, origin: nk_seek_origin, out_position: ptr<u64>) -> nk_result;
	/**
	 * Controls durable URI access on platforms that support it. access_flags is a
	 * combination of NK_RESOURCE_READABLE and NK_RESOURCE_WRITABLE; zero releases
	 * all persisted access. out_flags receives the actual resource flags retained.
	 * These functions are UI-thread-only.
	 */
	extern fn nk_resource_set_persisted_access(resource: ptr<const<nk_resource>>, access_flags: nk_resource_flags, out_flags: ptr<nk_resource_flags> @out) -> nk_result;
	/**
	 * Returns capabilities and size information for a resource stream.
	 */
	extern fn nk_resource_stream_info_get(stream: nk_resource_stream, out_info: ptr<nk_resource_stream_info>) -> nk_result;
	/**
	 * Writes up to `size` bytes; a successful write may transfer fewer bytes.
	 */
	extern fn nk_resource_write(stream: nk_resource_stream, buffer: ptr<const<void>>, size: u64, out_written: ptr<u64>) -> nk_result;
	/**
	 * Launches the platform share UI with optional text and URI resources.
	 */
	extern fn nk_share(options: ptr<const<nk_share_options>>) -> nk_result;
	/**
	 * Returns optional UTF-8 subject text from a received-share event.
	 */
	extern fn nk_share_event_subject(event: ptr<const<nk_event>>, out_subject: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * Returns optional UTF-8 text from a received-share event.
	 */
	extern fn nk_share_event_text(event: ptr<const<nk_event>>, out_text: ptr<ptr<const<c_char>>>, out_length: ptr<u32>) -> nk_result;
	/**
	 * URI inputs are copied before return. These functions are UI-thread-only.
	 */
	extern fn nk_shell_open_resource(resource: ptr<const<nk_resource>>) -> nk_result;
	/**
	 * Three-dimensional audio coordinate in the right-handed OpenGL convention.
	 */
	struct nk_audio_vec3 @layout(12, 4) {
		/**
		 * Positive X points right.
		 */
		x: f32 @offset(0);
		/**
		 * Positive Y points up.
		 */
		y: f32 @offset(4);
		/**
		 * Negative Z points forward.
		 */
		z: f32 @offset(8);
	}
	/**
	 * Optional creation and routing settings for a voice.
	 */
	struct nk_audio_voice_options @layout(32, 8) {
		/**
		 * Set to sizeof(nk_audio_voice_options) before passing the structure.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Combination of NK_AUDIO_VOICE_* flags.
		 */
		flags: nk_audio_voice_flags @offset(4);
		/**
		 * Optional mixer bus; NK_INVALID_HANDLE routes the voice to the master bus.
		 */
		bus: nk_audio_bus @offset(8);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(12);
		/**
		 * Reserved for compatible extensions; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(16);
	}
	/**
	 * One user-visible name and glob pattern set for a file dialog.
	 */
	struct nk_dialog_filter @layout(16, 8) {
		/**
		 * Optional label shown for the filter.
		 */
		name: nullable<utf8> @offset(0);
		/**
		 * Semicolon-separated UTF-8 glob patterns, for example "*.png;*.jpg".
		 */
		patterns: utf8 @offset(8);
	}
	/**
	 * Payload of NK_EVENT_DIALOG_MESSAGE_COMPLETE.
	 */
	struct nk_dialog_message_result @layout(4, 4) {
		/**
		 * Button selected by the user, or NK_MESSAGE_RESULT_NONE on cancellation.
		 */
		button: nk_message_result @offset(0);
	}
	/**
	 * Header at the start of NK_EVENT_DIALOG_PATHS_COMPLETE data.
	 */
	struct nk_dialog_paths @layout(16, 4) {
		/**
		 * 1 when the user accepted the dialog, and 0 when it was cancelled.
		 */
		accepted: nk_bool @offset(0);
		/**
		 * Number of returned paths.
		 */
		path_count: u32 @offset(4);
		/**
		 * Byte offset from the payload start to the uint32_t path-offset table.
		 */
		offsets_offset: u32 @offset(8);
		/**
		 * Byte offset from the payload start to the packed NUL-terminated paths.
		 */
		strings_offset: u32 @offset(12);
	}
	/**
	 * Options shared by local-file and URI-resource dialogs.
	 */
	struct nk_file_dialog_options @layout(48, 8) {
		/**
		 * Set to sizeof(nk_file_dialog_options) before starting the dialog.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Bitwise OR of NK_DIALOG_* flags.
		 */
		flags: nk_dialog_flags @offset(4);
		/**
		 * Optional dialog title.
		 */
		title: nullable<utf8> @offset(8);
		/**
		 * Optional initial local path or platform resource location.
		 */
		initial_path: nullable<utf8> @offset(16);
		/**
		 * Optional suggested filename for save operations.
		 */
		suggested_name: nullable<utf8> @offset(24);
		/**
		 * Caller-owned filters read during the call and copied by NativeKit.
		 */
		filters: ptr<const<nk_dialog_filter>> @offset(32) @borrowed @length_field("filter_count");
		/**
		 * Number of entries in `filters`.
		 */
		filter_count: u32 @offset(40);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(44);
	}
	/**
	 * Options for a native message dialog.
	 */
	struct nk_message_dialog_options @layout(32, 8) {
		/**
		 * Set to sizeof(nk_message_dialog_options) before starting the dialog.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Message presentation kind.
		 */
		kind: nk_message_kind @offset(4);
		/**
		 * Bitwise OR of NK_MESSAGE_BUTTON_* values; zero defaults to OK.
		 */
		buttons: nk_message_buttons @offset(8);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(12);
		/**
		 * Optional dialog title.
		 */
		title: nullable<utf8> @offset(16);
		/**
		 * Required UTF-8 message text.
		 */
		message: utf8 @offset(24);
	}
	/**
	 * Header at the start of NK_EVENT_SHARE_RECEIVED data.
	 */
	struct nk_received_share @layout(16, 4) {
		/**
		 * Byte offset to the embedded nk_resource_list.
		 */
		resources_offset: u32 @offset(0);
		/**
		 * Byte offset to optional shared UTF-8 text, or zero when absent.
		 */
		text_offset: u32 @offset(4);
		/**
		 * Byte offset to optional UTF-8 subject text, or zero when absent.
		 */
		subject_offset: u32 @offset(8);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(12);
	}
	/**
	 * URI identity and metadata for a resource.
	 */
	struct nk_resource @layout(56, 8) {
		/**
		 * Set to sizeof(nk_resource) before passing the structure.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Access and persistence flags known by the producer.
		 */
		flags: nk_resource_flags @offset(4);
		/**
		 * Required absolute UTF-8 URI; this is not necessarily a filesystem path.
		 */
		uri: utf8 @offset(8);
		/**
		 * Optional UTF-8 MIME type.
		 */
		mime_type: nullable<utf8> @offset(16);
		/**
		 * Optional UTF-8 name suitable for display.
		 */
		display_name: nullable<utf8> @offset(24);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(32);
		/**
		 * Reserved for future resource metadata; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(40);
	}
	/**
	 * Header at the start of NK_EVENT_RESOURCE_DROP data. Coordinates are logical pixels.
	 */
	struct nk_resource_drop @layout(32, 8) {
		/**
		 * Byte offset to the embedded nk_resource_list.
		 */
		resources_offset: u32 @offset(0);
		/**
		 * Byte offset to optional dropped UTF-8 text, or zero when absent.
		 */
		text_offset: u32 @offset(4);
		/**
		 * Drop x coordinate in the host's logical pixels.
		 */
		x: f32 @offset(8);
		/**
		 * Drop y coordinate in the host's logical pixels.
		 */
		y: f32 @offset(12);
		/**
		 * Reserved for future drop information; set all elements to zero.
		 */
		reserved: array<u64, 2> @offset(16);
	}
	/**
	 * One resource entry in a packed resource-bearing event payload.
	 */
	struct nk_resource_item @layout(16, 4) {
		/**
		 * Access and persistence flags for this resource.
		 */
		flags: nk_resource_flags @offset(0);
		/**
		 * Byte offset to the required URI string, relative to the payload start.
		 */
		uri_offset: u32 @offset(4);
		/**
		 * Byte offset to the optional MIME string, or zero when absent.
		 */
		mime_type_offset: u32 @offset(8);
		/**
		 * Byte offset to the optional display-name string, or zero when absent.
		 */
		display_name_offset: u32 @offset(12);
	}
	/**
	 * Header and item table stored in resource-bearing event data.
	 */
	struct nk_resource_list @layout(16, 4) {
		/**
		 * 1 when the user accepted the operation, and 0 when it was cancelled.
		 */
		accepted: nk_bool @offset(0);
		/**
		 * Number of resource items in the following item table.
		 */
		item_count: u32 @offset(4);
		/**
		 * Byte offset from the payload start to the nk_resource_item table.
		 */
		items_offset: u32 @offset(8);
		/**
		 * Byte offset from the payload start to packed NUL-terminated strings.
		 */
		strings_offset: u32 @offset(12);
	}
	/**
	 * Capabilities and size information for an opened resource stream.
	 */
	struct nk_resource_stream_info @layout(32, 8) {
		/**
		 * Set to sizeof(nk_resource_stream_info) before querying.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Bitwise OR of NK_RESOURCE_STREAM_* capabilities.
		 */
		flags: nk_resource_stream_flags @offset(4);
		/**
		 * Size in bytes, or UINT64_MAX when the provider cannot report it.
		 */
		size: u64 @offset(8);
		/**
		 * Reserved for future stream information; set all elements to zero.
		 */
		reserved: array<u64, 2> @offset(16);
	}
	/**
	 * Borrowed resource metadata decoded from an event payload.
	 */
	struct nk_resource_view @layout(72, 8) {
		/**
		 * Set to sizeof(nk_resource_view) before decoding an item.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Access and persistence flags for this resource.
		 */
		flags: nk_resource_flags @offset(4);
		/**
		 * Borrowed UTF-8 URI; valid until nk_event_release().
		 */
		uri: ptr<const<c_char>> @offset(8);
		/**
		 * URI length in bytes, excluding its trailing NUL.
		 */
		uri_length: u32 @offset(16);
		/**
		 * Borrowed optional MIME string, or NULL when absent.
		 */
		mime_type: ptr<const<c_char>> @offset(24);
		/**
		 * MIME length in bytes, excluding its trailing NUL.
		 */
		mime_type_length: u32 @offset(32);
		/**
		 * Borrowed optional display name, or NULL when absent.
		 */
		display_name: ptr<const<c_char>> @offset(40);
		/**
		 * Display-name length in bytes, excluding its trailing NUL.
		 */
		display_name_length: u32 @offset(48);
		/**
		 * Reserved for future decoded metadata; set all elements to zero.
		 */
		reserved: array<u64, 2> @offset(56);
	}
	/**
	 * Text and URI resources supplied to the platform share UI.
	 */
	struct nk_share_options @layout(56, 8) {
		/**
		 * Set to sizeof(nk_share_options) before sharing.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Reserved; set to zero.
		 */
		flags: u32 @offset(4);
		/**
		 * Optional title shown by the share UI.
		 */
		title: nullable<utf8> @offset(8);
		/**
		 * Optional UTF-8 text to share.
		 */
		text: nullable<utf8> @offset(16);
		/**
		 * Caller-owned resources read during the call and copied by NativeKit.
		 */
		resources: ptr<const<nk_resource>> @offset(24) @borrowed @length_field("resource_count");
		/**
		 * Number of entries in `resources`.
		 */
		resource_count: u32 @offset(32);
		/**
		 * Reserved; set to zero.
		 */
		reserved: u32 @offset(36);
		/**
		 * Reserved for future share options; set all elements to zero.
		 */
		reserved2: array<u64, 2> @offset(40);
	}
	/**
	 * Opaque handle for one audio mixer bus.
	 */
	handle nk_audio_bus : u32 @destroy("nk_audio_bus_destroy");
	/**
	 * Opaque handle for one effect inserted into an audio mixer bus.
	 */
	handle nk_audio_bus_effect : u32 @destroy("nk_audio_bus_effect_destroy");
	/**
	 * Opaque handle for one reusable audio clip source.
	 */
	handle nk_audio_clip : u32 @destroy("nk_audio_clip_destroy");
	/**
	 * Opaque handle for one independent playback voice.
	 */
	handle nk_audio_voice : u32 @destroy("nk_audio_voice_destroy");
}
