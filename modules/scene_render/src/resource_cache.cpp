#include "nativekit_scene_render.h"

namespace nkscene::render_internal {

/* Resource lifetime is deliberately a Phase 6 GPU concern. The headless plan
 * only retains stable scene resource IDs and revisions from the snapshot. */

} // namespace nkscene::render_internal
