// Generated by Haxeon from modules/ui/bindings/nativekit_ui_import.h for portable-abi64. Do not edit.
interface NativeKitUI @target("portable-abi64") @library("nativekit_ui") @depends("NativeKit") {
	/**
	 * Current UI ABI version.
	 */
	const NKUI_API_VERSION = 7;
	const NKUI_COMMAND_VERSION = 1;
	const NKUI_CUSTOM_EFFECT_PARAMETER_COMPONENTS = 20;
	const NKUI_EFFECT_PROGRAM_MAX_OPS = 8;
	const NKUI_GRADIENT_MAX_STOPS = 8;
	const NKUI_LAYER_COMMAND_VERSION = 2;
	const NKUI_LAYOUT_API_VERSION = 20;
	const NKUI_LAYOUT_MAX_TRANSACTION_BYTES = 16777216;
	/**
	 * Per-child cross-axis alignment override; zero inherits the parent.
	 */
	const NKUI_LAYOUT_NODE_ALIGN_SELF_OFFSET = 232;
	/**
	 * Width divided by height; zero disables aspect-ratio sizing.
	 */
	const NKUI_LAYOUT_NODE_ASPECT_RATIO_OFFSET = 204;
	const NKUI_LAYOUT_NODE_BACKGROUND_OFFSET = 52;
	/**
	 * Packed child alignment: x in bits 0..7, y in bits 8..15.
	 */
	const NKUI_LAYOUT_NODE_CHILD_ALIGNMENT_OFFSET = 172;
	/**
	 * Main-axis free-space distribution policy.
	 */
	const NKUI_LAYOUT_NODE_CHILD_DISTRIBUTION_OFFSET = 216;
	const NKUI_LAYOUT_NODE_CHILD_GAP_OFFSET = 48;
	const NKUI_LAYOUT_NODE_CLIP_FLAGS_OFFSET = 84;
	/**
	 * Horizontal gap between wrapped columns.
	 */
	const NKUI_LAYOUT_NODE_COLUMN_GAP_OFFSET = 224;
	const NKUI_LAYOUT_NODE_DIRECTION_OFFSET = 28;
	const NKUI_LAYOUT_NODE_FLAGS_OFFSET = 168;
	const NKUI_LAYOUT_NODE_FONT_FAMILY_OFFSET = 112;
	const NKUI_LAYOUT_NODE_FONT_SIZE_OFFSET = 116;
	/**
	 * Relative share of extra height for GROW sizing; must be positive and finite.
	 */
	const NKUI_LAYOUT_NODE_HEIGHT_GROW_WEIGHT_OFFSET = 212;
	/**
	 * Maximum height for FIT/GROW sizing; zero means unbounded.
	 */
	const NKUI_LAYOUT_NODE_HEIGHT_MAX_OFFSET = 200;
	/**
	 * Minimum height for FIT/GROW sizing; zero means no minimum.
	 */
	const NKUI_LAYOUT_NODE_HEIGHT_MIN_OFFSET = 196;
	const NKUI_LAYOUT_NODE_HEIGHT_SIZING_OFFSET = 20;
	const NKUI_LAYOUT_NODE_HEIGHT_VALUE_OFFSET = 24;
	const NKUI_LAYOUT_NODE_ID_OFFSET = 0;
	const NKUI_LAYOUT_NODE_LETTER_SPACING_OFFSET = 120;
	const NKUI_LAYOUT_NODE_LINE_HEIGHT_OFFSET = 124;
	/**
	 * Application-defined intrinsic-content version used by measurement caching.
	 */
	const NKUI_LAYOUT_NODE_MEASURE_VERSION_OFFSET = 236;
	const NKUI_LAYOUT_NODE_PADDING_BOTTOM_OFFSET = 44;
	const NKUI_LAYOUT_NODE_PADDING_LEFT_OFFSET = 32;
	const NKUI_LAYOUT_NODE_PADDING_RIGHT_OFFSET = 36;
	const NKUI_LAYOUT_NODE_PADDING_TOP_OFFSET = 40;
	const NKUI_LAYOUT_NODE_PARENT_OFFSET = 4;
	/**
	 * Parent-relative x offset for absolute-positioned nodes.
	 */
	const NKUI_LAYOUT_NODE_POSITION_X_OFFSET = 176;
	/**
	 * Parent-relative y offset for absolute-positioned nodes.
	 */
	const NKUI_LAYOUT_NODE_POSITION_Y_OFFSET = 180;
	const NKUI_LAYOUT_NODE_RADIUS_BOTTOM_LEFT_OFFSET = 76;
	const NKUI_LAYOUT_NODE_RADIUS_BOTTOM_RIGHT_OFFSET = 80;
	const NKUI_LAYOUT_NODE_RADIUS_TOP_LEFT_OFFSET = 68;
	const NKUI_LAYOUT_NODE_RADIUS_TOP_RIGHT_OFFSET = 72;
	const NKUI_LAYOUT_NODE_RECORD_BYTES = 248;
	/**
	 * Vertical gap between wrapped rows.
	 */
	const NKUI_LAYOUT_NODE_ROW_GAP_OFFSET = 220;
	const NKUI_LAYOUT_NODE_TEXT_ALIGNMENT_OFFSET = 132;
	const NKUI_LAYOUT_NODE_TEXT_COLOR_OFFSET = 96;
	const NKUI_LAYOUT_NODE_TEXT_DIRECTION_OFFSET = 136;
	const NKUI_LAYOUT_NODE_TEXT_FLAGS_OFFSET = 140;
	const NKUI_LAYOUT_NODE_TEXT_LENGTH_OFFSET = 92;
	const NKUI_LAYOUT_NODE_TEXT_OFFSET_OFFSET = 88;
	const NKUI_LAYOUT_NODE_TEXT_WRAP_OFFSET = 128;
	const NKUI_LAYOUT_NODE_TRANSFORM_A_OFFSET = 144;
	const NKUI_LAYOUT_NODE_TRANSFORM_B_OFFSET = 148;
	const NKUI_LAYOUT_NODE_TRANSFORM_C_OFFSET = 152;
	const NKUI_LAYOUT_NODE_TRANSFORM_D_OFFSET = 156;
	/**
	 * Normalized horizontal transform origin, in the range 0..1.
	 */
	const NKUI_LAYOUT_NODE_TRANSFORM_ORIGIN_X_OFFSET = 240;
	/**
	 * Normalized vertical transform origin, in the range 0..1.
	 */
	const NKUI_LAYOUT_NODE_TRANSFORM_ORIGIN_Y_OFFSET = 244;
	const NKUI_LAYOUT_NODE_TRANSFORM_TX_OFFSET = 160;
	const NKUI_LAYOUT_NODE_TRANSFORM_TY_OFFSET = 164;
	const NKUI_LAYOUT_NODE_VISUAL_KIND_OFFSET = 8;
	/**
	 * Relative share of extra width for GROW sizing; must be positive and finite.
	 */
	const NKUI_LAYOUT_NODE_WIDTH_GROW_WEIGHT_OFFSET = 208;
	/**
	 * Maximum width for FIT/GROW sizing; zero means unbounded.
	 */
	const NKUI_LAYOUT_NODE_WIDTH_MAX_OFFSET = 192;
	/**
	 * Minimum width for FIT/GROW sizing; zero means no minimum.
	 */
	const NKUI_LAYOUT_NODE_WIDTH_MIN_OFFSET = 188;
	const NKUI_LAYOUT_NODE_WIDTH_SIZING_OFFSET = 12;
	const NKUI_LAYOUT_NODE_WIDTH_VALUE_OFFSET = 16;
	/**
	 * Wrap policy: zero keeps one row/column, one enables wrapping.
	 */
	const NKUI_LAYOUT_NODE_WRAP_MODE_OFFSET = 228;
	/**
	 * Stacking order for absolute-positioned nodes, encoded as signed int32.
	 */
	const NKUI_LAYOUT_NODE_Z_INDEX_OFFSET = 184;
	const NKUI_LAYOUT_RESOLVED_ITEM_BYTES = 96;
	const NKUI_LAYOUT_TRANSACTION_HEADER_BYTES = 16;
	const NKUI_LAYOUT_TRANSACTION_VERSION = 15;
	/**
	 * 16-bit opcode identifying one display-list command record.
	 */
	enum nkui_command_opcode : u16 {
		/**
		 * Replace the current transform with a six-value affine matrix.
		 */
		NKUI_COMMAND_SET_TRANSFORM = 1;
		/**
		 * Select the paint resource used by subsequent drawing commands.
		 */
		NKUI_COMMAND_SET_PAINT = 2;
		/**
		 * Set the alpha multiplier applied to subsequent drawing commands.
		 */
		NKUI_COMMAND_SET_GLOBAL_ALPHA = 3;
		/**
		 * Select the compositing mode used by subsequent drawing commands.
		 */
		NKUI_COMMAND_SET_COMPOSITE_MODE = 4;
		/**
		 * Save the current transform, paint, alpha, composite, and clip state.
		 */
		NKUI_COMMAND_PUSH_STATE = 5;
		/**
		 * Restore the most recently saved drawing state.
		 */
		NKUI_COMMAND_POP_STATE = 6;
		/**
		 * Intersect the current clip with a logical-space rectangle.
		 */
		NKUI_COMMAND_CLIP_RECT = 7;
		/**
		 * Fill a previously created path resource.
		 */
		NKUI_COMMAND_DRAW_PATH = 8;
		/**
		 * Draw an image resource into a logical-space rectangle.
		 */
		NKUI_COMMAND_DRAW_IMAGE = 9;
		/**
		 * Draw a text layout at a logical-space position.
		 */
		NKUI_COMMAND_DRAW_TEXT_LAYOUT = 10;
		/**
		 * Start a composited layer with an opacity and composite mode.
		 */
		NKUI_COMMAND_BEGIN_LAYER = 11;
		/**
		 * End the most recently started layer.
		 */
		NKUI_COMMAND_END_LAYER = 12;
		/**
		 * Composite a render-target resource into a logical-space rectangle.
		 */
		NKUI_COMMAND_DRAW_RENDER_TARGET = 13;
		/**
		 * Stroke a path using the current paint and the command's stroke style.
		 */
		NKUI_COMMAND_STROKE_PATH = 14;
		/**
		 * Paint a geometry-based rounded-rectangle shadow.
		 */
		NKUI_COMMAND_DRAW_BOX_SHADOW = 15;
	}
	/**
	 * Alpha compositing mode supported by the current UI renderer.
	 */
	enum nkui_composite_mode : u32 {
		/**
		 * Draw new content over the existing destination.
		 */
		NKUI_COMPOSITE_SOURCE_OVER = 1;
	}
	/**
	 * Effect descriptors currently supported by a layer command.
	 */
	enum nkui_effect_kind : u32 {
		/**
		 * The layer has no sampled effect.
		 */
		NKUI_EFFECT_NONE = 0;
		/**
		 * Apply the supplied row-major 4x5 color matrix.
		 */
		NKUI_EFFECT_COLOR_MATRIX = 1;
		/**
		 * Apply a separable Gaussian blur; the compact operation stores sigma in slot 0.
		 */
		NKUI_EFFECT_BLUR = 2;
		/**
		 * Apply a subtree alpha drop shadow; the compact operation stores sigma, offset, and RGBA.
		 */
		NKUI_EFFECT_DROP_SHADOW = 3;
		/**
		 * Apply a renderer-registered custom effect.
		 */
		NKUI_EFFECT_CUSTOM = 4;
	}
	/**
	 * Font role used when adding a font file to a collection.
	 */
	enum nkui_font_family : u32 {
		/**
		 * General-purpose text font.
		 */
		NKUI_FONT_FAMILY_DEFAULT = 0;
		/**
		 * Font intended to provide emoji glyphs.
		 */
		NKUI_FONT_FAMILY_EMOJI = 1;
	}
	/**
	 * Sampling filter used when an image is scaled.
	 */
	enum nkui_image_filter : u32 {
		NKUI_IMAGE_FILTER_LINEAR = 1;
		NKUI_IMAGE_FILTER_NEAREST = 2;
	}
	/**
	 * Pixel format accepted by nkui_image_create().
	 */
	enum nkui_image_format : u32 {
		/**
		 * Invalid format; cannot be used to create an image.
		 */
		NKUI_IMAGE_FORMAT_INVALID = 0;
		/**
		 * One byte per pixel; rendered as white with the byte used as alpha.
		 */
		NKUI_IMAGE_R8 = 1;
		/**
		 * Four bytes per pixel in red, green, blue, alpha order.
		 */
		NKUI_IMAGE_RGBA8 = 2;
	}
	/**
	 * Flags describing why a layer needs an isolated render target.
	 */
	enum nkui_layer_flags : u32 {
		/**
		 * Allocate a separate target even when opacity is 1.
		 */
		NKUI_LAYER_ISOLATED = 1;
		/**
		 * The four bounds fields identify the logical target region.
		 */
		NKUI_LAYER_HAS_BOUNDS = 2;
	}
	/**
	 * Cross-axis child alignment values shared by the horizontal and vertical axes.
	 */
	enum nkui_layout_alignment : u32 {
		NKUI_LAYOUT_ALIGNMENT_START = 0;
		NKUI_LAYOUT_ALIGNMENT_END = 1;
		NKUI_LAYOUT_ALIGNMENT_CENTER = 2;
		/**
		 * Aligns child baselines in horizontal rows; baseline-less children use their bottom edge.
		 */
		NKUI_LAYOUT_ALIGNMENT_BASELINE = 3;
	}
	/**
	 * Persistent rendering policy for a retained custom-paint plane.
	 */
	enum nkui_layout_cache_policy : u32 {
		NKUI_LAYOUT_CACHE_NONE = 0;
		NKUI_LAYOUT_CACHE_AUTO = 1;
		NKUI_LAYOUT_CACHE_RASTER = 2;
	}
	/**
	 * Bit flags stored in each node's clip-flags field.
	 */
	flags nkui_layout_clip_flags : u32 {
		NKUI_LAYOUT_CLIP_HORIZONTAL = 1;
		NKUI_LAYOUT_CLIP_VERTICAL = 2;
	}
	/**
	 * Child flow directions encoded in node styles.
	 */
	enum nkui_layout_direction : u32 {
		NKUI_LAYOUT_DIRECTION_LEFT_TO_RIGHT = 0;
		NKUI_LAYOUT_DIRECTION_TOP_TO_BOTTOM = 1;
	}
	/**
	 * Main-axis free-space distribution policies.
	 */
	enum nkui_layout_distribution : u32 {
		NKUI_LAYOUT_DISTRIBUTION_START = 0;
		NKUI_LAYOUT_DISTRIBUTION_CENTER = 1;
		NKUI_LAYOUT_DISTRIBUTION_END = 2;
		NKUI_LAYOUT_DISTRIBUTION_SPACE_BETWEEN = 3;
		NKUI_LAYOUT_DISTRIBUTION_SPACE_AROUND = 4;
		NKUI_LAYOUT_DISTRIBUTION_SPACE_EVENLY = 5;
	}
	/**
	 * Flags returned in nkui_layout_measure_result.flags.
	 */
	flags nkui_layout_measure_flags : u32 {
		NKUI_LAYOUT_MEASURE_HAS_BASELINE = 1;
	}
	/**
	 * Node flags stored at NKUI_LAYOUT_NODE_FLAGS_OFFSET.
	 */
	flags nkui_layout_node_flags : u32 {
		NKUI_LAYOUT_NODE_VISIBLE = 1;
		NKUI_LAYOUT_NODE_FLOATING = 2;
		NKUI_LAYOUT_NODE_CLIP_TO_PARENT = 4;
		/**
		 * Excludes this node's own bounds from geometric hit testing.
		 */
		NKUI_LAYOUT_NODE_HIT_SELF_DISABLED = 8;
		/**
		 * Excludes this node's descendants from geometric hit testing.
		 */
		NKUI_LAYOUT_NODE_HIT_CHILDREN_DISABLED = 16;
	}
	/**
	 * Flags returned with each resolved item.
	 */
	flags nkui_layout_resolved_flags : u32 {
		NKUI_LAYOUT_RESOLVED_VISIBLE = 1;
		NKUI_LAYOUT_RESOLVED_HAS_BASELINE = 2;
	}
	/**
	 * Optional per-child cross-axis alignment override.
	 */
	enum nkui_layout_self_alignment : u32 {
		NKUI_LAYOUT_SELF_ALIGNMENT_INHERIT = 0;
		NKUI_LAYOUT_SELF_ALIGNMENT_START = 1;
		NKUI_LAYOUT_SELF_ALIGNMENT_END = 2;
		NKUI_LAYOUT_SELF_ALIGNMENT_CENTER = 3;
		NKUI_LAYOUT_SELF_ALIGNMENT_BASELINE = 4;
	}
	/**
	 * Sizing modes encoded for each node axis.
	 */
	enum nkui_layout_sizing : u32 {
		NKUI_LAYOUT_SIZING_FIT = 0;
		NKUI_LAYOUT_SIZING_GROW = 1;
		NKUI_LAYOUT_SIZING_FIXED = 2;
		NKUI_LAYOUT_SIZING_PERCENT = 3;
	}
	/**
	 * Visual content kinds encoded in the transaction.
	 */
	enum nkui_layout_visual_kind : u32 {
		NKUI_LAYOUT_VISUAL_BOX = 1;
		NKUI_LAYOUT_VISUAL_TEXT = 2;
		NKUI_LAYOUT_VISUAL_IMAGE = 3;
		NKUI_LAYOUT_VISUAL_CUSTOM = 4;
	}
	/**
	 * Whether children stay on one row/column or flow onto additional lines.
	 */
	enum nkui_layout_wrap_mode : u32 {
		NKUI_LAYOUT_WRAP_NO_WRAP = 0;
		NKUI_LAYOUT_WRAP_WRAP = 1;
	}
	/**
	 * Source-alpha masks are separate composition inputs, not sequential effects.
	 */
	enum nkui_mask_kind : u32 {
		NKUI_MASK_NONE = 0;
		NKUI_MASK_RECTANGLE = 1;
		NKUI_MASK_ROUNDED_RECT = 2;
		NKUI_MASK_CIRCLE = 3;
		NKUI_MASK_LINEAR_GRADIENT = 4;
		NKUI_MASK_IMAGE = 5;
	}
	/**
	 * End-cap style used when stroking an open path.
	 */
	enum nkui_path_line_cap : u32 {
		/**
		 * Stop the stroke at the endpoint.
		 */
		NKUI_PATH_LINE_CAP_BUTT = 0;
		/**
		 * Add a semicircular cap at the endpoint.
		 */
		NKUI_PATH_LINE_CAP_ROUND = 1;
		/**
		 * Extend the stroke by half its width at the endpoint.
		 */
		NKUI_PATH_LINE_CAP_SQUARE = 2;
	}
	/**
	 * Join style used where two stroked segments meet.
	 */
	enum nkui_path_line_join : u32 {
		/**
		 * Join segments with a circular arc.
		 */
		NKUI_PATH_LINE_JOIN_ROUND = 1;
		/**
		 * Join segments with a clipped corner.
		 */
		NKUI_PATH_LINE_JOIN_BEVEL = 3;
		/**
		 * Extend the outer edges to their intersection, subject to the miter limit.
		 */
		NKUI_PATH_LINE_JOIN_MITER = 4;
	}
	/**
	 * Geometry operation used by one element of a path.
	 */
	enum nkui_path_verb : u32 {
		/**
		 * Move the current point; values[0..1] are x and y.
		 */
		NKUI_PATH_MOVE_TO = 1;
		/**
		 * Add a straight segment; values[0..1] are x and y.
		 */
		NKUI_PATH_LINE_TO = 2;
		/**
		 * Add a cubic Bézier segment; values contain two controls and an endpoint.
		 */
		NKUI_PATH_BEZIER_TO = 3;
		/**
		 * Add a quadratic Bézier segment; values contain one control and an endpoint.
		 */
		NKUI_PATH_QUADRATIC_TO = 4;
		/**
		 * Add a tangent arc; values contain the two tangent points and radius.
		 */
		NKUI_PATH_ARC_TO = 5;
		/**
		 * Close the current contour; values are ignored.
		 */
		NKUI_PATH_CLOSE = 6;
	}
	/**
	 * Result returned by a NativeKit UI operation.
	 */
	enum nkui_result : i32 {
		/**
		 * The operation completed successfully.
		 */
		NKUI_OK = 0;
		/**
		 * An argument was null, out of range, or otherwise invalid.
		 */
		NKUI_ERROR_INVALID_ARGUMENT = -1;
		/**
		 * A handle is invalid, stale, or has already been destroyed.
		 */
		NKUI_ERROR_INVALID_HANDLE = -2;
		/**
		 * A display-list command stream failed structural or semantic validation.
		 */
		NKUI_ERROR_INVALID_TRANSACTION = -3;
		/**
		 * The operation could not allocate the required memory.
		 */
		NKUI_ERROR_OUT_OF_MEMORY = -4;
		/**
		 * The graphics backend could not initialize or execute the render.
		 */
		NKUI_ERROR_RENDERING = -5;
	}
	/**
	 * Horizontal alignment of paragraph lines.
	 */
	enum nkui_text_alignment : u32 {
		NKUI_TEXT_ALIGN_START = 0;
		NKUI_TEXT_ALIGN_CENTER = 1;
		NKUI_TEXT_ALIGN_END = 2;
	}
	/**
	 * Base direction requested for a paragraph.
	 */
	enum nkui_text_direction : u32 {
		NKUI_TEXT_DIRECTION_AUTO = 0;
		NKUI_TEXT_DIRECTION_LTR = 1;
		NKUI_TEXT_DIRECTION_RTL = 2;
	}
	/**
	 * Text navigation convention used by text editors.
	 */
	enum nkui_text_navigation_behavior : u32 {
		/**
		 * Standard Control-arrow word and paragraph movement.
		 */
		NKUI_TEXT_NAVIGATION_BEHAVIOR_STANDARD = 0;
		/**
		 * macOS Option-arrow word and paragraph movement.
		 */
		NKUI_TEXT_NAVIGATION_BEHAVIOR_MACOS = 1;
	}
	/**
	 * Word-breaking policy used by both explicit and automatic text layouts.
	 */
	enum nkui_text_wrap : u32 {
		/**
		 * Do not wrap the paragraph.
		 */
		NKUI_TEXT_WRAP_NONE = 0;
		/**
		 * Wrap at word boundaries.
		 */
		NKUI_TEXT_WRAP_WORD = 1;
		/**
		 * Wrap at word or character boundaries.
		 */
		NKUI_TEXT_WRAP_WORD_CHARACTER = 2;
	}
	/**
	 * Returns the UI ABI version implemented by this module.
	 */
	extern fn nkui_api_version() -> u32;
	/**
	 * Creates an empty display list.
	 *
	 * On NKUI_OK, writes the list handle to `out_list`. The list remains valid
	 * until nkui_display_list_destroy() and can be replaced repeatedly with
	 * nkui_display_list_submit().
	 */
	extern fn nkui_display_list_create(out_list: ptr<nkui_display_list> @out) -> nkui_result;
	/**
	 * Destroys a display list and releases its retained resource references.
	 *
	 * The list handle becomes invalid. Resources explicitly destroyed by the
	 * caller are kept alive until this release if the list still references them.
	 */
	extern fn nkui_display_list_destroy(list: nkui_display_list) -> nkui_result;
	/**
	 * Returns the byte and command counts for a display list.
	 *
	 * On NKUI_OK, writes a structure whose `struct_size` is set to
	 * sizeof(nkui_transaction_info) and whose `api_version` identifies the ABI.
	 */
	extern fn nkui_display_list_get_info(list: nkui_display_list, out_info: ptr<nkui_transaction_info> @out) -> nkui_result;
	/**
	 * Clears a display list while keeping its handle valid.
	 *
	 * Any resources retained by the previous command stream are released. The
	 * list can then accept a new stream with nkui_display_list_submit().
	 */
	extern fn nkui_display_list_reset(list: nkui_display_list) -> nkui_result;
	/**
	 * Validates and atomically replaces a display list's command stream.
	 *
	 * `commands` contains native-layout command records and may be NULL only when
	 * `command_bytes` is zero; submitting zero bytes clears the list. Every record
	 * must have a valid nkui_command_header, version, size, and resource kind.
	 * Validation happens before replacement, so an invalid stream leaves the old
	 * list unchanged. The command bytes are copied, and referenced resources are
	 * retained until the next successful replacement, reset, or destruction.
	 */
	extern fn nkui_display_list_submit(list: nkui_display_list, commands: ptr<const<u8>> @in_array("command_bytes"), command_bytes: u32) -> nkui_result;
	/**
	 * Adds a font file to a collection.
	 *
	 * `path` is a non-empty UTF-8 path and is copied by the module. Add at least
	 * one font before creating a layout; multiple fonts can provide fallback
	 * glyphs, with emoji fonts selected using NKUI_FONT_FAMILY_EMOJI.
	 */
	extern fn nkui_font_collection_add(fonts: nkui_resource, path: utf8, family: nkui_font_family) -> nkui_result;
	/**
	 * Adds a copied TTF/OTF byte buffer, suitable for asynchronously fetched browser assets.
	 */
	extern fn nkui_font_collection_add_data(fonts: nkui_resource, name: utf8, font_data: ptr<const<u8>> @in_array("font_bytes"), font_bytes: u32, family: nkui_font_family) -> nkui_result;
	/**
	 * Enables best-effort platform system-font fallback discovery for new layouts.
	 */
	extern fn nkui_font_collection_add_system_fallbacks(fonts: nkui_resource) -> nkui_result;
	/**
	 * Creates an empty font collection and writes its resource handle.
	 */
	extern fn nkui_font_collection_create(out_fonts: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Imports a sampled NativeKit graphics image as a compositable Canvas surface.
	 */
	extern fn nkui_graphics_surface_create(image: nk_graphics_image, out_surface: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Creates an image by copying a tightly packed pixel array.
	 *
	 * For NKUI_IMAGE_R8, `pixel_bytes` must equal width*height. For
	 * NKUI_IMAGE_RGBA8, it must equal width*height*4. Width and height must be
	 * positive, and on NKUI_OK the image resource is written to `out_image`.
	 */
	extern fn nkui_image_create(width: u32, height: u32, format: nkui_image_format, pixels: ptr<const<u8>> @in_array("pixel_bytes"), pixel_bytes: u32, out_image: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Creates an image with an explicit scaling filter.
	 */
	extern fn nkui_image_create_filtered(width: u32, height: u32, format: nkui_image_format, pixels: ptr<const<u8>> @in_array("pixel_bytes"), pixel_bytes: u32, filter: nkui_image_filter, out_image: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Decodes a PNG, JPEG, BMP, TGA, GIF, PSD, HDR, PIC, or PNM file as RGBA8.
	 */
	extern fn nkui_image_load_file(path: utf8, filter: nkui_image_filter, out_width: ptr<u32> @out, out_height: ptr<u32> @out, out_image: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Clears all custom-paint display lists attached to the session.
	 */
	extern fn nkui_layout_session_clear_custom_paints(session: nkui_layout_session) -> nkui_result;
	/**
	 * Creates a private layout session.
	 */
	extern fn nkui_layout_session_create(out_session: ptr<nkui_layout_session> @out) -> nkui_result;
	/**
	 * Destroys a layout session and releases its retained native frame state.
	 */
	extern fn nkui_layout_session_destroy(session: nkui_layout_session) -> nkui_result;
	/**
	 * Returns cumulative geometric hit-test counters for the session.
	 */
	extern fn nkui_layout_session_get_hit_test_stats(session: nkui_layout_session, out_stats: ptr<nkui_layout_hit_test_stats> @out) -> nkui_result;
	/**
	 * Returns cumulative intrinsic-measure counters and current cache occupancy.
	 */
	extern fn nkui_layout_session_get_measure_stats(session: nkui_layout_session, out_stats: ptr<nkui_layout_measure_stats> @out) -> nkui_result;
	/**
	 * Copies the resolved geometry for every node in submission order.
	 *
	 * The buffer contains `nkui_layout_item` records with `struct_size` set.
	 * Bounds remain in pre-transform layout coordinates; `transform` maps them to
	 * viewport coordinates. `clip_*` is the effective inherited clip, and
	 * `content_*` describes the union of direct children relative to the node's
	 * padded content origin. Pass NULL to query the required byte count, then
	 * pass a buffer of that size.
	 */
	extern fn nkui_layout_session_get_resolved_items(session: nkui_layout_session, out_buffer: nullable<ptr<u8>> @out_buffer("inout_bytes"), inout_bytes: ptr<u32> @inout) -> nkui_result;
	/**
	 * Returns the root-to-target geometric hit path for a viewport point.
	 *
	 * The returned path contains stable node IDs in root-to-target order. Pass
	 * NULL to query the required byte count, then provide a buffer containing at
	 * least that many bytes. The path buffer is an array of uint32_t values even
	 * though its capacity is reported in bytes. A point with no eligible target
	 * returns an empty path. Geometric questions only are considered: visibility,
	 * clipping, transforms, hit policies, and native paint order.
	 */
	extern fn nkui_layout_session_hit_test(session: nkui_layout_session, x: f32, y: f32, out_path: nullable<ptr<u8>> @out_buffer("inout_bytes"), inout_bytes: ptr<u32> @inout) -> nkui_result;
	/**
	 * Executes a geometric hit test into caller-owned reusable storage.
	 *
	 * `path` is a mutable byte buffer containing uint32_t node IDs in
	 * root-to-target order. `path_capacity_bytes` is its capacity in bytes and
	 * must be a multiple of sizeof(uint32_t). `out_count` receives the number of
	 * node IDs written. If the buffer is too small, no IDs are written,
	 * `out_count` receives the required number of IDs, and NKUI_ERROR_INVALID_ARGUMENT
	 * is returned so the caller can grow and retry without a size-query allocation.
	 * A zero-capacity buffer is valid when `path` is NULL.
	 */
	extern fn nkui_layout_session_hit_test_into(session: nkui_layout_session, x: f32, y: f32, path: ptr<u8> @in_array("path_capacity_bytes"), path_capacity_bytes: u32, out_count: ptr<u32> @out) -> nkui_result;
	/**
	 * Executes the submitted layout through the existing NativeKit renderer.
	 *
	 * The retained UI surface uses on-demand scheduling; request another frame
	 * with nk_surface_request_frame() after state, input, or animation changes.
	 * When load_existing is non-zero, the layout is composited over the current
	 * surface contents instead of clearing the frame first.
	 */
	extern fn nkui_layout_session_render_frame(renderer: nkui_renderer, session: nkui_layout_session, surface: nk_surface, frame_info: ptr<const<nkui_frame_info>>, load_existing: nk_bool) -> nkui_result;
	/**
	 * Selects whether a render-node subtree is rendered normally or cached as a raster.
	 */
	extern fn nkui_layout_session_set_cache_policy(session: nkui_layout_session, node_id: u32, policy: nkui_layout_cache_policy) -> nkui_result;
	/**
	 * Associates a retained display list with a custom-visual node from the most
	 * recently submitted tree. The session retains the list until it is replaced,
	 * cleared, or the session is destroyed.
	 */
	extern fn nkui_layout_session_set_custom_paint(session: nkui_layout_session, node_id: u32, display_list: nkui_display_list) -> nkui_result;
	/**
	 * Selects whether a custom-paint plane is rendered as vector content or cached as a raster.
	 */
	extern fn nkui_layout_session_set_custom_paint_cache_policy(session: nkui_layout_session, node_id: u32, policy: nkui_layout_cache_policy) -> nkui_result;
	/**
	 * Copies the font collection configuration into the layout session.
	 */
	extern fn nkui_layout_session_set_font_collection(session: nkui_layout_session, fonts: nkui_resource) -> nkui_result;
	/**
	 * Installs or removes the synchronous intrinsic measurer for custom-visual
	 * nodes. The callback is invoked during submit on the submitting thread. Its
	 * user data is borrowed and the callback must be detached before its Haxe/C
	 * callback handle is closed.
	 */
	extern fn nkui_layout_session_set_measure_callback(session: nkui_layout_session, callback: nkui_nullable_layout_measure_callback @retained, user_data: nullable<ptr<void>>) -> nkui_result;
	/**
	 * Submits one flat, Haxe-owned render/layout tree transaction.
	 *
	 * The transaction is little-endian and consists of a fixed header, node
	 * records of NKUI_LAYOUT_NODE_RECORD_BYTES bytes, and a UTF-8 string table.
	 * Node text offsets are absolute byte offsets from the beginning of the
	 * transaction. The native side copies all values before returning, so the
	 * input buffer may be reused. The transaction is bounded by
	 * NKUI_LAYOUT_MAX_TRANSACTION_BYTES; node capacity grows with submitted data.
	 * Each node transform is applied about its normalized resolved-box origin;
	 * clipped nodes must have an axis-aligned cumulative transform. The default
	 * origin is the center of the resolved box.
	 */
	extern fn nkui_layout_session_submit(session: nkui_layout_session, transaction: ptr<const<u8>> @in_array("transaction_bytes"), transaction_bytes: u32, frame: ptr<const<nkui_layout_frame_input>>) -> nkui_result;
	/**
	 * Creates an immutable linear gradient paint.
	 *
	 * The gradient line runs from (`start_x`, `start_y`) to (`end_x`, `end_y`)
	 * in the path's user coordinate space. `stops` must contain between two and
	 * NKUI_GRADIENT_MAX_STOPS entries with finite, strictly increasing offsets in
	 * the inclusive range 0..1. The endpoints must differ, and each color
	 * component must be finite and in the inclusive range 0..1. The colors and
	 * stop array are copied on success.
	 */
	extern fn nkui_paint_create_linear_gradient(start_x: f32, start_y: f32, end_x: f32, end_y: f32, stops: ptr<const<nkui_gradient_stop>> @in_array("stop_count"), stop_count: u32, out_paint: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Creates a solid paint from four finite RGBA components.
	 */
	extern fn nkui_paint_create_solid(color: nkui_color, out_paint: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Creates a path by copying `count` path elements.
	 *
	 * The array must contain at least one drawing operation and all coordinates
	 * used by each operation must be finite. On NKUI_OK, writes the path resource
	 * to `out_path`.
	 */
	extern fn nkui_path_create(elements: ptr<const<nkui_path_element>> @in_array("count"), count: u32, out_path: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Creates a renderer and writes its handle to `out_renderer`.
	 */
	extern fn nkui_renderer_create(out_renderer: ptr<nkui_renderer> @out) -> nkui_result;
	/**
	 * Destroys a renderer and releases its backend caches.
	 */
	extern fn nkui_renderer_destroy(renderer: nkui_renderer) -> nkui_result;
	/**
	 * Returns preparation and geometry-cache counters for a renderer.
	 */
	extern fn nkui_renderer_get_stats(renderer: nkui_renderer, out_stats: ptr<nkui_renderer_stats> @out) -> nkui_result;
	/**
	 * Registers one native-owned custom effect implementation.
	 *
	 * The supplied fragment source must use the standard NativeKit effect
	 * interface: effect_fs_params at fragment uniform block 1, tex_smp at image
	 * slot 0, and frag_color as the output. The renderer supplies the matching
	 * fullscreen vertex shader and backend pipeline. Input strings are copied by
	 * the renderer; registrations are recreated after device loss.
	 */
	extern fn nkui_renderer_register_custom_effect(renderer: nkui_renderer, registration: ptr<const<nkui_custom_effect_registration>>) -> nkui_result;
	/**
	 * Renders a display list using the surface's current framebuffer size.
	 *
	 * This convenience form uses a one-to-one logical-to-physical pixel scale.
	 * It selects on-demand scheduling for the retained UI surface, makes `surface`
	 * current, and executes the list, but does not present it; call
	 * nk_surface_present() after NKUI_OK. Request subsequent frames with
	 * nk_surface_request_frame() when state, input, or animation changes.
	 */
	extern fn nkui_renderer_render(renderer: nkui_renderer, list: nkui_display_list, surface: nk_surface) -> nkui_result;
	/**
	 * Renders a display list with explicit logical and framebuffer dimensions.
	 *
	 * `frame_info` must have a sufficient `struct_size`, positive finite logical
	 * dimensions and pixel scale, and positive framebuffer dimensions. The
	 * surface is switched to on-demand scheduling, made current, and the list is
	 * validated and executed. Rendering does not present the surface; call
	 * nk_surface_present() after NKUI_OK, then request later frames with
	 * nk_surface_request_frame() when retained UI state changes.
	 */
	extern fn nkui_renderer_render_frame(renderer: nkui_renderer, list: nkui_display_list, surface: nk_surface, frame_info: ptr<const<nkui_frame_info>>) -> nkui_result;
	/**
	 * Renders a display list over the currently presented frame without clearing it.
	 * The retained UI surface remains on-demand; request later frames with
	 * nk_surface_request_frame() after state, input, or animation changes.
	 */
	extern fn nkui_renderer_render_frame_overlay(renderer: nkui_renderer, list: nkui_display_list, surface: nk_surface, frame_info: ptr<const<nkui_frame_info>>) -> nkui_result;
	/**
	 * Releases the caller's ownership of a UI resource.
	 *
	 * A submitted display list keeps a referenced resource alive until that list
	 * is reset, replaced, or destroyed. After this call the handle cannot be used
	 * in a new command stream, even if an existing list still retains it.
	 */
	extern fn nkui_resource_destroy(resource: nkui_resource) -> nkui_result;
	/**
	 * Returns the nearest grapheme boundary to `offset`.
	 */
	extern fn nkui_text_layout_align_grapheme(layout: nkui_resource, offset: i32, out_offset: ptr<i32> @out) -> nkui_result;
	/**
	 * Returns visual caret geometry for a text position.
	 *
	 * `position` should normally be a value returned by
	 * nkui_text_layout_hit_test(). On NKUI_OK, writes the caret geometry to
	 * `out_caret`.
	 */
	extern fn nkui_text_layout_caret(layout: nkui_resource, position: nkui_text_position, out_caret: ptr<nkui_text_caret> @out) -> nkui_result;
	/**
	 * Shapes and lays out a UTF-8 string within a maximum width.
	 *
	 * `fonts` must be a valid collection containing at least one font. `width` and
	 * `font_size` are positive logical-pixel values. On NKUI_OK, writes a text
	 * layout resource to `out_layout`; the input string is consumed while the
	 * layout is built. A null text pointer represents an empty UTF-8 string.
	 */
	extern fn nkui_text_layout_create(fonts: nkui_resource, text: nullable<utf8>, width: f32, font_size: f32, out_layout: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Creates a text layout using the shared semantic text and paragraph styles; null text is empty.
	 */
	extern fn nkui_text_layout_create_styled(fonts: nkui_resource, text: nullable<utf8>, width: f32, text_style: ptr<const<nkui_text_style>>, paragraph_style: ptr<const<nkui_paragraph_style>>, out_layout: ptr<nkui_resource> @out) -> nkui_result;
	/**
	 * Returns the visual rectangles covered by a code-point selection.
	 *
	 * Query the required buffer size by passing NULL. On NKUI_OK, `inout_bytes` is
	 * set to the exact byte count. Each `nkui_text_rect` record has its
	 * `struct_size` field populated. Coordinates are relative to the text origin.
	 */
	extern fn nkui_text_layout_get_selection_rects(layout: nkui_resource, start: nkui_text_position, end: nkui_text_position, out_buffer: nullable<ptr<u8>> @out_buffer("inout_bytes"), inout_bytes: ptr<u32> @inout) -> nkui_result;
	/**
	 * Converts a logical-space point into the nearest text caret position.
	 *
	 * On NKUI_OK, writes a code-point offset and affinity to `out_position`.
	 * Coordinates are relative to the text layout's origin; pass the returned
	 * position to nkui_text_layout_caret() when its visual geometry is needed.
	 */
	extern fn nkui_text_layout_hit_test(layout: nkui_resource, x: f32, y: f32, out_position: ptr<nkui_text_position> @out) -> nkui_result;
	/**
	 * Returns the visual line range containing the code-point `offset`.
	 */
	extern fn nkui_text_layout_line_range_at(layout: nkui_resource, offset: i32, out_start: ptr<i32> @out, out_end: ptr<i32> @out) -> nkui_result;
	/**
	 * Returns the layout bounds in `out_metrics`.
	 */
	extern fn nkui_text_layout_measure(layout: nkui_resource, out_metrics: ptr<nkui_text_metrics> @out) -> nkui_result;
	/**
	 * Moves to a paragraph boundary using Control-arrow or macOS Option-arrow conventions.
	 */
	extern fn nkui_text_layout_move_paragraph(layout: nkui_resource, offset: i32, direction: i32, behavior: nkui_text_navigation_behavior, out_offset: ptr<i32> @out) -> nkui_result;
	/**
	 * Moves to a word boundary. `direction` must be -1 or +1. The behavior selects
	 * standard Control-arrow or macOS Option-arrow word movement.
	 */
	extern fn nkui_text_layout_move_word(layout: nkui_resource, offset: i32, direction: i32, behavior: nkui_text_navigation_behavior, out_offset: ptr<i32> @out) -> nkui_result;
	/**
	 * Returns the next grapheme boundary at or after `offset`.
	 */
	extern fn nkui_text_layout_next_grapheme(layout: nkui_resource, offset: i32, out_offset: ptr<i32> @out) -> nkui_result;
	/**
	 * Converts a shaped text position to its affinity-aware code-point insertion offset.
	 */
	extern fn nkui_text_layout_position_offset(layout: nkui_resource, position: nkui_text_position, out_offset: ptr<i32> @out) -> nkui_result;
	/**
	 * Returns the previous grapheme boundary at or before `offset`.
	 */
	extern fn nkui_text_layout_previous_grapheme(layout: nkui_resource, offset: i32, out_offset: ptr<i32> @out) -> nkui_result;
	/**
	 * Re-shapes an existing layout with new UTF-8 text while retaining its handle and style; null text
	 * is empty.
	 */
	extern fn nkui_text_layout_set_text(layout: nkui_resource, text: nullable<utf8>) -> nkui_result;
	/**
	 * Re-shapes an existing layout while retaining its native resource handle; null text is empty.
	 */
	extern fn nkui_text_layout_update(layout: nkui_resource, text: nullable<utf8>, width: f32, text_style: ptr<const<nkui_text_style>>, paragraph_style: ptr<const<nkui_paragraph_style>>) -> nkui_result;
	/**
	 * Returns a half-open insertion-offset range for the word under a hit-tested position.
	 */
	extern fn nkui_text_layout_word_range(layout: nkui_resource, position: nkui_text_position, out_start: ptr<i32> @out, out_end: ptr<i32> @out) -> nkui_result;
	/**
	 * Returns the Skribidi word range containing the code-point `offset`.
	 */
	extern fn nkui_text_layout_word_range_at(layout: nkui_resource, offset: i32, out_start: ptr<i32> @out, out_end: ptr<i32> @out) -> nkui_result;
	/**
	 * A normalized RGBA paint color.
	 */
	struct nkui_color @layout(16, 4) {
		/**
		 * Red component, normally in the range 0..1.
		 */
		red: f32 @offset(0);
		/**
		 * Green component, normally in the range 0..1.
		 */
		green: f32 @offset(4);
		/**
		 * Blue component, normally in the range 0..1.
		 */
		blue: f32 @offset(8);
		/**
		 * Alpha component, normally in the range 0..1.
		 */
		alpha: f32 @offset(12);
	}
	/**
	 * Fixed header present at the start of every display-list command record.
	 */
	struct nkui_command_header @layout(8, 4) {
		/**
		 * One of the NKUI_COMMAND_* opcode values.
		 */
		opcode: nkui_command_opcode @offset(0);
		/**
		 * Must match the opcode: NKUI_COMMAND_VERSION, or NKUI_LAYER_COMMAND_VERSION for BeginLayer.
		 */
		version: u16 @offset(2);
		/**
		 * Total record size in bytes, including this header; must be 4-byte aligned.
		 */
		size: u32 @offset(4);
	}
	/**
	 * Payload for NKUI_COMMAND_SET_COMPOSITE_MODE.
	 */
	struct nkui_composite_command @layout(12, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Compositing mode for subsequent drawing.
		 */
		mode: nkui_composite_mode @offset(8);
	}
	/**
	 * Backend-neutral descriptor for a renderer-owned custom effect.
	 */
	struct nkui_custom_effect_descriptor @layout(112, 4) {
		/**
		 * Positive ID of the native effect registration.
		 */
		registration_id: u32 @offset(0);
		/**
		 * Number of meaningful entries in parameters.
		 */
		parameter_count: u32 @offset(4);
		/**
		 * Reserved; must be 1 until multipass custom effects are introduced.
		 */
		pass_count: u32 @offset(8);
		/**
		 * Reserved; must be 1 (source input) until explicit custom graphs are introduced.
		 */
		sampling_inputs: u32 @offset(12);
		/**
		 * Logical ink expansion: left, top, right, bottom.
		 */
		ink_overflow: array<f32, 4> @offset(16);
		/**
		 * Typed parameters flattened according to the native registration schema.
		 */
		parameters: array<f32, 20> @offset(32);
	}
	/**
	 * Native shader implementation registered for a custom effect ID.
	 */
	struct nkui_custom_effect_registration @layout(80, 8) {
		/**
		 * Set to sizeof(nkui_custom_effect_registration).
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Positive application-defined ID carried by nkui_custom_effect_descriptor.
		 */
		registration_id: u32 @offset(4);
		/**
		 * Diagnostic name; copied by the renderer registration.
		 */
		name: utf8 @offset(8);
		/**
		 * GLSL 4.10 fragment source, or NULL when the backend does not use it.
		 */
		glsl410_fragment: nullable<utf8> @offset(16);
		/**
		 * GLSL ES 3.00 fragment source, or NULL when the backend does not use it.
		 */
		glsl300es_fragment: nullable<utf8> @offset(24);
		/**
		 * HLSL shader-model-5 fragment source, or NULL when the backend does not use it.
		 */
		hlsl5_fragment: nullable<utf8> @offset(32);
		/**
		 * Metal fragment source, or NULL when the backend does not use it.
		 */
		metal_macos_fragment: nullable<utf8> @offset(40);
		/**
		 * Number of meaningful float components in the custom descriptor.
		 */
		parameter_components: u32 @offset(48);
		/**
		 * Reserved; must be 1; the current runtime supports one registered pass.
		 */
		pass_count: u32 @offset(52);
		/**
		 * Reserved; must be 1 (source input).
		 */
		sampling_inputs: u32 @offset(56);
		/**
		 * Logical ink expansion: left, top, right, bottom.
		 */
		ink_overflow: array<f32, 4> @offset(60);
	}
	/**
	 * Payload for NKUI_COMMAND_DRAW_BOX_SHADOW.
	 */
	struct nkui_draw_box_shadow_command @layout(72, 4) {
		header: nkui_command_header @offset(0);
		x: f32 @offset(8);
		y: f32 @offset(12);
		width: f32 @offset(16);
		height: f32 @offset(20);
		offset_x: f32 @offset(24);
		offset_y: f32 @offset(28);
		/**
		 * Gaussian sigma; high-level ShadowBlur values are converted before encoding.
		 */
		blur_sigma: f32 @offset(32);
		spread: f32 @offset(36);
		radii: array<f32, 4> @offset(40);
		color: array<f32, 4> @offset(56);
	}
	/**
	 * Payload for image, text-layout, and render-target draw commands.
	 */
	struct nkui_draw_rect_command @layout(28, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Resource drawn by the command.
		 */
		resource: nkui_resource @offset(8);
		/**
		 * Left edge in logical coordinates.
		 */
		x: f32 @offset(12);
		/**
		 * Top edge in logical coordinates.
		 */
		y: f32 @offset(16);
		/**
		 * Draw width; must be non-negative and finite.
		 */
		width: f32 @offset(20);
		/**
		 * Draw height; must be non-negative and finite.
		 */
		height: f32 @offset(24);
	}
	/**
	 * One compact wire operation in a foreground or backdrop effect program.
	 */
	struct nkui_effect_op_command @layout(196, 4) {
		/**
		 * Operation kind; custom operations use the descriptor below.
		 */
		kind: nkui_effect_kind @offset(0);
		/**
		 * Matrix values, or compact scalar/color parameters decoded by the native compositor.
		 */
		color_matrix: array<f32, 20> @offset(4);
		/**
		 * Renderer-owned custom operation data; ignored for non-custom kinds.
		 */
		custom: nkui_custom_effect_descriptor @offset(84);
	}
	/**
	 * Describes the logical layout space and physical framebuffer target for one render.
	 */
	struct nkui_frame_info @layout(24, 4) {
		/**
		 * Set to sizeof(nkui_frame_info) or a larger compatible structure size.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Width of the UI's logical coordinate space in logical pixels.
		 */
		logical_width: f32 @offset(4);
		/**
		 * Height of the UI's logical coordinate space in logical pixels.
		 */
		logical_height: f32 @offset(8);
		/**
		 * Width of the target framebuffer in physical pixels.
		 */
		framebuffer_width: i32 @offset(12);
		/**
		 * Height of the target framebuffer in physical pixels.
		 */
		framebuffer_height: i32 @offset(16);
		/**
		 * Physical pixels per logical pixel; must be positive.
		 */
		pixel_scale: f32 @offset(20);
	}
	/**
	 * One normalized color stop in a gradient paint.
	 */
	struct nkui_gradient_stop @layout(20, 4) {
		/**
		 * Position along the gradient line, in the inclusive range 0..1.
		 */
		offset: f32 @offset(0);
		/**
		 * Color at this stop.
		 */
		color: nkui_color @offset(4);
	}
	/**
	 * Fixed prefix for the variable-length version 2 NKUI_COMMAND_BEGIN_LAYER record.
	 */
	struct nkui_layer_command @layout(84, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Layer opacity in the inclusive range 0..1. Values below 1 isolate the layer.
		 */
		opacity: f32 @offset(8);
		/**
		 * Compositing mode used when the layer is applied.
		 */
		composite_mode: nkui_composite_mode @offset(12);
		/**
		 * Left edge of the optional bounded target in logical coordinates.
		 */
		x: f32 @offset(16);
		/**
		 * Top edge of the optional bounded target in logical coordinates.
		 */
		y: f32 @offset(20);
		/**
		 * Width of the optional bounded target; must be positive when present.
		 */
		width: f32 @offset(24);
		/**
		 * Height of the optional bounded target; must be positive when present.
		 */
		height: f32 @offset(28);
		/**
		 * Combination of nkui_layer_flags.
		 */
		flags: nkui_layer_flags @offset(32);
		/**
		 * Source-alpha mask; use NKUI_MASK_NONE when no mask is present.
		 */
		mask: nkui_mask_descriptor @offset(36);
		/**
		 * Number of active foreground operations following this prefix.
		 */
		foreground_count: u32 @offset(76);
		/**
		 * Number of active backdrop operations following the foreground operations.
		 */
		backdrop_count: u32 @offset(80);
	}
	struct nkui_layer_v1_backdrop_command @layout(244, 4) {
		base: nkui_layer_v1_command @offset(0);
		effect_kind: nkui_effect_kind @offset(36);
		effect_matrix: array<f32, 20> @offset(40);
		mask: nkui_mask_descriptor @offset(120);
		backdrop_effect_kind: nkui_effect_kind @offset(160);
		backdrop_effect_matrix: array<f32, 20> @offset(164);
	}
	/**
	 * Common version 1 bounded layer prefix retained for wire decoding.
	 */
	struct nkui_layer_v1_command @layout(36, 4) {
		header: nkui_command_header @offset(0);
		opacity: f32 @offset(8);
		composite_mode: nkui_composite_mode @offset(12);
		x: f32 @offset(16);
		y: f32 @offset(20);
		width: f32 @offset(24);
		height: f32 @offset(28);
		flags: nkui_layer_flags @offset(32);
	}
	struct nkui_layer_v1_custom_effect_command @layout(148, 4) {
		base: nkui_layer_v1_command @offset(0);
		effect: nkui_custom_effect_descriptor @offset(36);
	}
	struct nkui_layer_v1_effect_command @layout(120, 4) {
		base: nkui_layer_v1_command @offset(0);
		effect_kind: nkui_effect_kind @offset(36);
		effect_matrix: array<f32, 20> @offset(40);
	}
	struct nkui_layer_v1_mask_command @layout(160, 4) {
		base: nkui_layer_v1_command @offset(0);
		effect_kind: nkui_effect_kind @offset(36);
		effect_matrix: array<f32, 20> @offset(40);
		mask: nkui_mask_descriptor @offset(120);
	}
	/**
	 * Version 1 unbounded layer record retained for wire decoding.
	 */
	struct nkui_layer_v1_unbounded_command @layout(16, 4) {
		header: nkui_command_header @offset(0);
		opacity: f32 @offset(8);
		composite_mode: nkui_composite_mode @offset(12);
	}
	/**
	 * Per-submission logical viewport and layout timing.
	 */
	struct nkui_layout_frame_input @layout(16, 4) {
		/**
		 * Set to sizeof(nkui_layout_frame_input) or a larger compatible size.
		 */
		struct_size: u32 @offset(0) @struct_size;
		width: f32 @offset(4);
		height: f32 @offset(8);
		delta_seconds: f32 @offset(12);
	}
	/**
	 * Cumulative geometric hit-test activity for one layout session.
	 */
	struct nkui_layout_hit_test_stats @layout(56, 8) {
		/**
		 * Set to sizeof(nkui_layout_hit_test_stats) when returned by the API.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Valid native hit-test queries performed by the session.
		 */
		hit_test_count: u64 @offset(8);
		/**
		 * Resolved scene nodes inspected across all queries.
		 */
		nodes_visited: u64 @offset(16);
		/**
		 * Scene branches rejected by visibility, bounds, or clipping.
		 */
		subtrees_rejected: u64 @offset(24);
		/**
		 * Precise inverse-transform self tests performed.
		 */
		precise_hit_tests: u64 @offset(32);
		/**
		 * Maximum resolved scene nodes inspected by one query.
		 */
		max_nodes_visited: u64 @offset(40);
		/**
		 * Monotonic elapsed time spent in native hit testing, in nanoseconds.
		 */
		hit_test_time_nanoseconds: u64 @offset(48);
	}
	/**
	 * Resolved geometry for one node in the most recently submitted layout.
	 */
	struct nkui_layout_item @layout(96, 4) {
		/**
		 * Set to sizeof(nkui_layout_item) or a larger compatible size.
		 */
		struct_size: u32 @offset(0) @struct_size;
		node_id: u32 @offset(4);
		flags: nkui_layout_resolved_flags @offset(8);
		/**
		 * Layout bounds before the returned world transform is applied.
		 */
		x: f32 @offset(12);
		y: f32 @offset(16);
		width: f32 @offset(20);
		height: f32 @offset(24);
		/**
		 * Effective ancestor clip in viewport coordinates, after transforms.
		 */
		clip_x: f32 @offset(28);
		clip_y: f32 @offset(32);
		clip_width: f32 @offset(36);
		clip_height: f32 @offset(40);
		/**
		 * Union of direct child bounds, relative to this node's content origin.
		 */
		content_x: f32 @offset(44);
		content_y: f32 @offset(48);
		content_width: f32 @offset(52);
		content_height: f32 @offset(56);
		/**
		 * World affine transform using x'=a*x+c*y+tx, y'=b*x+d*y+ty.
		 */
		transform: array<f32, 6> @offset(60);
		/**
		 * First text-line baseline in layout coordinates when HAS_BASELINE is set.
		 */
		baseline: f32 @offset(84);
		reserved: array<u32, 2> @offset(88);
	}
	/**
	 * Constraints supplied to an external intrinsic measurer for one custom node.
	 */
	struct nkui_layout_measure_constraints @layout(20, 4) {
		/**
		 * Always sizeof(nkui_layout_measure_constraints) for this ABI.
		 */
		struct_size: u32 @offset(0) @struct_size;
		min_width: f32 @offset(4);
		max_width: f32 @offset(8);
		min_height: f32 @offset(12);
		max_height: f32 @offset(16);
	}
	/**
	 * Optional metrics returned by an external intrinsic measurer.
	 */
	struct nkui_layout_measure_result @layout(20, 4) {
		/**
		 * Set to sizeof(nkui_layout_measure_result) when returned.
		 */
		struct_size: u32 @offset(0) @struct_size;
		width: f32 @offset(4);
		height: f32 @offset(8);
		baseline: f32 @offset(12);
		flags: nkui_layout_measure_flags @offset(16);
	}
	/**
	 * Cumulative intrinsic-measure activity for one layout session.
	 */
	struct nkui_layout_measure_stats @layout(56, 8) {
		/**
		 * Set to sizeof(nkui_layout_measure_stats) when returned by the API.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Intrinsic-measure requests made by Clay.
		 */
		requests: u64 @offset(8);
		/**
		 * Requests served by the persistent exact-constraint cache.
		 */
		cache_hits: u64 @offset(16);
		/**
		 * Requests not found in the persistent cache.
		 */
		cache_misses: u64 @offset(24);
		/**
		 * Calls made to the external intrinsic-measure callback.
		 */
		callback_calls: u64 @offset(32);
		/**
		 * Current number of entries in the bounded cache.
		 */
		cache_entries: u64 @offset(40);
		/**
		 * Maximum number of entries retained by the cache.
		 */
		cache_capacity: u64 @offset(48);
	}
	/**
	 * Descriptor for the source-alpha mask of an isolated layer.
	 */
	struct nkui_mask_descriptor @layout(40, 4) {
		/**
		 * Shape, gradient, or image mask kind.
		 */
		kind: nkui_mask_kind @offset(0);
		/**
		 * Image resource used only by NKUI_MASK_IMAGE.
		 */
		image: nkui_resource @offset(4);
		/**
		 * Kind-specific values; coordinates are normalized for gradients.
		 */
		values: array<f32, 8> @offset(8);
	}
	/**
	 * Paragraph-level wrapping, alignment, and line-height inputs.
	 */
	struct nkui_paragraph_style @layout(20, 4) {
		/**
		 * Set to sizeof(nkui_paragraph_style) or a larger compatible structure size.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Explicit line height, or zero for the natural line height.
		 */
		line_height: f32 @offset(4);
		wrap: nkui_text_wrap @offset(8);
		alignment: nkui_text_alignment @offset(12);
		direction: nkui_text_direction @offset(16);
	}
	/**
	 * One path operation supplied to nkui_path_create().
	 */
	struct nkui_path_element @layout(28, 4) {
		/**
		 * Operation encoded by this element.
		 */
		verb: nkui_path_verb @offset(0);
		/**
		 * Up to six finite coordinates; the active count depends on `verb`.
		 */
		values: array<f32, 6> @offset(4);
	}
	/**
	 * Payload for NKUI_COMMAND_CLIP_RECT.
	 */
	struct nkui_rect_command @layout(24, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Left edge in logical coordinates.
		 */
		x: f32 @offset(8);
		/**
		 * Top edge in logical coordinates.
		 */
		y: f32 @offset(12);
		/**
		 * Rectangle width; must be non-negative and finite.
		 */
		width: f32 @offset(16);
		/**
		 * Rectangle height; must be non-negative and finite.
		 */
		height: f32 @offset(20);
	}
	/**
	 * Preparation and geometry-cache counters for one renderer.
	 */
	struct nkui_renderer_stats @layout(536, 8) {
		/**
		 * Set to sizeof(nkui_renderer_stats) when returned by the API.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Number of NanoVG fill/stroke preparations performed after cache misses.
		 */
		path_preparations: u64 @offset(8);
		/**
		 * Number of retained path geometry cache hits.
		 */
		path_cache_hits: u64 @offset(16);
		/**
		 * Number of retained path geometry cache misses.
		 */
		path_cache_misses: u64 @offset(24);
		/**
		 * Number of vertices produced by direct path preparation.
		 */
		path_vertices_generated: u64 @offset(32);
		/**
		 * Vector capacity bytes allocated for newly prepared path geometry.
		 */
		path_geometry_bytes_allocated: u64 @offset(40);
		/**
		 * Nanoseconds spent inside NanoVG path preparation.
		 */
		path_tessellation_nanoseconds: u64 @offset(48);
		/**
		 * Vector capacity bytes currently retained by the path geometry cache.
		 */
		path_geometry_bytes_retained: u64 @offset(56);
		/**
		 * Render-plan commands submitted through this renderer.
		 */
		render_plan_commands: u64 @offset(64);
		/**
		 * Display-list render calls submitted through this renderer.
		 */
		display_list_count: u64 @offset(72);
		/**
		 * Cumulative display-list bytes processed by this renderer.
		 */
		display_list_bytes: u64 @offset(80);
		/**
		 * Completed GPU window and offscreen frames.
		 */
		gpu_frames: u64 @offset(88);
		/**
		 * GPU passes submitted by this renderer.
		 */
		gpu_passes: u64 @offset(96);
		/**
		 * GPU draw calls submitted by this renderer.
		 */
		gpu_draw_calls: u64 @offset(104);
		/**
		 * Live GPU buffer count.
		 */
		buffers_live: u64 @offset(112);
		/**
		 * Live GPU image count, excluding render-target images.
		 */
		images_live: u64 @offset(120);
		/**
		 * Live GPU sampler count.
		 */
		samplers_live: u64 @offset(128);
		/**
		 * Live GPU shader count.
		 */
		shaders_live: u64 @offset(136);
		/**
		 * Live GPU pipeline count.
		 */
		pipelines_live: u64 @offset(144);
		/**
		 * Live offscreen render-target count.
		 */
		render_targets_live: u64 @offset(152);
		/**
		 * Estimated live GPU buffer bytes.
		 */
		buffer_bytes: u64 @offset(160);
		/**
		 * Estimated live GPU image bytes, excluding render targets.
		 */
		image_bytes: u64 @offset(168);
		/**
		 * Estimated render-target color and depth storage bytes.
		 */
		render_target_bytes: u64 @offset(176);
		/**
		 * Bytes uploaded into GPU resources.
		 */
		upload_bytes: u64 @offset(184);
		/**
		 * Successfully created GPU resources.
		 */
		resource_creations: u64 @offset(192);
		/**
		 * GPU resource destruction requests completed.
		 */
		resource_destructions: u64 @offset(200);
		/**
		 * Surface/device identity changes observed by the GPU adapter.
		 */
		surface_recreations: u64 @offset(208);
		/**
		 * Fatal device-loss transitions.
		 */
		device_losses: u64 @offset(216);
		/**
		 * Failed GPU resource allocations.
		 */
		failed_allocations: u64 @offset(224);
		/**
		 * Live text atlas pages.
		 */
		atlas_pages: u64 @offset(232);
		/**
		 * Live text atlas bytes mirrored by the renderer.
		 */
		atlas_bytes: u64 @offset(240);
		/**
		 * Atlas page uploads issued by the UI renderer.
		 */
		glyph_uploads: u64 @offset(248);
		/**
		 * Monotonic text raster-scale generation observed by the renderer.
		 */
		atlas_scale_generation: u64 @offset(256);
		/**
		 * Retained text-layout cache hits.
		 */
		text_layout_cache_hits: u64 @offset(264);
		/**
		 * Retained text-layout cache misses.
		 */
		text_layout_cache_misses: u64 @offset(272);
		/**
		 * Custom paint nodes compiled by layout sessions.
		 */
		custom_paint_nodes: u64 @offset(280);
		/**
		 * Display-list bytes used by custom paint nodes.
		 */
		custom_paint_bytes: u64 @offset(288);
		/**
		 * Glyphs rasterized into the retained text atlas.
		 */
		glyphs_rasterized: u64 @offset(296);
		/**
		 * Atlas page generations created or rebuilt on the GPU.
		 */
		atlas_rebuilds: u64 @offset(304);
		/**
		 * Atlas partial subregion updates; currently zero when full uploads are used.
		 */
		atlas_partial_updates: u64 @offset(312);
		/**
		 * Bytes dirtied by glyph rasterization and submitted for atlas upload.
		 */
		atlas_dirty_upload_bytes: u64 @offset(320);
		/**
		 * Effect passes compiled by the UI compositor.
		 */
		effect_passes: u64 @offset(328);
		/**
		 * Mask passes compiled by the UI compositor.
		 */
		mask_passes: u64 @offset(336);
		/**
		 * Backdrop effect passes compiled by the UI compositor.
		 */
		backdrop_passes: u64 @offset(344);
		/**
		 * Isolated layers compiled by the UI compositor.
		 */
		isolated_layers: u64 @offset(352);
		/**
		 * Bounded layers compiled by the UI compositor.
		 */
		bounded_layers: u64 @offset(360);
		/**
		 * Transient render-target acquisitions served by the renderer pool.
		 */
		transient_target_pool_hits: u64 @offset(368);
		/**
		 * Transient render-target acquisitions that required allocation.
		 */
		transient_target_pool_misses: u64 @offset(376);
		/**
		 * Number of transient targets retained in the pool at the last query.
		 */
		transient_target_pool_count: u64 @offset(384);
		/**
		 * Estimated bytes retained by pooled transient render targets.
		 */
		transient_target_pool_bytes: u64 @offset(392);
		/**
		 * Effect results served by the persistent renderer cache.
		 */
		effect_cache_hits: u64 @offset(400);
		/**
		 * Effect results that required a new render pass.
		 */
		effect_cache_misses: u64 @offset(408);
		/**
		 * Number of effect results retained by the renderer cache.
		 */
		effect_cache_entries: u64 @offset(416);
		/**
		 * Estimated bytes retained by cached effect results.
		 */
		effect_cache_bytes: u64 @offset(424);
		/**
		 * Rasterized custom-paint results served by the persistent renderer cache.
		 */
		raster_cache_hits: u64 @offset(432);
		/**
		 * Rasterized custom-paint results that required a new render pass.
		 */
		raster_cache_misses: u64 @offset(440);
		/**
		 * Number of rasterized custom-paint results retained by the renderer cache.
		 */
		raster_cache_entries: u64 @offset(448);
		/**
		 * Estimated bytes retained by cached rasterized custom-paint results.
		 */
		raster_cache_bytes: u64 @offset(456);
		/**
		 * Sealed render plans accepted by the serialized render scheduler.
		 */
		render_submissions: u64 @offset(464);
		/**
		 * Pending render plans replaced by a newer plan before execution.
		 */
		render_submission_replacements: u64 @offset(472);
		/**
		 * Render plans whose frame tickets were cancelled before presentation.
		 */
		render_submission_cancellations: u64 @offset(480);
		/**
		 * Render plans that failed execution or could not be dispatched.
		 */
		render_submission_failures: u64 @offset(488);
		/**
		 * Render plans removed from the pending slot and attempted on RENDER.
		 */
		render_submission_executions: u64 @offset(496);
		/**
		 * Cumulative time spent preparing sealed render plans, in nanoseconds.
		 */
		render_submission_build_ns: u64 @offset(504);
		/**
		 * Cumulative time sealed plans waited in the pending scheduler slot, in nanoseconds.
		 */
		render_submission_queue_latency_ns: u64 @offset(512);
		/**
		 * Cumulative time spent executing sealed plans on RENDER, in nanoseconds.
		 */
		render_submission_execution_ns: u64 @offset(520);
		/**
		 * Cumulative time from frame acquisition until platform frame close, in nanoseconds.
		 */
		render_submission_acquire_to_present_ns: u64 @offset(528);
	}
	/**
	 * Payload for NKUI_COMMAND_SET_PAINT and resource-only draw commands.
	 */
	struct nkui_resource_command @layout(12, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Paint or path resource selected or drawn by this command.
		 */
		resource: nkui_resource @offset(8);
	}
	/**
	 * Payload for NKUI_COMMAND_SET_GLOBAL_ALPHA.
	 */
	struct nkui_scalar_command @layout(12, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Alpha multiplier in the inclusive range 0..1.
		 */
		value: f32 @offset(8);
	}
	/**
	 * Payload for NKUI_COMMAND_STROKE_PATH.
	 */
	struct nkui_stroke_path_command @layout(28, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Path resource to stroke.
		 */
		path: nkui_resource @offset(8);
		/**
		 * Stroke width in logical pixels; must be positive and finite.
		 */
		width: f32 @offset(12);
		/**
		 * End-cap style for open contours.
		 */
		line_cap: nkui_path_line_cap @offset(16);
		/**
		 * Join style for connected segments.
		 */
		line_join: nkui_path_line_join @offset(20);
		/**
		 * Positive miter limit used by NKUI_PATH_LINE_JOIN_MITER.
		 */
		miter_limit: f32 @offset(24);
	}
	/**
	 * Visual geometry and direction for a text caret.
	 */
	struct nkui_text_caret @layout(28, 4) {
		/**
		 * Set to sizeof(nkui_text_caret) when returned by the API.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * X coordinate of the caret baseline origin.
		 */
		x: f32 @offset(4);
		/**
		 * Y coordinate of the caret baseline origin.
		 */
		y: f32 @offset(8);
		/**
		 * Signed distance from the baseline to the top of the caret.
		 */
		ascender: f32 @offset(12);
		/**
		 * Distance from the baseline to the bottom of the caret.
		 */
		descender: f32 @offset(16);
		/**
		 * Horizontal caret slope per unit of vertical distance.
		 */
		slope: f32 @offset(20);
		/**
		 * Text-direction code at the caret; preserve it when selecting a visual direction.
		 */
		direction: nkui_text_direction @offset(24);
	}
	/**
	 * Bounding rectangle returned for a text layout.
	 */
	struct nkui_text_metrics @layout(20, 4) {
		/**
		 * Set to sizeof(nkui_text_metrics) when returned by the API.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Left edge of the bounds in layout coordinates.
		 */
		x: f32 @offset(4);
		/**
		 * Top edge of the bounds in layout coordinates.
		 */
		y: f32 @offset(8);
		/**
		 * Width of the laid-out text.
		 */
		width: f32 @offset(12);
		/**
		 * Height of the laid-out text.
		 */
		height: f32 @offset(16);
	}
	/**
	 * A text caret position measured in Unicode code points.
	 */
	struct nkui_text_position @layout(8, 4) {
		/**
		 * Zero-based code-point offset in the original UTF-8 text.
		 */
		offset: i32 @offset(0);
		/**
		 * Caret affinity returned by hit testing; pass it back unchanged to caret queries.
		 */
		affinity: u32 @offset(4);
	}
	/**
	 * One visual rectangle covered by a text selection.
	 */
	struct nkui_text_rect @layout(20, 4) {
		/**
		 * Set to sizeof(nkui_text_rect) in the returned buffer.
		 */
		struct_size: u32 @offset(0) @struct_size;
		x: f32 @offset(4);
		y: f32 @offset(8);
		width: f32 @offset(12);
		height: f32 @offset(16);
	}
	/**
	 * Font and inline spacing inputs shared by explicit and tree-owned layouts.
	 */
	struct nkui_text_style @layout(16, 4) {
		/**
		 * Set to sizeof(nkui_text_style) or a larger compatible structure size.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * Font role selected from the owning font collection.
		 */
		family: nkui_font_family @offset(4);
		/**
		 * Font size in logical pixels; must be positive and finite.
		 */
		font_size: f32 @offset(8);
		/**
		 * Additional horizontal spacing between adjacent characters.
		 */
		letter_spacing: f32 @offset(12);
	}
	/**
	 * Summary of the command stream currently stored in a display list.
	 */
	struct nkui_transaction_info @layout(16, 4) {
		/**
		 * Set to sizeof(nkui_transaction_info) when returned by the API.
		 */
		struct_size: u32 @offset(0) @struct_size;
		/**
		 * UI ABI version that produced this summary.
		 */
		api_version: u32 @offset(4);
		/**
		 * Number of command-stream bytes currently stored.
		 */
		command_bytes: u32 @offset(8);
		/**
		 * Number of validated command records currently stored.
		 */
		command_count: u32 @offset(12);
	}
	/**
	 * Payload for NKUI_COMMAND_SET_TRANSFORM.
	 */
	struct nkui_transform_command @layout(32, 4) {
		/**
		 * Command record header.
		 */
		header: nkui_command_header @offset(0);
		/**
		 * Affine matrix [a, b, c, d, tx, ty]; x'=a*x+c*y+tx and y'=b*x+d*y+ty.
		 */
		matrix: array<f32, 6> @offset(8);
	}
	/**
	 * Opaque handle for a retained display list.
	 */
	handle nkui_display_list : u32;
	/**
	 * Synchronous intrinsic measurement callback for custom-visual nodes.
	 *
	 * The callback may use unrelated UI registries, but must not re-enter layout
	 * session operations. Such re-entry returns NKUI_ERROR_INVALID_ARGUMENT.
	 */
	callback nkui_layout_measure_callback = fn(arg0: u32, arg1: nkui_layout_measure_constraints, arg2: nullable<ptr<void>>) -> nkui_layout_measure_result;
	/**
	 * Opaque retained layout session used by a Haxe-owned component tree.
	 */
	handle nkui_layout_session : u32;
	type nkui_nullable_layout_measure_callback = nullable<nkui_layout_measure_callback>;
	/**
	 * Opaque handle for a renderer and its backend resources.
	 */
	handle nkui_renderer : u32;
	/**
	 * Opaque handle for a path, paint, image, font collection, or text layout.
	 */
	handle nkui_resource : u32;
}
