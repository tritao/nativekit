package nativekit.ui.widgets;

/** Axes whose content may move inside a ScrollView viewport. */
class ScrollAxis {
	public static inline var Vertical:Int = 0;
	public static inline var Horizontal:Int = 1;
	public static inline var Both:Int = 2;
}
