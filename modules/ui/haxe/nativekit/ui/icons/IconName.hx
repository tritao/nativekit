package nativekit.ui.icons;

/** Typed names for vector icons embedded in NativeKit UI. */
enum abstract IconName(Int) from Int to Int {
	var Search = 0;
	var Close = 1;
	var Sun = 2;
	var Moon = 3;
	var Inspect = 4;
}
